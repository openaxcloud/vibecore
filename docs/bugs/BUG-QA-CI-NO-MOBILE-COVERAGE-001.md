---
id: BUG-QA-CI-NO-MOBILE-COVERAGE-001
---

## Bug

**P1 — Aucun signal automatisé à la largeur iPhone : le seul job CI qui teste en 390 px n'aboutit jamais, et l'étape E2E « mobile » n'exécute que 2 tests sur 21, aucun à une largeur de téléphone.** **(1) `i18n-live-audit.yml`** est le seul workflow avec une matrice de viewports (`desktop-1440`, `desktop-1024`, `tablet-768`, `mobile-390`). Sur les runs des 24 et 25/08, **`Playwright mobile-390` est `cancelled` à chaque fois** pendant que les 3 autres viewports passent en `success`. Cause : `timeout-minutes: 90` atteint — run `32873413018` démarré à 16:41:00, terminé à 18:12:06 = **91 min** ; l'étape « Run exhaustive EN/FR live audit » est annulée, puis « Verify complete proof set » échoue. Le shard 390 ne produit donc **aucune preuve**, alors que `tablet-768` en produit. **(2) `e2e.yml:141`**, étape nommée « Playwright mobile viewport tests », lance `tests/e2e/responsive-ide.spec.ts` avec **`--project=chromium`** (Desktop Chrome, 1280×720, `isMobile:false`). Or les tests sont gardés par `test.skip(!isMobile, …)` et `test.skip(!isCompactIdeProject(testInfo), …)` avec `isCompactIdeProject = project.name === 'mobile' \
'tablet'`. Décompte sur `origin/main` : **21 tests** → 3 exclus par `--grep-invert @runtime`, **16 ignorés** par ces gardes (7× `!isMobile`, 9× `!isCompactIdeProject`), **2 réellement exécutés** — `tablet exposes icon-only tab navigation…` et `tablet landscape uses the compact mobile IDE shell`, **tous deux en 1024×768 uniquement**. **Aucun `--project=mobile` ni `--project=tablet` n'existe dans aucun workflow** (`git grep -- '--project=' .github/workflows/` → seulement `chromium`, plus la matrice i18n). **(3)** Le projet Playwright `mobile` est **Pixel 7 (412×915, UA Android)** : même s'il était lancé, il ne couvrirait ni la largeur **390** ni **WebKit/iOS**. Conséquence : les régressions responsive à largeur iPhone — débordement horizontal, cibles tactiles < 44 px, panneaux tronqués — **ne peuvent pas être détectées par la CI**. C'est précisément le format de test principal d'Avi.

## 📤

☑ 16/09 — instruit par cette session, sans dispatch.

## 💻

☑ 16/09 — **(1)** le shard `Playwright mobile-390` de l'audit i18n **aboutit** désormais : job `104680998966` (run `35060966706`, PR #433, 16/09) **success**, « Run exhaustive EN/FR live audit » 10:36:40 → 10:48:33 soit **15 min** contre 91 le 25/08, avec « Verify complete proof set » vert — la preuve à 390 px existe. **(2)** l'étape « Playwright mobile viewport tests » de `e2e.yml` lance **`--project=mobile`** (Pixel 7) au lieu de `--project=chromium` : les 16 assertions gardées par `!isMobile` / `isCompactIdeProject` s'exécutent enfin à une largeur de téléphone ; le passage Chromium desktop du même spec reste couvert par l'étape principale (tout `tests/e2e` en `chromium`). **Épinglé par `scripts/garde-e2e-projet-mobile.spec.mjs`** (3 verts : la ligne `run:` de l'étape porte `--project=mobile` et non `chromium`, le projet `mobile` est un téléphone dans `playwright.config.ts`, et les gardes des tests reconnaissent bien ce nom) ; contre-épreuve : étape remise sur `chromium` → rouge. **(3)** reste partiel et dit tel quel : l'étape bloquante tourne à **412 px (Pixel 7, Chromium)** ; la largeur **390** est couverte par le shard i18n `mobile-390` (Chromium), et **WebKit/iOS** par le canari `webkit-iphone` (iPhone 15 Pro) ajouté à `e2e.yml`, **non bloquant** tant qu'il n'a pas quelques runs stables.

## ✅

☐ — à constater sur le premier run CI de ce changement : dans le job « Playwright local stack », l'étape « Playwright mobile viewport tests » doit exécuter bien plus que 2 tests (les 18 du spec hors `@runtime`, moins ceux réservés à la tablette), au projet `mobile`.

## Preuve

Relevé du 24-25/08 (runs `32873413018` et voisins) : shard `mobile-390` `cancelled` 3/3 à 91 min ; `git grep -- '--project=' .github/workflows/` → uniquement `chromium` + la matrice i18n. Relevé du 16/09 : shard `mobile-390` **success** en 15 min (job `104680998966`). **Contrôle local avant push, 16/09, `responsive-ide.spec.ts --project=mobile --grep-invert @runtime`, pile locale (API 3001 / web 5175, build `d094df01`), Pixel 7 (412×915), première exécution de ces assertions à une largeur de téléphone** : 18 tests → **6 verts, 2 ignorés, 10 rouges** (8,1 min). Les dix, classés un par un :

| # | test | erreur mesurée | classement | suite |
|---|---|---|---|---|
| 1, 4 | rail des réglages « readable » / « AI provider settings controls » | `Expected >= 120, Received 80.08` (largeur d'une puce) | assertion écrite pour la colonne TABLETTE ; sur téléphone le rail est une rangée de puces dont la largeur suit le libellé (« AI » 40 px, « Preferences » 119 px), libellés entiers, 44 px de haut | assertion remplacée : libellé non coupé + cible ≥ 44 px dans les deux sens |
| — | (trouvé par la nouvelle assertion) | puce « AI » **40 × 44 px** | **vrai défaut** : `min-width: 0 !important` sur téléphone | plancher `var(--vc-touch-min, 44px)` posé (`index.scss`, règle téléphone du rail) |
| 2 | « agent model controls and composer menus usable » | `waiting for getByRole('button', { name: 'More composer tools' })` (180 s) | sélecteur périmé : le bouton s'appelle « More composer & tools » | corrigé ; **reliquat** : l'étape suivante attend `agent-model-selector`, or `ModelSelector.tsx` n'est plus importé nulle part dans l'app (composant mort) — le test vise une surface disparue, à réécrire vers la feuille « Agent » mobile |
| 3 | « command palette and panel action menus inside the viewport » | `waiting for getByRole('textbox', { name: 'Search commands' })` (180 s) | sélecteur périmé : le champ est un `combobox`, les entrées des `option` | corrigé (combobox + option) |
| 5 | « run button controls the real preview runtime » | `mobile-editor-run-toggle` reste « Run project » | a besoin d'un pod d'exécution, comme ses trois voisins de bureau tagués `@runtime` | tagué `@runtime` (exclu par `--grep-invert @runtime`, couvert par le workflow E2E Runtime) |
| 6 | « visible webview startup state until the iframe renders » | `mobile toolbar right : Expected <= 413, Received 417.47` | **reliquat, vrai défaut probable** : la barre d'outils de l'aperçu dépasse de 5 px pendant l'état de démarrage (à l'état stable, mesurée à 412/412 : grille `364px 32px`) | à instruire : état de démarrage avec le sélecteur d'appareil |
| 7, 10 | « menus follow dark and light theme tokens » / « one canonical tools palette » | après `Escape`, `tools-sheet` reste visible ; `mobile-more-menu-sheet` compte 1 au lieu de 0 | **vrai défaut, cause MESURÉE** (sonde hors E2E, Pixel 7) : `useKeybindings` possède Échap en phase CAPTURE sur window et arrête sa propagation — tracé dans la page : capture reçue avec `defaultPrevented=true`, aucune phase de bulle ni sur window ni sur document ; l'écouteur de bulle des feuilles mobiles (BaseChat ~3522) ne tournait donc jamais. Deux voisins (menu d'options des panneaux, menu contextuel des messages) avaient déjà constaté ce mécanisme | **corrigé** : l'action `overlay.close`, propriétaire d'Échap, ferme d'abord la feuille mobile ouverte. Sonde sur build local corrigé : feuille « more » 1 → **0** au premier Échap |
| 8 | « mobile opens the agent by default and uses panel URLs for restore » | `data-mobile-panel` : Expected "chat", Received "files" | **reliquat, décision** : un `ide-state` stocké avec `mobilePanel: 'files'` est restauré (BaseChat ~5410) alors que le test attend l'agent par défaut sans `?panel=` — le test est la spec d'origine, l'app a divergé sans qu'aucun test ne rougisse | à trancher (Avi) : restaurer le dernier panneau, ou ouvrir l'agent par défaut |
| 9 | « mobile can deep-link to real IDE service panels » | en-tête : Expected "Files", Received "Library" | sélecteur périmé : le panneau s'appelle « Library » (parité Replit) | corrigé |

**Seconde passe, même pile, build avec le plancher tactile (12:33)** : 17 tests (le test `@runtime` désormais exclu) → **8 verts, 2 ignorés, 7 rouges** (4,9 min). Les deux tests du rail des réglages sont verts (puce « AI » ≥ 44 px, libellés non coupés). Restent rouges : 2 (`agent-model-selector`, composant mort), 3 (la correction `option` de la palette a été posée APRÈS le lancement de cette passe — non mesurée en local, le canari CI le dira), 6 (barre d'outils 417 px), 7 et 10 (Échap), 8 (panneau restauré), et 9 qui a progressé jusqu'à `getByRole('button', { name: 'Backups' })` dans le panneau `database` — **reliquat** : ce bouton n'existe plus tel quel sur mobile, à réaligner sur la surface actuelle.

**Décision** : l'étape passe au projet `mobile` en **canari non bloquant** (résumé imprimé), même règle que le canari iOS — ces assertions n'avaient jamais tourné, et les reliquats ci-dessus sont des défauts de tests ou de produit à instruire un par un, pas à contourner. Bascule en bloquant quand le résumé est vert sur quelques runs.
