---
id: BUG-I18N-008
---

## Bug

**P3 — PANNEAU GIT, ÉTAT VIDE : « Aucun dépôt distant connecté » s'affiche DEUX FOIS d'affilée.** Ce ne sont pas deux rendus de la même chaîne mais **deux chaînes de catalogues différents au texte identique** : `idePanels.git.noRemote` dans `GitTab.tsx`, et `gitProvider.title` dans `GitProviderConnectPanel.tsx`, imbriqué juste en dessous. C'est ce qui rendait le doublon invisible à la relecture d'un seul fichier.

## 📤 Dispatché

☑

## 💻 Codé

☑ *(la ligne extérieure retirée ; le titre du panneau reste — il porte la description et précède les actions)*

## ✅ Testé live

✅ 19/08

## Preuve

**Certifié live 19/08** aux **3 formats** (390/768/1440) : la phrase apparaît désormais **une seule fois** dans l'état vide du panneau Git.

