# V3_05_REMIX_AUDIT_20260720 — audit licence/PII du remix (directive point 6)

schemaVersion: 1
repoCommit: a4a9f71c
reviewer: UNKNOWN

V3-05 ne sera considéré clos QUE quand chaque exigence ci-dessous est prouvée
ET re-signée par le relecteur, ET la politique fail-closed appliquée (PR #25).
État par exigence (preuves rejouables) :

| Exigence | État | Preuve |
|---|---|---|
| sourceRelease immuable | ✅ prouvé | pin snapshot (test « clones the PINNED snapshot » — la modif live post-snapshot n'apparaît pas dans le clone) |
| credentials détachés AVANT clonage | ✅ prouvé | machine à états (CLONING refusé avant CREDENTIALS_DETACHED, 409) |
| recherche valeur secret fichiers/DB/jobs | ✅ prouvé (fichiers+DB+job) ; logs/historique : partiel | test « secret is nowhere » (fichiers, secrets/env du clone, JSON du job) ; recherche dans LOGS et HISTORIQUE git non couverte → à ajouter |
| PII : aucune copie sans masking/consentement | ✅ prouvé | SOURCE_SANITIZED masque email/tél/IBAN/carte ; consentement auteur versionné sinon masqué ; e2e CI run 29747404378 ✓ |
| licence + consentVersion enregistrées | ✅ prouvé | licenseSnapshot sha256 immuable + consentVersion sur le job (test immutabilité) |
| attribution conservée | 🟡 partiel | provenance sourceListingId/sourceSnapshotId sur le job ; attribution VISIBLE côté clone (UI) non implémentée → à faire |
| fail-closed sans licence | 💻 codé (PR #25, non mergée) | 403 REMIX_LICENSE_REQUIRED + défaut false + 4 tests négatifs |
| aucun partage cross-tenant implicite | ✅ prouvé | clone dans l'org du remixeur uniquement (test e2e : l'org ne contient QUE le clone) ; storagePolicy DETACH forcé |
| quarantaine si valeur sensible survit | ✅ prouvé | SCANNING → FAILED + 409 REMIX_SECRET_LEAK (test) ; résiduel PII → RemixInvariantError |

RESTE pour clôture : merge PR #25 + recherche secrets dans logs/historique +
attribution visible + preuve LIVE PROD + re-signature relecteur (reçu complet).
