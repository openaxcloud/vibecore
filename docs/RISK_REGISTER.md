# Risk Register

Date: 2026-05-04

Reviewed commit: `3cbc1d2` plus current workspace changes.

| ID | Risk | Severity | Likelihood | Evidence | Impact | Mitigation | Owner | Status |
|---|---|---:|---:|---|---|---|---|---|
| R-001 | Remote Kubernetes runtime not validated live | Critical | High | `runtime:validate:api-kubernetes` fails during preflight because workspace-manager health is not reachable at `http://127.0.0.1:3010/health`; GKE drill not run | SaaS IDE runtime can fail for users | Start/expose workspace-manager against staging GKE and run `Staging Runtime Validation` | Platform/Runtime | Open |
| R-002 | Workspace isolation not proven in cluster | Critical | Medium | gVisor, Kyverno and NetworkPolicy source/tests exist; live `networkpolicies:validate:live` fails at Kubernetes reachability preflight | User workloads could reach platform/internal services | Install policies in staging and run denied-traffic probes | Security/SRE | Open |
| R-003 | Stripe live webhook flow not verified | Critical | Medium | Local webhook/idempotency tests exist; `production:validate` lacks Stripe secrets/catalog | Paid users can be billed or quotaed incorrectly | Stripe CLI/test-mode drill for checkout, portal and all billing events | Billing | Open |
| R-004 | Capacity unknown for 1,000 and 10,000 users | Critical | High | k6 scripts exist but no reports | DB, Redis, GKE, preview or AI providers can overload | Run staged k6 and update capacity/cost model | SRE | Open |
| R-005 | Backup restore not proven | Critical | Medium | Backup dry-run checksum passes; no Cloud SQL/storage restore drill | Data loss or long outage | Execute staging restore, record RTO/RPO | SRE | Open |
| R-006 | Enterprise SSO/SCIM only code-tested | High | Medium | OIDC/SAML/SCIM handlers and tests exist; no real IdP proof | Enterprise customers may fail login/provisioning | Test Google/GitHub/Entra/Okta/SAML/SCIM clients in staging | Identity | Open |
| R-007 | Admin browser audit incomplete | High | Medium | Static guard validates 21 admin mutations for re-auth/AdminAuditLog, API tests cover re-auth/MFA/AdminAuditLog for key dangerous actions including manual abuse events, and admin UI smoke passes; no full browser dangerous-action audit | Dangerous mutation could bypass re-auth or audit through an untested UI path | Playwright admin action audit and AdminAuditLog assertions | Admin/Security | Open |
| R-008 | Secret leakage through long-tail outputs | Critical | Low-Medium | Shared string redaction now masks canary, OpenAI, GitHub, Google OAuth and Slack-shaped secrets even under neutral keys; API canary/security tests pass; AI tool route tests verify redaction in responses and persisted `AiToolCall.output`. Live Kubernetes runtime/admin/export canary still not fully proven end-to-end | Credential exposure | End-to-end canary through live runtime command output, admin views, audit export and deployment logs | Security | Open |
| R-009 | Preview ingress/custom domains unproven live | High | Medium | preview-proxy tests pass; wildcard TLS/custom domain not live-tested | Preview/customer domains may fail | Staging wildcard TLS, preview-proxy and custom-domain drill | Platform/SRE | Open |
| R-010 | Mobile/tablet quality not device-proven | Medium | Medium | CodeMirror fallback tests and mobile build pass | Mobile beta users may hit save/input/layout bugs | Real-device QA matrix | Web/Mobile | Open |
| R-011 | Desktop release artifacts not verified | Medium | Medium | `desktop:test` passes smoke only | Desktop users may receive broken signed binaries | macOS/Windows/Linux signed release matrix | Desktop | Open |
| R-012 | CI/CD not proven with real secrets | High | Medium | `cicd:validate` passes; workflows not run with production environments here | Deploy could fail at credentials, IAM, Helm or rollback | Execute full PR->staging->prod and rollback | DevOps | Open |
| R-013 | Legal pages not counsel-approved | High | High | Terms/privacy/AUP exist; no DPA/subprocessor signoff | Paid/public compliance risk | Counsel review and publish final legal set | Legal | Open |
| R-014 | Abuse thresholds not tuned | Medium | Medium | Command/usage abuse detection tests exist; no real traffic tuning | False positives or missed abuse | Run abuse drills and SIEM observation | Trust/Safety | Open |
| R-015 | Observability not live | High | Medium | `sre:validate` passes assets; no deployed dashboard/alerts proof | Incidents may be invisible | Deploy Prometheus/Grafana/OTLP/synthetics and verify alerts | SRE | Open |
| R-016 | Deployment providers not sandbox-proven | High | Medium | Deploy hook/rollback code and tests exist | Customer deploys could fail live | Run provider sandbox deploy and rollback E2E | Deployments | Open |
| R-017 | Native mobile release not proven | Medium | Medium | mobile validation/build-web pass; no signed store artifacts | Mobile release could be undistributable | TestFlight and Play internal signed releases | Mobile | Open |
| R-018 | Collaboration lacks CRDT/OT | High | Medium | Optimistic locking prevents silent overwrite but causes conflicts | Multi-writer editing can degrade UX | Integrate CRDT/OT or restrict beta collaboration scope | Collaboration | Open |
| R-019 | CSP still permits inline styles | Medium | Medium | `script-src` no longer allows `'unsafe-inline'`/`'unsafe-eval'` (security-routes spec line 127). `style-src` keeps `'unsafe-inline'` because Remix-Vite emits hydration `<style>` tags and `/static-deployments/:id/*` serves operator-owned CSS we don't author | Residual XSS surface is limited to style-only injection — `script-src` blocks the active-execution path, and X-Frame-Options/COOP block exfiltration via embedding | Implement style nonces/hashes and browser-verify before GA; meanwhile `connect-src` is bound to the API origin, `frame-ancestors 'none'` blocks UI redress | Security/Web | Accepted for private beta (re-open before GA — Go-Live gate line 114) |
| R-020 | Production environment incomplete | Critical | High | `production:validate` fails on OAuth/OIDC/SAML/email/SIEM/prod DB/Redis/Stripe/runtime/monitoring/SOC2 | Production deploy must be blocked | Populate real staging/prod secret set and rerun validation | Platform/SRE | Open |
| R-021 | Workspace manager unavailable in local/staging validation path | Critical | High | `runtime:validate:api-kubernetes` now fails before test-data creation because workspace-manager health is unavailable; the API also returns a controlled 502 instead of an opaque 500 if the manager disappears after preflight | IDE backend runtime unavailable | Start/fix workspace-manager deployment and rerun runtime validation | Platform/Runtime | Open |
| R-022 | Live NetworkPolicy drill cannot run from current context | Critical | High | `networkpolicies:validate:live` fails during `kubectl cluster-info` preflight because the current context points at localhost:8080 and is refused | No evidence that workspace pods are isolated | Configure staging kube context and rerun with exact blocked CIDRs | SRE/Security | Open |
