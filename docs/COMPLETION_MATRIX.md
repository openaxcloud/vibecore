# Completion Matrix

Date: 2026-05-04

Reviewed commit: `3cbc1d2` plus the current uncommitted readiness, runtime, mobile, CI/CD, and 403-access fixes in this workspace.

Status definitions:

- `complete`: implemented, integrated, and covered by meaningful automated checks in this repo.
- `partial`: implemented or scaffolded, but live proof, external provider proof, scale proof, release signing, or exhaustive route coverage is missing.
- `missing`: no meaningful implementation or no acceptable proof exists.

This matrix is intentionally strict. A feature with good source coverage is still `partial` when the remaining risk depends on a real staging or production environment.

Latest repo-local validation on 2026-05-04:

- `pnpm run platform:verify`: passed.
- `pnpm exec playwright test tests/e2e --project=chromium --reporter=line`: passed, 42 passed and 2 mobile-only skipped.
- `pnpm exec playwright test tests/e2e --project=tablet --reporter=line`: passed, 42 passed and 2 mobile-only skipped.
- `pnpm exec playwright test tests/e2e --project=mobile --reporter=line`: passed, 31 passed and 13 desktop/tablet-only skipped.
- `pnpm run ide:panel-audit && pnpm run ide:panel-audit:validate`: passed, 81/81 panels.
- `pnpm run mobile:validate`: passed.
- `pnpm run mobile:build:web`: passed.
- `pnpm run desktop:test`: passed.
- `pnpm run mobile:validate:release`: failed because the production Android app link host is still `app.example.com`.
- `pnpm run mobile:release-assets:check`: failed because `MOBILE_APP_LINK_HOST` is not configured.
- `pnpm run production:validate`: failed because real production/staging secrets and HTTPS provider/runtime URLs are not configured in this workstation environment.
- `pnpm run runtime:validate:api-kubernetes`: failed during workspace-manager health preflight at `http://127.0.0.1:3010/health`.
- `pnpm run networkpolicies:validate:live`: failed during Kubernetes cluster reachability preflight because the active context points to unavailable `localhost:8080`.

| # | Item | Status | Files implemented | Tests implemented | Commands run | Blockers | Production risk | Next action |
|---|---|---|---|---|---|---|---|---|
| 1 | Bolt IDE preserved | complete | `app/components/workbench/*`, `app/components/chat/*`, `app/routes/projects.$projectId.ide.tsx`, `docs/BOLT_BASELINE_MAP.md` | `app/lib/runtime/message-parser.spec.ts`, `tests/e2e/dashboard.spec.ts`, `tests/e2e/responsive-ide.spec.ts`, `scripts/audit-ide-panels.mjs` | `pnpm run test`, `pnpm run build`, `pnpm run ide:panel-audit`, `pnpm exec playwright test tests/e2e --project=chromium --reporter=line`, `pnpm exec playwright test tests/e2e --project=tablet --reporter=line`, `pnpm exec playwright test tests/e2e --project=mobile --reporter=line` | None found in repo-local audit | Medium: legacy Bolt/WebContainer compatibility remains complex | Keep IDE panel audit and Playwright IDE regressions required on every release |
| 2 | RuntimeAdapter complete | partial | `packages/runtime-contract/src/index.ts`, `packages/runtime-webcontainer/src/index.ts`, `packages/runtime-remote/src/index.ts`, `app/lib/runtime/RuntimeAdapterProvider.tsx` | `packages/runtime-webcontainer/src/index.spec.ts`, `packages/runtime-remote/src/index.spec.ts`, workspace-manager/agent tests | `pnpm run test`, `pnpm run typecheck`, `pnpm run runtime:validate:api-kubernetes` | API Kubernetes validation currently fails during preflight because `http://127.0.0.1:3010/health` is not reachable; validator now checks workspace-manager before creating test data and uses returned `session.id` for pod/service checks | Critical for SaaS production runtime | Start/expose workspace-manager against staging GKE, then run the remote runtime drill |
| 3 | WebContainer mode conserved | complete | `packages/runtime-webcontainer/`, `app/lib/webcontainer/index.ts`, `RuntimeAdapterProvider.tsx` | WebContainer adapter tests and build coverage | `pnpm run test`, `pnpm run build` | Browser manual regression still needed before OSS release | Medium | Add a dedicated Playwright smoke for WebContainer mode |
| 4 | Remote Kubernetes mode functional | partial | `services/workspace-manager/src/*`, `services/workspace-agent/src/*`, `packages/k8s-client/src/*`, `packages/runtime-remote/src/*`, `infra/helm/workspaces-runtime/*`, `scripts/validate-remote-kubernetes-runtime.ts` | k8s-client, workspace-manager, workspace-agent, remote adapter tests | `pnpm run infra:validate`, `pnpm run runtime:validate:api-kubernetes`, `pnpm run runtime:validate:remote-kubernetes` | Live GKE proof missing; local API runtime validation fails at workspace-manager health preflight because the manager is unavailable. Direct remote validation now fails with actionable context while pulling `kindest/node:v1.34.0` after 600000ms instead of hanging silently | Critical | Run GitHub Actions `Staging Runtime Validation` with a pullable workspace-agent image and fix any failing manager/agent path |
| 5 | Auth | partial | `packages/auth/src/index.ts`, `services/api/src/app.ts`, auth/account routes | API auth, MFA, OAuth/OIDC/SAML callback and session tests | `pnpm run test`, `pnpm run production:validate` | Production OAuth/OIDC/SAML/SMTP env is absent or placeholder in this workstation | High until live IdP/email proof | Verify Google, GitHub, OIDC, SAML, SMTP and password-reset flows in staging |
| 6 | RBAC | complete | `packages/rbac/src/index.ts`, `services/api/src/app.ts`, membership/roles routes | API RBAC tests including read vs manage 403 regressions | `pnpm run test` | None found in code-level review | Medium | Keep deny-by-default endpoint audit and route matrix checks |
| 7 | Enterprise SSO/SCIM readiness | partial | `docs/SSO_SCIM.md`, SSO routes, SCIM token/settings routes, API SAML/OIDC/SCIM handlers | API SSO/SCIM tests, OIDC `id_token` tests | `pnpm run test`, `pnpm run production:validate` | Real Entra/Okta/SAML/SCIM client proof missing; production env incomplete | High for enterprise beta | Run real IdP and SCIM client drills in staging |
| 8 | Projects | partial | `services/api/src/project-storage.ts`, project routes, snapshots/import/export routes, `docs/PROJECTS_AND_STORAGE.md` | API project CRUD/import/export/snapshot tests | `pnpm run test`, `pnpm run ide:panel-audit` | Large-project and live storage restore not proven | Medium-high | Run project lifecycle E2E against staging storage |
| 9 | File operations | partial | Runtime adapters, `app/lib/stores/files.ts`, `app/lib/runtime/runtime-files.ts`, workspace agent file endpoints | adapter/file tests, IDE panel audit | `pnpm run test`, `pnpm run ide:panel-audit` | Remote file operations not proven on live GKE workspace | High | Include create/read/write/rename/delete/search/watch in staging runtime drill |
| 10 | Terminal | partial | `packages/runtime-remote/src/index.ts`, `packages/runtime-webcontainer/src/index.ts`, `services/workspace-agent/src/app.ts`, terminal UI | workspace-agent terminal tests, adapter tests | `pnpm run test`, `pnpm run ide:panel-audit` | Terminal WebSocket through live ingress/NetworkPolicy not proven | High | Validate terminal open, reconnect, resize, stop and logs in staging |
| 11 | Preview | partial | `services/preview-proxy/src/app.ts`, preview routes, runtime port APIs, static preview fallback in `app/components/workbench/Preview.tsx` | preview-proxy tests, preview runtime E2E assets, cross-profile Playwright preview tests | `pnpm run test`, `pnpm run build`, `pnpm run ide:panel-audit`, `pnpm exec playwright test tests/e2e/preview-runtime.spec.ts --project=chromium --reporter=line`, all E2E profiles | Wildcard TLS, ingress, port routing, and custom-domain preview not proven live | High | Run preview-proxy and wildcard TLS drill in staging |
| 12 | AI tools | partial | `services/ai-gateway/src/*`, `app/routes/api.chat.ts`, LLM provider modules, tokenizer logic | ai-gateway tests, AI tokenizer tests, prompt tests | `pnpm run test`, `pnpm run production:validate` | Live provider throttling, fallback, BYOK, and cost ledger proof missing | High | Run staged AI provider drills with real keys and rate-limit scenarios |
| 13 | Billing | partial | `packages/billing/src/index.ts`, billing routes, Stripe webhook handlers, dashboard billing fallback | API billing/webhook tests, loader fallback tests | `pnpm run test`, `pnpm run production:validate` | Stripe secret/catalog missing; live Stripe CLI/test-mode webhooks not verified | Critical before paid users | Run Stripe test-mode checkout, portal, webhooks, idempotency and plan transition drill |
| 14 | Quotas | partial | `packages/billing/src/index.ts`, quota enforcement in API, usage routes, admin quota overrides | API quota tests and override tests | `pnpm run test` | Backend quotas exist but not exercised under realistic load/concurrency | High | Run k6 quota pressure and concurrent workspace start tests |
| 15 | Admin | partial | `app/routes/admin.*.tsx`, admin API handlers, `packages/audit/src/index.ts`, `scripts/validate-admin-dangerous-actions.mjs`, audit docs | API admin RBAC/reauth/MFA/audit tests, manual abuse-event re-auth/audit test, static dangerous-action guard, admin typography/UI Playwright smoke | `pnpm run test`, `pnpm exec vitest --run services/api/src/tests/api.spec.ts`, `pnpm run admin:dangerous-actions:validate`, `pnpm run ide:panel-audit`, `pnpm exec playwright test tests/e2e/admin-typography.spec.ts tests/e2e/ui-details.spec.ts --project=chromium --workers=1` | Full browser-level dangerous-action audit not executed; static/API proof covers 21 admin mutations and browser proof covers admin console rendering/UI tokens | High | Add/play a Playwright admin action audit requiring AdminAuditLog row per dangerous mutation |
| 16 | Desktop app | partial | `electron/*`, `apps/desktop/package.json`, `scripts/electron-smoke-test.mjs`, `docs/DESKTOP_APP.md`, `docs/DESKTOP_RELEASE.md` | Electron smoke test | `pnpm run desktop:test` | Signed macOS/Windows/Linux release artifacts not produced in this review | Medium, high if desktop is in beta scope | Run release matrix with signing and auto-updater non-prod channel |
| 17 | iOS app | partial | `apps/mobile/ios/*`, Capacitor config, mobile docs | mobile asset validation, mobile web build, mobile unit tests | `pnpm run mobile:validate`, `pnpm run mobile:build:web`, `pnpm run test` | No signed IPA/TestFlight run; associated domains not proven with production host | Medium, critical if iOS beta scope | Configure Apple signing, TestFlight, APNs and device QA |
| 18 | Android app | partial | `apps/mobile/android/*`, Gradle signing gates, mobile docs | mobile asset validation, mobile web build, mobile unit tests | `pnpm run mobile:validate`, `pnpm run mobile:build:web`, `pnpm run test` | No signed AAB/Play internal track proof; app links not proven live | Medium, critical if Android beta scope | Configure keystore, Play internal testing, FCM and device QA |
| 19 | Tablet UX | partial | responsive IDE/editor code, mobile/tablet routes and styles | responsive editor tests, Playwright responsive specs across tablet profile | `pnpm run test`, `pnpm run build`, `pnpm exec playwright test tests/e2e --project=tablet --reporter=line` | Real iPad/Android tablet QA not executed | Medium | Run tablet device matrix and fix any layout/input issues |
| 20 | Mobile editor fallback | partial | `packages/editor/src/index.ts`, `apps/mobile/src/mobile.spec.ts`, responsive IDE mobile navigation | tests assert CodeMirror on phone and portrait tablet, Monaco on desktop/tablet landscape; Playwright mobile editor save path passes in mobile profile | `pnpm run test`, `pnpm run mobile:validate`, `pnpm exec playwright test tests/e2e --project=mobile --reporter=line` | Real-device save/edit performance not proven | Medium | Add device QA and Playwright mobile editor save regression |
| 21 | Collaboration | partial | collaboration routes/libs, Redis-backed presence/edit/comment APIs, `docs/REALTIME_COLLABORATION.md` | API collaboration tests | `pnpm run test` | Uses optimistic locking; no CRDT/OT for safe multi-writer editing | High for collaborative editing launch | Integrate Yjs/Automerge or keep collaboration limited to beta with conflict UX |
| 22 | Deployments | partial | `services/api/src/deployments.ts`, deployment routes, docs/runbook | deploy guard and deploy-hook tests | `pnpm run test`, `pnpm run production:validate` | Provider sandbox runs and rollback not exercised live | High | Run selected provider deploy+rollback sandbox E2E |
| 23 | Security | partial | `packages/security/src/index.ts`, API helmet/CSP/CSRF/rate limits, docs security/threat model | security route tests, canary/redaction tests, deployment guard tests, AI tool route canary tests | `pnpm run test`, `pnpm exec vitest --run services/api/src/tests/security.spec.ts services/api/src/tests/canary-logs.spec.ts`, `pnpm exec vitest --run services/api/src/tests/api.spec.ts`, `pnpm run production:validate` | CSP style hardening, external pentest, live canary across Kubernetes runtime/admin/export still missing; AI tool API responses and persisted tool output now have route-level canary proof | Critical | Run security E2E, external review, and canary propagation audit |
| 24 | Workspace isolation | partial | `packages/k8s-client/src/index.ts`, `infra/admission/kyverno/*`, `infra/kubernetes/workspaces-runtime/*` | k8s-client security manifest tests | `pnpm run infra:validate`, `pnpm run networkpolicies:validate:live` | Live Kubernetes unavailable locally; isolation not proven in cluster. Validator now fails during cluster reachability preflight before mutating probe pods | Critical | Install policies in staging and prove gVisor/no-host/no-privileged/no-internal-egress |
| 25 | NetworkPolicies | partial | `infra/helm/*/templates/networkpolicy.yaml`, `infra/kubernetes/networkpolicies/*`, k8s-client policy generator | k8s-client and infra validation | `pnpm run infra:validate`, `pnpm run networkpolicies:validate:live` | Live drill fails during `kubectl cluster-info` preflight because no reachable cluster is configured | Critical | Run live denied-traffic drill with exact Cloud SQL/Redis CIDRs |
| 26 | Admission policies | partial | `infra/admission/kyverno/workspace-security-policies.yaml`, `infra/kubernetes/admission-policies/workspace-restricted-policies.yaml` | k8s-client tests assert shipped policies | `pnpm run test`, `pnpm run infra:validate` | Kyverno policy not installed/proven in staging | Critical | Install Kyverno in Enforce mode and test rejected non-gVisor/privileged/latest pods |
| 27 | Abuse detection | partial | `packages/security/src/index.ts`, API abuse event handlers, SIEM delivery paths, `docs/ABUSE_PREVENTION.md` | API abuse/security tests | `pnpm run test` | Thresholds and SIEM delivery not tuned/observed under real traffic | Medium-high | Run abuse drills and SIEM delivery test |
| 28 | Observability | partial | `infra/observability/*`, `packages/observability/src/index.ts`, API telemetry, docs SLO/OBSERVABILITY | observability asset validator | `pnpm run sre:validate` | Metrics/alerts/synthetics not deployed and observed live | High | Deploy dashboard/alerts/synthetics and verify alert routing |
| 29 | Backups | partial | Terraform Cloud SQL backup/PITR, storage lifecycle, `docs/BACKUP_RESTORE.md`, `scripts/backup-restore-dry-run.mjs` | backup dry-run checksum script | `pnpm run sre:validate` | No Cloud SQL/storage restore drill; no measured RTO/RPO | Critical | Execute staging restore and document measurements |
| 30 | CI/CD | partial | `.github/workflows/*`, `scripts/validate-ci-cd-assets.mjs`, release docs | CI/CD asset validator parses workflows and gates | `pnpm run cicd:validate` | Workflows not proven with real GitHub environments/secrets; rollback not exercised | High | Run full PR->staging->prod pipeline and rollback drill |
| 31 | GCP infra | partial | `infra/terraform/*`, `infra/helm/*`, `infra/gcp/bootstrap.sh` | infra scaffold/Helm validation | `pnpm run infra:validate` | Terraform not applied to real GCP in this review | Critical | Apply staging Terraform, deploy Helm, validate private services and Workload Identity |
| 32 | Load tests | partial | `tests/load/api-load.js`, `workspace-lifecycle-load.js`, `preview-load.js`, `ai-simulated-load.js`, `billing-webhook-load.js`, `docs/LOAD_TESTING.md` | k6 scripts have thresholds; CI validator checks presence | `pnpm run cicd:validate` | No k6 reports executed against staging | Critical before 1,000 users | Run all 5 k6 scenarios and publish p50/p95/p99/error/cost results |
| 33 | Documentation | complete | `docs/*`, especially readiness, security, runtime, billing, CI/CD, rollback, backup, mobile, SSO docs | readiness docs validator | `pnpm run readiness:validate` | Docs need continuous update with live evidence links | Medium | Update docs after every staging/prod gate |
| 34 | Legal/compliance pages | partial | `app/routes/terms.tsx`, `app/routes/privacy.tsx`, `app/routes/acceptable-use.tsx`, compliance docs | build coverage | `pnpm run build` | No counsel-approved ToS/privacy/DPA/subprocessor list | High before paid/public launch | Legal review and publish finalized ToS, Privacy, DPA, AUP, subprocessors |
