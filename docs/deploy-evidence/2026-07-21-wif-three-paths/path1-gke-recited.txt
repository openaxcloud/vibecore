# PATH 1 — GKE WIF re-cité (lecture seule, cluster prod vibecore-prod-app)
(1a) workloadPool géré par GKE (aucun IdP externe):
vibecore-495216.svc.id.goog

(1b) liaison KSA->GSA en usage:
vibecore-vibecore-platform-api	vibecore-prod-platform@vibecore-495216.iam.gserviceaccount.com

(1c) AUTORISÉ — grant workloadIdentityUser (impersonation via metadata, sans clé):
serviceAccount:vibecore-495216.svc.id.goog[vibecore/vibecore-vibecore-platform-api]

(1d) ZÉRO clé user (que SYSTEM_MANAGED):
   4 SYSTEM_MANAGED

(1e) NÉGATIF — une KSA non liée n'a aucun grant -> refus d'impersonation.
