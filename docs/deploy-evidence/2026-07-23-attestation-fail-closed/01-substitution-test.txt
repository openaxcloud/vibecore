[substitution-test] 1. contrôle positif (golden authentique)
  ✓ golden + attestation cohérente → 0 erreur
[substitution-test] 2. falsification indépendante par champ (run)
  ✓ champ path falsifié seul → erreur "workflow:" levée (1 erreur(s))
  ✓ champ name falsifié seul → erreur "workflow:" levée (1 erreur(s))
  ✓ champ event falsifié seul → erreur "event:" levée (1 erreur(s))
  ✓ champ head_branch falsifié seul → erreur "branche:" levée (1 erreur(s))
  ✓ champ head_sha falsifié seul → erreur "sha:" levée (3 erreur(s))
  ✓ champ conclusion falsifié seul → erreur "conclusion:" levée (1 erreur(s))
  ✓ champ html_url falsifié seul → erreur "url:" levée (1 erreur(s))
  ✓ champ created_at falsifié seul → erreur "date:" levée (1 erreur(s))
  ✓ url suffixée (…/attempts/2) → REJETÉE (égalité exacte, plus de préfixe)
[substitution-test] 3. commits de provenance liés au head_sha (verdict -06 §1)
  ✓ mergedCommit ≠ head_sha → erreur sha
  ✓ repoCommit (préfixe) ≠ head_sha → erreur sha
  ✓ préfixe court 10 hex valide → accepté
  ✓ préfixe 6 hex (<7) → refusé (règle explicite)
  ✓ normalizeRunUrl retire le slash final
[substitution-test] 3c. suppression par champ → attestation amputée rejetée (CODEX-07)
  ✓ mergedCommit SUPPRIMÉ seul → attestation amputée REJETÉE (erreur "sha: mergedCommit ABSENT")
  ✓ repoCommit SUPPRIMÉ seul → attestation amputée REJETÉE (erreur "sha: repoCommit ABSENT")
  ✓ runUrl SUPPRIMÉ seul → attestation amputée REJETÉE (erreur "url: runUrl ABSENT")
  ✓ mergedCommit vide ("") → REJETÉ comme absent (fail-closed)
  ✓ repoCommit vide ("") → REJETÉ comme absent (fail-closed)
  ✓ runUrl vide ("") → REJETÉ comme absent (fail-closed)
[substitution-test] 4. run étranger complet (fixture Preview Deployment)
  ✓ run étranger 29812663423 (Preview Deployment) → REJETÉ sur l'identité workflow
[substitution-test] OK — toutes les gardes cassent indépendamment (déterministe)
[substitution-test] sonde live SAUTÉE (pas de GH_TOKEN) — les cas déterministes font foi
