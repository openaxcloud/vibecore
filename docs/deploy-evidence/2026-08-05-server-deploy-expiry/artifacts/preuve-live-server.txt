PREUVE LIVE — extinction 30 j du chemin SERVER (option A, rejeu expert PR #95)
==============================================================================
Banc : vraie API + vrai PostgreSQL 16 + VRAI preview-proxy
       + workspace-manager INSTRUMENTÉ (journalise chaque arrêt de workload)
       + faux workload applicatif (tout octet servi par lui prouve du trafic amont)
Compte : créé par le VRAI POST /auth/register ; plan résolu serveur = free/starter
Déploiement : provider=server, environmentName=production, status=READY

1. AVANT EXPIRATION — l'app est servie
--------------------------------------
  état de service              : {"state":"live"}
  GET / via proxy (host d-<id>): HTTP 200
  corps                        : CONTENU-APPLICATIF-SERVEUR
  requêtes atteignant le workload : 1

2. VIEILLISSEMENT
-----------------
  âge en base : 31 days   (TTL Starter = 30 j)
  état de service : {"state":"expired"}

3. FAÇADE — l'URL publique est éteinte
--------------------------------------
  GET / via proxy : HTTP 410
  corps : {"error":"Cette publication a expiré. Republiez le projet pour remettre
           l'adresse en ligne.","code":"PUBLISHED_DEPLOYMENT_EXPIRED"}
  contenu applicatif présent      : 0
  requêtes atteignant le workload : 0   <-- refus AVANT tout forward

4. SUBSTANCE — le workload est réellement ARRÊTÉ
------------------------------------------------
  POST /internal/deployments/reap
  publicationExpiry = {"examined":1,"expired":1,"stopped":["depsrv001"],"failed":[]}

  Journal du workspace-manager instrumenté :
      STOP depsrv001

  Ligne marquée en base :
      {"expiredAt":"2026-08-05T12:14:01.182Z", ...}

  => ce n'est pas un 410 de façade : l'arrêt du workload a été RÉELLEMENT
     demandé en amont et observé côté manager.

5. IDEMPOTENCE ET ABSENCE DE FAUX POSITIF
------------------------------------------
  2e balayage : {"examined":1,"expired":0,"stopped":[],"failed":[]}
                arrêts cumulés = 1 (aucun ré-arrêt)
  déploiement server RÉCENT ajouté :
                {"examined":2,"expired":0,"stopped":[],"failed":[]}
                état = {"state":"live"} (non touché)

TESTS
-----
  services/preview-proxy : 75/75 (dont 7 dédiés, incl. TEST NÉGATIF)
  published-expiry-sweep : 15/15
  tsc services/api       : 0 erreur

TEST NÉGATIF (reproduit la faille d'avant correctif)
-----------------------------------------------------
  « SANS garde d'expiration, une publication SERVER expirée reste SERVIE »
  -> HTTP 200, corps applicatif servi, trafic atteignant le workload > 0
  Le test positif correspondant donne 410 / 0 octet / 0 requête amont.
