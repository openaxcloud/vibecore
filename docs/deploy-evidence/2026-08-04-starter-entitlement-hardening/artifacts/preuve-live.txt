PREUVE LIVE — durcissement entitlement Starter (contre-audit expert)
====================================================================
Base : main @ 4e1f4c362270793f9a899cae1ea5c41917fb6a95
Branche : fix/starter-entitlement-hardening (fix-forward)
Banc : vraie API (tsx src/server.ts, port 3403) + vrai PostgreSQL 16 (port 55437)
Compte : créé par le VRAI POST /auth/register ; plan résolu serveur = free/starter

RÉSERVE 1+5 — SÉRIALISATION ET REJEU CONCURRENT RÉEL
-----------------------------------------------------
Deux publications de projets DISTINCTS lancées SIMULTANÉMENT (deux curl en
parallèle, & + wait), contre le vrai advisory lock PostgreSQL
(pg_advisory_xact_lock, clé "publish:org:<orgId>") :

  Projet A -> HTTP 402
  Projet B -> HTTP 201

  Publications de production réellement créées en base : 1

Exactement un 201 et un 402. La lecture des publications, la décision
d'entitlement et la création du déploiement sont dans UNE SEULE section
critique — auparavant les trois étapes étaient séparées.

CONTRÔLE NÉGATIF (le test a des dents)
--------------------------------------
En désactivant la sérialisation du store de test, le test concurrent échoue
avec exactement le bug décrit par l'expert :

  AssertionError: expected [ 201, 201 ] to deeply equal [ 201, 402 ]

Les deux publications passaient. Avec le verrou : [201, 402].

RÉSERVE 2 — FAIL-CLOSED
-----------------------
Le `listPublishedProjects(...).catch(() => [])` est SUPPRIMÉ. Une panne de
lecture du quota ou de l'état de facturation renvoie désormais :

  HTTP 503  code=ENTITLEMENT_CHECK_UNAVAILABLE  retryable=true

et AUCUNE publication n'est créée (vérifié en base par le test). Une panne ne
remet jamais le quota à zéro.

RÉSERVE 3 — EXTINCTION RÉELLE DANS LE CHEMIN DE SERVICE
-------------------------------------------------------
  AVANT expiration :  GET /static-deployments/<id>/index.html -> HTTP 200
                      corps servi : "<!doctype html><h1>App A en ligne</h1>"

  Publication vieillie de 31 jours (age vérifié en base : "31 days")

  APRÈS expiration :  GET /static-deployments/<id>/index.html -> HTTP 410
                      {"code":"PUBLISHED_DEPLOYMENT_EXPIRED"}
                      octets de l'app servis : 0

  L'URL est réellement éteinte (410 Gone, pas 404 : la ressource a existé).

  PUIS SEULEMENT, l'autre projet devient publiable :
                      POST /projects/<A>/deployments/srcA/publish -> HTTP 201

  État final : ProjA READY (age 0), ProjB READY (age 31 days, URL éteinte).

RÉSERVE 4 — MÉTRIQUE DE CONCURRENCE DISTINCTE
----------------------------------------------
maxActivePublishedProjects(plan payant) = +Infinity  (publications illimitées)
maxConcurrentRunningWorkloads()         = 20         (workloads en EXÉCUTION)

La borne « 20 apps simultanées » ne sert plus de plafond de publications : elle
transformait « illimité » en plafond persistant de 20 projets publiés à vie.
Test : un plan payant publie un 26e projet distinct -> autorisé.

NOTE DE MERGE (4e1f4c362270793f9a899cae1ea5c41917fb6a95)
---------------------------------------------------------------
La branche partait de main @ 35292dc0, ANTÉRIEUR au revert c61bba01
(« PR #93 protection mot de passe — contre-audit expert P0 SÉCURITÉ »).
Elle transportait donc le code P104 retiré de main pour défaut de sécurité.

Résolution des 4 conflits : base = version de MAIN, puis réapplication de MES
seuls ajouts (createdAt / environmentName / organizationId / planKey + le gate
d'extinction). Le bloc P104 n'a PAS été ressuscité — vérifié : 0 occurrence de
accessConfigFromMetadata et accessGateHtml dans services/api/src/app.ts.

Une résolution « garder HEAD » aurait silencieusement réintroduit la faille.

Après merge : tsc 0 erreur ; 19/19 (publish + extinction).
