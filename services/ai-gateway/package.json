{
  "name": "@vibecore/ai-gateway",
  "private": true,
  "type": "module",
  "dependencies": {
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc --outDir dist --rootDir src --module NodeNext --moduleResolution NodeNext --target ES2022 --lib ES2022 --types node --skipLibCheck true --strict true src/server.ts",
    "test": "vitest --run --config vitest.config.ts && pnpm run typecheck",
    "typecheck": "tsc --noEmit --module NodeNext --moduleResolution NodeNext --target ES2022 --lib ES2022 --types node --skipLibCheck true --strict true src/server.ts",
    "lint": "pnpm run typecheck"
  }
}
