logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
# Les preuves de déploiement doivent être suivies malgré la règle *.log ci-dessus.
!docs/deploy-evidence/**

node_modules
dist
dist-ssr
*.local

# Transient Vite config temp files.
*.timestamp-*.mjs
vite.config.ts.timestamp-*.mjs

.vscode/*
.vscode/launch.json
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

/.history
/.cache
/build
# React Router 7 typegen output (regenerated by `react-router typegen` / build).
.react-router/
functions/build/
.vibecore-project-storage/
.vibecore-static-deployments/
**/.vibecore/
test-results/
tmp/
.claude/
.playwright-mcp/
/connected-accounts-fixed.png
# Secrets: ignore EVERY .env* (real values) — keep only the *.example templates.
.env
.env.*
!.env.example
!.env.production.example
.dev.vars
*.vars
.wrangler
_worker.bundle

# Terraform local state, plans, and secret variable files
**/.terraform/
**/terraform.tfvars
**/terraform.tfvars.json
**/*.tfplan
**/plan.out

Modelfile
modelfiles

# docs ignore
site

# commit file ignore
app/commit.json
changelogUI.md
docs/instructions/Roadmap.md
.cursorrules
*.md
!docs/*.md
!docs/**/*.md
!outputs/SECURITY_ENV_LEAK_REMEDIATION.md
# Root-level LIVE tracking files (CLAUDE.md « Suivi ») — versioned despite *.md,
# so a clone sees them and they never drift as untracked local-only files again.
!BUG_INVENTORY_LIVE.md
!PLAN_REMAINING_UNIFIED.md
!REPLIT_PARITY.md
!DESIGN_PROGRAM_MASTER.md
!DESIGN_AUDIT_LIVE.md
!DESIGN_CAPTURES.md
!LAUNCH_READINESS.md
.qodo
.vibecore-audit/
.rebuild/

# responsive-matrix verification output (local only)
scratch-matrix/

.blk-*.mjs
