/**
 * Public-facing press / media contact details for the E-Code marketing surface.
 *
 * The product brand is E-Code (e-code.ai). The internal codename "vibecore" must
 * never leak to public visitors, so this value is centralised here and asserted
 * against in press-contact.spec.ts.
 */
export const PRESS_CONTACT_EMAIL = 'press@e-code.ai';

export const PRESS_CONTACT_MAILTO = `mailto:${PRESS_CONTACT_EMAIL}`;
