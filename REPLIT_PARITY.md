# REPLIT PARITY

> **Fichier de suivi LIVE** (parité fonctionnelle Replit Gallery), un des 4 fichiers de suivi du `CLAUDE.md` (avec `DESIGN_PROGRAM_MASTER.md`, `BUG_INVENTORY_LIVE.md`, `PLAN_REMAINING_UNIFIED.md`). NE PAS confondre avec l'**audit historique** `docs/REPLIT_PARITY_{DELIVERY,MATRIX,SPEC}.md` (snapshot 2026-06-17, figé). Ce fichier-ci est le suivi courant.

Cette mission suit la parité fonctionnelle et visuelle avec la Replit Gallery actuelle : des applications communautaires publiées à voir et remixer, pas des starters par langage/framework.

| ID | Point | 📤 Dispatché | 💻 Codé | ✅ Testé live | Preuve |
|---|---|:---:|:---:|:---:|---|
| I18N-FR-001 | Localisation plein site EN/FR : catalogues, sélecteur FR/EN, détection navigateur, persistance, métadonnées SEO localisées | ✅ | ✅ **mergé sur `main` 07/08** — merge `d68533f3` (parents `64cce7b6` + `9efd247b`) | 🟡 07/08 (socle prouvé live ; « zéro anglais résiduel » NON atteint) | **Fusion des DEUX branches en une seule passe** : `9efd247b98` (enterprise) contenait déjà `65e426cbb3` (plein site). **Ancêtres vérifiés, pas seulement « MERGED »** : `git merge-base --is-ancestor 65e426cbb3 origin/main` = vrai ET `… 9efd247b98 origin/main` = vrai. **35 fichiers en conflit résolus à la main**, règle = garder le correctif de `main` ET la localisation (détail dans le message de `d68533f3`). **Live prod après déploiement (run `31177922789`, vert)** : `https://e-code.ai` **200**, `https://app.e-code.ai/gallery` **200** ; détection navigateur réelle — `Accept-Language: fr` → `<html lang="fr">` + titre « Créez, déployez et faites évoluer vos applications avec l'IA », `Accept-Language: en` → `<html lang="en">` + titre EN ; bascule explicite `?lang=fr` / `?lang=en` conforme ; `og:locale` `fr_FR`/`en_US`, `og:site_name`, `og:url`, `canonical` présents. **Défaut trouvé EN LIVE et corrigé dans la foulée** (`defb3dd9`) : `canonical`/`og:url` étaient émis en **`http://`** (TLS termine sur l'ingress, `request.url` est en clair) — canonical désignant une origine autre que celle servie ; `X-Forwarded-Proto` est désormais honoré, avec test de non-régression qui reproduit la condition de prod. **12/08 — « zéro anglais résiduel » ATTEINT au niveau source** : le scanner sortait 14 résidus et le baseline commité en revendiquait 145 (périmé de 5 fichiers) ; `i18n:scan:source:zero` passe désormais (**residual=0**, 780 chaînes explicitement autorisées, baseline à 0). Les 14 étaient : **9 chaînes de `makeSolution()`** (« What you can build », « Production workflow », « Prompt to project »…) **jamais rendues** — `localizeMarketingPage()` remplace eyebrow/highlights/sections par le catalogue, et chacun des 9 slugs a sa **route statique localisée** que React Router fait gagner ; **3 cadres de titre** `{…} - E-Code` / `— E-Code` ; **2 invariants programmeur**. **Vérifié EN RÉEL (pas déduit du code)** : les 9 pages `/solutions/*` + `/enterprise` rendent en `<html lang="fr">` sans AUCUNE de ces chaînes, en FR comme en EN. ⚠️ mesure faite au **navigateur** pour `/enterprise` et `/solutions` : leur HTML servi ne contient que le splash (39 et 38 caractères), un grep HTTP n'y prouvait rien. Garde ajoutée `solutions.localized-route-coverage.spec.ts` (vérifiée non-vacante : retirer une route fait échouer le test en nommant le slug) — sans elle, un 10e slug servirait de l'anglais en silence. **🟡 RESTE** : la matrice responsive 3 formats n'a PAS été rejouée — l'audit Playwright complet (84 pages × 2 langues × 2 thèmes × 4 formats) s'est **bloqué** sur l'env de test (navigateur à 0 % CPU pendant 17 min) ; un passage borné auth+user-area a tourné **53 min** puis expiré. Sur ce qui a réellement été audité : **zéro chaîne non traduite** (les 2 échecs sont `fonts.googleapis.com` injoignable depuis l'env et un timeout, pas des défauts i18n). `playwright.i18n.config.ts` rend les budgets d'attente paramétrables — sans quoi l'audit échouait sur de la LATENCE (sélecteur de langue à 6,1 s puis 15,2 s contre 5 s de budget local). |
| TPL-02.1 | Gallery d'applications publiées : cartes riches, recherche, catégories, artefacts, technologies, tri, featured, modération, signalement, preview et permission de remix | ✅ | ✅ `266fefac`/`e6afdfbf`/`c59674e8`/`3181b31f`/`d95108ae` | ✅ 17/07 (fonctionnel) + 24/07 (design) | Fonctionnel LIVE (17/07). **Design responsive validé LIVE (24/07)** : matrice grille+détail × 390/768/1024/1440 × clair+sombre (16 captures), débordement=0 partout, grille 1→2→3 col, rail détail empilé↔latéral, **cibles ≥44px** (fix `d95108ae` : recherche/chips 39-36→44). `docs/deploy-evidence/2026-07-24-gallery-responsive/`. NB « preview embarquée » = décision E-CODE de NE PAS faire (View App = lien sortant, `UNK-GALLERY-EMBED-PREVIEW`). |
| TPL-02.2 | Remix/Fork complet avec isolation, provenance et analyse Agent | ✅ | ✅ `266fefac`/`3181b31f`/`d95108ae` | ✅ 24/07 | Core LIVE (17/07) + **reste levé (24/07)** : **(a) handoff clic-connecté prouvé** — session authentifiée (cookie `vc_session`), clic « Remix this app » → **redirection réelle vers l'IDE** `e-code.ai/@qa-handoff-org/…`, IDE chargé propre (arborescence = 7 fichiers du snapshot épinglé), DB : clone dans l'org du remixeur, `RemixJob COMPLETED` épinglé listing+snapshot, `secrets=0`, `useCount=1`. **(b) test secret-absent EXHAUSTIVE** dans `gallery-routes.spec.ts` : cherche activement un vrai secret+valeur d'env dans **fichiers+DB+env+logs+job** et échoue à les trouver (balayage complet non-vacant), **20/20 verts**. `docs/deploy-evidence/2026-07-24-gallery-responsive/README.md`. |
| TPL-02.3 | Hub Import à 12 sources avec validation et aperçu avant création | ✅ | ✅ `2ad85147` + `a1e893ab` | ✅ **13/08** | **Hub `/import` LIVE prod** (deploy `2ad85147af`) : 12 tuiles groupées par catégorie, statut HONNÊTE par source, responsive 390/768/1024/1440 × clair/sombre (8 captures authentifiées, empilement 1 col→4 col). **Prouvé live** : spreadsheet (CSV/TSV → app triable réelle) et zip via l'API `import/zip` (`sourceType:zip`, fichiers réels) ; github/bitbucket/zip/empty routent vers les flux existants prouvés ; bolt/lovable/base44/previous-agent réutilisent le pipeline zip prouvé (staging + scan secrets) avec copie cadrée. **🟡 honnête** : vercel/figma/claude restent **credential-gated** (pages « Connect token/source », aucun faux succès, pas de token) ; la validation/progression/erreur-récupérable existe sur zip+spreadsheet. `docs/deploy-evidence/2026-08-02-tpl-import/`. **12/08 — écran d'aperçu CODÉ et contrat prouvé live, mais point NON clos** : route `/import/preview/:importJobId` (fichiers qui atterriraient, détections à aperçu **déjà masqué**, décision masquer/conserver **par détection**, puis commit ou annulation) + `GET /orgs/:orgId/imports/:id` expose un sous-objet `preview` (chemins + tailles, **jamais le contenu** — sinon l'aperçu deviendrait une 2e façon de lire le secret signalé ; `preview:null` quand le staging est disposé). **Prouvé LIVE 6/7** sur images construites du commit `1c091dc51e` et déployées sur l'env de test : liste des fichiers exacte, aucun contenu ni secret dans l'aperçu, relecture stable qui **n'avance pas** la machine à états, détections recalculées, commit sans décision → **409 `IMPORT_UNRESOLVED_FINDINGS`**. **La 7e étape a révélé BUG-IMPORT-001** : staging (et registre de crédits) en **mémoire du processus** alors que l'API tourne en 2 réplicas — 8 lectures du même import → **5 aperçus / 3 vides**. Staging corrigé (migration `0084`, partagé en base) ; **le registre de crédits reste en mémoire** → commit cross-réplica en `BILLING_RESERVATION_MISSING`. **Le câblage `/import-zip` → aperçu a donc été RETIRÉ** : livré, il aurait cassé ~1 fois sur 2 les 5 tuiles ZIP/Bolt/Lovable/Base44/export-agent qui marchent. **✅ CLOS le 13/08** : BUG-IMPORT-001 corrigé dans ses DEUX moitiés (staging `0084` + registre de crédits `0085`), câblage `/import-zip` **rétabli** — 5 tuiles du hub (ZIP/Bolt/Lovable/Base44/export-agent) passent désormais par l'aperçu. **API 17/17** dont `everyReplicaSeesPreview` : **8 lectures sur 8** renvoient l'aperçu (contre 5/3 avant), commit sans décision → **409**, commit avec décision → **201**, `preview:null` + réservation **SETTLED** après coup. **Parcours NAVIGATEUR complet** (connexion par le vrai formulaire) : dépôt d'archive → écran d'aperçu rendu **intégralement en FR** (« Vérifier avant d'importer », 3 fichiers avec tailles, détection à aperçu **déjà masqué** `API_SECRET=fadf…888`, radios `consent:.env:2` masquer/conserver) → « Créer le projet » → **IDE du projet créé** ; octets relus : `API_SECRET` masqué, **secret brut absent**. **3e défaut trouvé au passage** : la clé d'idempotence, choisie par le CLIENT, était indexée **globalement** — 2 orgs avec la même clé partageaient une réservation (la 2e recevait celle de la 1re) ; contrainte désormais `@@unique([organizationId, key])`. `docs/deploy-evidence/2026-08-12-tpl023-import-preview/`. |
| TPL-02.4 | Projet vide sans Agent, framework ni scaffolding | ✅ | ✅ `2ad85147` | ✅ 02/08 | **LIVE prod, prouvé E2E via l'UI** : route `/import/empty` → formulaire soumis dans le navigateur (session `vc_session`) → **vrai projet créé** (`qa-ui-empty-msbjj506`, `sourceType:blank`) → **IDE complet chargé** (éditeur, arbre, assistant, Webview, « Connected »). Responsive 390/768/1024/1440 × clair/sombre (8 captures) + capture IDE E2E. **Honnête** : pas de contenu agent ni de framework imposé, mais **scaffold runtime minimal** (7 fichiers Vite) pour que l'IDE/preview soit utilisable — pas « zéro fichier ». `docs/deploy-evidence/2026-08-02-tpl-import/`. |
| TPL-02.5 | Gallery peuplée d'applications RÉELLES : 8 nouvelles apps complètes (8 chips distincts) + 6 démos historiques, cartes avec image de preview, remixables sous licence | ✅ | ✅ `881576beda` (PR #60) | ✅ 31/07 | **LIVE prod 31/07** : 8 apps (vendor-risk-review, field-service-inspector Expo+Metro, revenue-cohort-explorer, qbr-generator 2-artefacts-1-backend, incident-postmortem-explainer sans Remotion, warehouse-layout-planner 3D, pipeline-crm, storefront Stripe-test) **publiées + remixables (MIT, FAIL-CLOSED respecté)** avec `thumbnailUrl` réel (screenshot Playwright 1200×675). Chaque app prouvée `install→build→HTTP 200→DOM 0-pageerror` PUIS **certifiée bouton-par-bouton** (259 contrôles OK cumulés, 6 défauts réels corrigés, 221 captures — `docs/deploy-evidence/2026-07-17-gallery-apps/certification/`). **Preuve live grille** : 8/8 cartes AVEC preview en clair ET sombre × 390/768/1024/1440 (overflow=0, pageerr=0), détail avec preview, **remix réel HTTP 201** → projets clonés. `docs/deploy-evidence/2026-07-17-gallery-apps/live-prod/`. Aucune carte Python/Go/Rust (spec verrouillée). |
| TPL-02.6 | Masquage PII du remix : un IBAN masqué ne doit laisser **aucun** groupe en clair (politique P0-V3-05) | ✅ | ✅ `022f23fc` (PR #89, merge `7aa677ff`) | ✅ 12/08 | Défaut corrigé : le dernier groupe de l'IBAN restait **en clair** dans les fichiers clonés chez le remixeur. Verdict expert **SIGNED, aucune réserve**. Le checksum MOD-97 **étiquette** désormais le masquage sans le conditionner ; un code pays hors registre ISO 13616 n'est **pas** masqué mais **compté et journalisé** (échantillonné 1/pays/fenêtre, sans aucun fragment du candidat) pour rendre visible un pays manquant plutôt que laisser la fuite passer inaperçue. Conflit `app.ts` résolu **par merge, pas rebase**, pour garder le SHA signé ancêtre littéral de `main` (piège #55 : le statut « MERGED » ne suffit pas — `git merge-base --is-ancestor 022f23fc origin/main` = vrai). Tests **73/73** (`remix-pipeline.spec.ts` + `gallery-routes.spec.ts`). **✅ Rejeu LIVE 12/08** sur l'env de test dédié : auteur crée un projet → 3 fichiers PII écrits par **vrai import zip** → snapshot immuable → curation platform-admin (step-up, licence MIT, **`piiConsentVersion` absent** donc masquage obligatoire) → **un AUTRE utilisateur remixe (201)**, `piiMaskedCount=9` → relecture des **VRAIS OCTETS** via `export/zip`. **Source 10/10 fragments d'IBAN trouvés, clone 0/10** — dont **`189`, le défaut exact de BUG-REMIX-001**. Piège écarté : `GET /projects/:id/files` passe par `publicFiles()` qui **supprime le contenu**, une première assertion passait donc au vert sur des chaînes vides (preuve vacante) ; ajout d'un **contrôle positif** (IBAN en clair dans le source) + **détecteur exercé** sur le source. Substrat recevable : `remix-pipeline.ts`/`remix-pii-metrics.ts` déployés **byte-identiques** à `origin/main` et diff `app.ts` sans **aucune** ligne remix/gallery/PII ; digest api épinglé `sha256:3e5bb2b9…`. `docs/deploy-evidence/2026-08-12-tpl026-remix-pii/`. Détail : `BUG-REMIX-001` dans `BUG_INVENTORY_LIVE.md`. |
| TPL-02.PROOF | Prompt + import + remix → IDE → runtime → Preview → publiable | ✅ | ☐ | ☐ | Aucun push avant validation des captures par Avi. **État qualifié le 12/08, par branche, sur l'env de test dédié** — les trois ne sont pas bloquées pour la même raison, les confondre en un seul ☐ masquait l'information : **(a) prompt → BLOCKED par manque de ressource, pas par un défaut produit** — vérifié en réel : aucune clé LLM (Anthropic/OpenAI/Google/xAI/Moonshot) n'est présente dans les secrets du namespace, conformément au choix « secrets internes uniquement » du §5 du runbook ; **Avi doit fournir une clé de test plafonnée** pour que cette branche devienne prouvable. **(b) import → BLOCKED par `BUG-IMPORT-001`** (staging + registre de crédits en mémoire du processus, API en 2 réplicas) ; le staging est corrigé, le registre reste à persister. **(c) remix → le clone est prouvé LIVE le 12/08** (TPL-02.6 : snapshot épinglé → clone chez un autre utilisateur, PII masquée, octets relus) ; **reste à prouver la suite** clone → IDE → runtime → Preview → publiable. |
