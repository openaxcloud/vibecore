# REPLIT PARITY

> **Fichier de suivi LIVE** (parité fonctionnelle Replit Gallery), un des 4 fichiers de suivi du `CLAUDE.md` (avec `DESIGN_PROGRAM_MASTER.md`, `BUG_INVENTORY_LIVE.md`, `PLAN_REMAINING_UNIFIED.md`). NE PAS confondre avec l'**audit historique** `docs/REPLIT_PARITY_{DELIVERY,MATRIX,SPEC}.md` (snapshot 2026-06-17, figé). Ce fichier-ci est le suivi courant.

Cette mission suit la parité fonctionnelle et visuelle avec la Replit Gallery actuelle : des applications communautaires publiées à voir et remixer, pas des starters par langage/framework.

| ID | Point | 📤 Dispatché | 💻 Codé | ✅ Testé live | Preuve |
|---|---|:---:|:---:|:---:|---|
| TPL-02.1 | Gallery d'applications publiées : cartes riches, recherche, catégories, artefacts, technologies, tri, featured, modération, signalement, preview et permission de remix | ✅ | ✅ `266fefac`/`e6afdfbf`/`c59674e8`/`3181b31f`/`d95108ae` | ✅ 17/07 (fonctionnel) + 24/07 (design) | Fonctionnel LIVE (17/07). **Design responsive validé LIVE (24/07)** : matrice grille+détail × 390/768/1024/1440 × clair+sombre (16 captures), débordement=0 partout, grille 1→2→3 col, rail détail empilé↔latéral, **cibles ≥44px** (fix `d95108ae` : recherche/chips 39-36→44). `docs/deploy-evidence/2026-07-24-gallery-responsive/`. NB « preview embarquée » = décision E-CODE de NE PAS faire (View App = lien sortant, `UNK-GALLERY-EMBED-PREVIEW`). |
| TPL-02.2 | Remix/Fork complet avec isolation, provenance et analyse Agent | ✅ | ✅ `266fefac`/`3181b31f`/`d95108ae` | ✅ 24/07 | Core LIVE (17/07) + **reste levé (24/07)** : **(a) handoff clic-connecté prouvé** — session authentifiée (cookie `vc_session`), clic « Remix this app » → **redirection réelle vers l'IDE** `e-code.ai/@qa-handoff-org/…`, IDE chargé propre (arborescence = 7 fichiers du snapshot épinglé), DB : clone dans l'org du remixeur, `RemixJob COMPLETED` épinglé listing+snapshot, `secrets=0`, `useCount=1`. **(b) test secret-absent EXHAUSTIVE** dans `gallery-routes.spec.ts` : cherche activement un vrai secret+valeur d'env dans **fichiers+DB+env+logs+job** et échoue à les trouver (balayage complet non-vacant), **20/20 verts**. `docs/deploy-evidence/2026-07-24-gallery-responsive/README.md`. |
| TPL-02.3 | Hub Import à 12 sources avec validation et aperçu avant création | ✅ | ☐ | ☐ | Sources vérifiées sur la documentation Replit ; screenshot exclu des providers. |
| TPL-02.4 | Projet vide sans Agent, framework ni scaffolding | ✅ | ☐ | ☐ | Voie power-user réelle à conserver. |
| TPL-02.5 | Six starters historiques requalifiés en apps de démo/fixtures | ✅ | ☐ | ☐ | Aucune carte Python/Go/Rust. |
| TPL-02.PROOF | Prompt + import + remix → IDE → runtime → Preview → publiable | ✅ | ☐ | ☐ | Aucun push avant validation des captures par Avi. |

## Parité facturation & quotas (prêt lancement public)

Un vrai utilisateur payant doit pouvoir utiliser la plateforme sans faux blocage. Le « faux gate » perçu (`API usage limits`) est en réalité un quota Free correct ; les deux voies de déblocage doivent être **prouvées exécutables** (mode test Stripe, aucun vrai paiement).

| ID | Point | 📤 Dispatché | 💻 Codé | ✅ Testé live | Preuve |
|---|---|:---:|:---:|:---:|---|
| BILL-LAUNCH-01 | Upgrade payant (Pro/Team) lève réellement le gate quota + génération | ✅ | ✅ (ce commit) `services/api/src/tests/billing-launch-readiness.spec.ts` | 🟡 prouvé au **contrat serveur** (pas encore UI Checkout live) | Spec exécutable **5/5** : org Free épuisée → `429 QUOTA_EXCEEDED` → **webhook Stripe mode-test à signature vérifiée** (`verifyStripeSignature` HMAC) → `Subscription ACTIVE pro` → `check-quota` **200**, plafond Pro=1000. + preuve négative : signature invalide → rejet, gate maintenu. Moteur réel (`ensureQuota`/`billingState`/webhook), **pas de mock** ; pas de vrai paiement. `docs/deploy-evidence/2026-07-31-billing-launch-readiness/`. **Reste** : preuve UI Checkout hébergé (carte test 4242) sur stack complet. |
| BILL-LAUNCH-02 | Override `/admin/quotas` débloque un compte | ✅ | ✅ (ce commit) même spec | 🟡 prouvé au **contrat serveur** | Spec **5/5** : org Free épuisée → `429` → `POST /admin/quota-overrides` (platform-admin + MFA + re-auth step-up) sur `ai.messages` **et** `ai.inputTokens` → `check-quota` **200** aux plafonds override ; refus **403** si non platform-admin. Override consulté dans le vrai `ensureQuota`. Même dossier de preuve. |
| BILL-LAUNCH-03 | Cohérence page pricing (plans/prix affichés = facturés) | ✅ | ✅ (ce commit) | 🟡 prouvé au **contrat serveur** | **Cutover exécuté** (décision Avi = parité Replit EUR, mensuel+annuel). Vocabulaire unifié Starter/Core/Pro/Enterprise de la page → checkout → gateway. Spec 6/6 : checkout facture €25/€240 (Core m/a) et €100/€1140 (Pro m/a) via faux-Stripe injecté capturant le price id ; affiché == facturé (`creditPlanChargeCents`). Fini core→pro silencieux + fallback Free. `docs/deploy-evidence/2026-07-31-billing-plan-cutover/`. **Reste** : Avi fournit les 6 price IDs Stripe (script `seed-stripe-catalog.mjs`) + déploiement séquencé + preuve UI Checkout hébergé. |
| BILL-LAUNCH-05 | Unification vocabulaire plans (page↔checkout↔gateway↔quotas) | ✅ | ✅ (ce commit) | 🟡 tests verts | 3 taxonomies (billingPlans €29/€99, creditPlanCatalog €25/€100, ecodePaymentPlans teams €40) → **une seule** starter/core/pro/enterprise. 11 points de rupture corrigés (seed Plan, billingState limites, gateway tier, BYOK, tier DB, k8s resources, upgrade normalize, 3 UIs pricing, migration backfill `0080`). CI : `services/api` 1307✓, billing 109✓, k8s 118✓, web 4014✓ ; build strict + typecheck web + helm render **EXIT 0**. |
| BILL-LAUNCH-04 | Webhooks Stripe réels (pas de mock) | — | ✅ (préexistant) | ✅ (signature vérifiée en spec) | `POST /billing/stripe/webhook` : `verifyStripeSignature` HMAC-SHA256 (tolérance 300 s, rotation `v1=`), dédup, garde out-of-order/stale-reactivation, `invoice.payment_failed`→PAST_DUE / `invoice.paid`→recovery, **pas de downgrade silencieux**. Client `StripeBillingClient` (fetch, `stripe-version` épinglé), non-mock. Prod déjà câblée (price IDs Pro/Team dans `values-prod.yaml`). |
