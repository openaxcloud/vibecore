# BUG INVENTORY LIVE

| ID | Bug | 📤 Dispatché | 💻 Codé | ✅ Testé live | Preuve |
|---|---|:---:|:---:|:---:|---|
| BUG-DEPLOY-001 | Le log de build d'un server deploy affiche « Deployment ready: <url> » AVANT l'issue réelle du build : le flux générique loggue prématurément, et un deploy FAILED expose donc une URL « ready » qui 502. Constaté live sur `cmrmb9igi…` (15/07) — a conduit à tester une URL d'un build échoué. | ✅ | ✅ `523795af` | ✅ **15/07** | `createDeploymentLogs` ne fabrique plus applied/ready pour provider=server. Preuve live : deploy cassé exprès (`cmrmkmfck…`, build `exit 1`) → FAILED, logs = étapes réelles uniquement, zéro « Deployment ready ». `docs/deploy-evidence/2026-07-15-phase-b/` |
| BUG-DEPLOY-002 | L'URL d'un déploiement FAILED (manifestes nettoyés) rend un 502 JSON brut `SERVER_DEPLOY_UPSTREAM_ERROR` au lieu d'une page « deployment failed » propre côté preview-proxy. | ✅ | ✅ `523795af` | ✅ **15/07** | Wake tri-état au proxy : 404 manager → **410** `SERVER_DEPLOY_NOT_LIVE` (JSON) + page HTML « This deployment is not live ». Prouvé live sur `d-cmrmb9igi…` ET `d-cmrmkmfck…`. |
| BUG-API-003 | `POST /api/runtime/workspaces` répond 500 `API_ERROR` pendant un cold start : `metrics.increment('workspace_cold_start_pending_total')` (et `workspace_cold_start_write_recovered_total`, `app.ts:12714/14605/14820`, introduits par `a41239eb`/`bdce73d0`) jette `Unknown metric` car les compteurs ne sont pas déclarés dans le registre `@vibecore/observability`. Le pod workspace est bien créé avant le throw → l'UI voit un 500 au lieu de `starting`. Reproduit live le 15/07 (projet QA `cmrm9vobb00070nfe075k2bpv`, log `reqId 0e43d6cec610ca0b6bd357e77a16fbf7`). | ✅ | ✅ `a41239eb`/`bdce73d0` (prod `6d57a401`) | ✅ **24/07** | Compteurs déclarés dans `packages/observability`. **Preuve live dans le pod api prod** (`kubectl exec … tsx`) : `increment('workspace_cold_start_pending_total')` et `…_write_recovered_total` = **OK_NO_THROW** ; contrôle négatif (métrique inconnue) jette encore `Unknown metric` (garde réelle). **20 h de logs api prod = 0 erreur `Unknown metric`** (avant le fix chaque cold start en émettait une). Le throw seul cause du 500 est éliminé dans le binaire déployé. `docs/deploy-evidence/2026-07-24-bug-closeout/BUG-API-003-cold-start-metrics.md` |
| BUG-SOL-001 | Le chemin public `/solutions/internal-ai` aboutit sur une 404 au lieu de la page Internal AI | ✅ | ✅ `8c965f19` (poussé sur `main`) | ✅ **24/07** | Le code de redirection n'avait **jamais été commité** (le tableau sur-revendiquait) : `solutionPages` n'avait que `internal-ai-builder`. Ajout d'un alias 308 dans le loader `solutions.$slug.tsx` + spec 3/3 (redirect / page canonique servie / slug inconnu 404). **AVANT (prod 6d57a401)** : `/solutions/internal-ai` → **404**. **APRÈS déploiement (prod web `7287c27d83`)** : `curl` → **HTTP 308 → `/solutions/internal-ai-builder`** puis chaîne suivie → **200**. |
| BUG-SOL-002 | App Builder présente des démos UI salon à proximité d'affirmations de génération sans montrer le vrai run dans l'IDE E-Code | ✅ | ✅ (générateur supprimé, wording corrigé — sur `main`) | ✅ **24/07** | **(1) Défaut corrigé** : `generate-app-builder-visuals.ts` (générateur HTML/CSS/JS autonome) **supprimé** (n'existe plus que dans l'historique) ; plus aucun wording « vitrine » ; `/solutions/app-builder` utilise de **vraies captures produit** + copy honnête (« AI-guided generation, real files and preview validation ») + CTA « Start building »→/signup. **(2) Preuve réelle prompt→agent→fichiers→Preview live sur prod** (compte JETABLE, supprimé après) : projet `cms8ikwey…`, prompt envoyé → **agent « Done 100% »** (`✓ Create src/App.tsx 1.6s`, `✓ Create src/styles.css 2.5s`, `Start Application — Running`). Fichiers **vérifiés octet-par-octet via l'API prod** (`GET /projects/:id/export/zip`) : `src/App.tsx` (1600o, `useClock` setInterval 1s + heading « E-Code live proof » + bouton « Change color ») et `src/styles.css` (1739o, accent `#9E7FFF`) — **vrai projet E-Code, pas du HTML autonome**. **(3) Preview E-Code rendue live** : le Webview affiche l'app générée servie par le dev server Vite (:5173) — texte iframe capté `« E-CODE PROJECT · E-Code live proof · CURRENT TIME 09:13:20 AM · Change color »` (horloge qui tourne), capture `sol002/09b-preview-iframe.png`. Boucle complète prompt→agent→fichiers→Preview prouvée. Compte jetable **supprimé** après. `docs/deploy-evidence/2026-07-24-bug-closeout/BUG-SOL-002-app-builder-live.md` |
| BUG-UX-001 | Le CTA « Resume project » est ambigu quand plusieurs projets existent : le projet ciblé n'est pas nommé et aucun choix n'est proposé | ✅ | ✅ | ✅ | Correction intermédiaire `8a08f570`, ensuite simplifiée par BUG-UX-003 : les CTA Resume/Choose ont été supprimés au profit des cartes triées et de leur action `Open IDE`. |
| BUG-UX-002 | Le menu de navigation mobile et tablette ne peut pas défiler sur les écrans de faible hauteur | ✅ | ✅ | ✅ | `7cf6d1cc` poussé sur `main`. Scroll réellement exécuté à 390×600, 768×600 et 1024×600 ; `scrollHeight > clientHeight`, `scrollTop > 0`, dernière entrée visible, cibles ≥44 px et aucun débordement horizontal. Captures clair/sombre 390 px dans `docs/ui-ux-evidence/2026-07-14/`. |
| BUG-BRAND-001 | Le splash pré-hydratation « Loading E-Code » affiche encore le carré orange hérité de bolt.diy sur l’IDE et les pages marketing ; audit complet favicon/PWA/OG/title/assets requis | ✅ | ✅ | ✅ | Correctif `b1404989` poussé sur `main` ; déploiement `29346264927` vert. Test pré-hydratation réel sur `app.e-code.ai` et `e-code.ai`, clair/sombre : 4/4 PASS, SVG inline, variante exclusive correcte, zéro image externe et zéro carré legacy. Captures + rapport : `docs/ui-ux-evidence/2026-07-14/branding-splash/`. |
| BUG-UX-003 | Les actions d’en-tête « Resume project » et « Choose project » font doublon avec la grille triée par activité et le bouton « Open IDE » de chaque projet | ✅ | ✅ | ✅ | `a328f3a1` poussé sur `main`. Validé en réel avec deux projets triés par activité : aucun CTA Resume/Choose, deux liens Open IDE et New project conservé. Captures `dashboard-project-actions-{light,dark}.jpg` ; matrice Playwright `0642266a` verte. |
| BUG-UX-004 | La navigation de l’espace utilisateur est trop espacée sur mobile et tablette | ✅ | ✅ | ✅ | `93ba2248` poussé sur `main`. Espacements densifiés avec cibles ≥44 px ; scroll et absence de débordement validés à 390/768/1024/1440. Captures `mobile-navigation-scroll-{light,dark}.jpg` ; matrice Playwright `0642266a` verte. |
| BUG-UX-005 | Le loader global plein écran peut recouvrir un dashboard déjà rendu pendant plus de 30 secondes lors d’un fetch ou d’une revalidation secondaire | ✅ | ✅ | ✅ | `GlobalRouteLoader` est désormais piloté uniquement par `useNavigation`; les fetchers/revalidations secondaires conservent leur UI locale. Reproduction E2E exacte validée le 2026-07-14 sur `127.0.0.1:5176` avec backend réel `3001` et captures activées : `1 passed (37.5s)`, loader `aria-hidden="true"` et opacité `0` sur toutes les captures. |
| BUG-API-001 | Le gate global TypeScript est bloqué par cinq erreurs TS18046 dans `services/api/src/prisma-store.ts` lorsque le code Prisma inspecte un `error` de type `unknown` | ✅ | ✅ | ✅ | Type guard Prisma explicite, sans cast `any` ni changement de contrat métier. Store spec : 6 tests verts, 3 intégrations DB skippées ; typecheck API et `pnpm typecheck` global verts ; lint root 0 erreur. Correctif backend `a64d685c`. |
| BUG-CI-001 | Le spec des types de déploiement attend encore que `scheduled` soit indisponible alors que le backend scheduler le marque disponible, ce qui casse la suite globale | ✅ | ✅ | ✅ | Le spec reflète désormais le contrat backend : `static`, `autoscale` et `scheduled` sont disponibles, `reserved-vm` reste indisponible. Test ciblé : 5/5 ; suite globale : 515 fichiers et 3 850 tests verts le 2026-07-14. |
| BUG-CI-002 | Le build Cloud Build `runtime-tier` lance six installations pnpm/Docker en parallèle sur une VM 8 Go et peut se figer sans logs, bloquant tous les rollouts production en file | ✅ | ✅ | ☐ | Reproduction réelle : build régional `ded1fb17-39b1-4ddf-903c-59784cdd54cf` en `WORKING`, dernier log à `2026-07-14T19:02:36Z` pendant les six builds parallèles. DAG deps→api→workspace-manager→preview-proxy→ai-gateway→worker→screenshotter désormais séquentiel ; test de garde 1/1, validation CI/CD, lint, format et typecheck global verts. **✅ Testé live 24/07** : rollout réel Cloud Build `07c99732` (config `infra/cloudbuild/runtime-tier.yaml`, SHA `58c2a05e` / PR #56) = **SUCCESS**, 9/9 étapes **strictement séquentielles** (chaque étape démarre exactement à la fin de la précédente, zéro chevauchement), total 4m42s, **aucun gel**. `docs/deploy-evidence/2026-07-24-bug-closeout/BUG-CI-002-runtime-tier-sequential.md` |
| BUG-API-002 | Le scheduler local interroge `ScheduledTask` à chaque tick alors que la relation n'existe pas dans la base locale | ✅ | ✅ | ✅ | La migration existante `0069_scheduled_tasks` était en attente, pas absente. Le `predev` API déploie désormais les migrations avant le scheduler. Validé sur PostgreSQL 16 vierge (72 migrations, second passage no-op, tables/enums/index/PK/FK), puis sur la base locale `:55432` : statut à jour, `/health` 200 et ticks à 500 ms sans erreur ; 58 tests verts, 3 intégrations DB skippées. |
| BUG-UX-006 | La route `/account-settings/data` marque simultanément « Account » et « Data & privacy » comme éléments actifs dans la sidebar | ✅ | ✅ | ✅ | `93ba2248` poussé sur `main`. Une seule ancre visible porte `aria-current="page"`, avec le libellé « Data & privacy ». Capture `account-data-light-1440.jpg` et matrice Playwright `0642266a` verte. |
| BUG-UX-007 | Les panneaux « Organization members » et « Data & privacy » peuvent remplacer leur contenu par une erreur de route sans état local récupérable | ✅ | ✅ | ✅ | `7523f14e` poussé sur `main`. Injection de panne réelle via une instance web reliée à un port API indisponible : contrôles membres/export/suppression masqués, message explicite, retry fonctionnel et cible ≥44 px, sans débordement à 1440 clair et 390 sombre. Captures `organization-members-error-*` et `account-data-error-*` ; matrice Playwright `0642266a` verte. |
| BUG-UX-008 | Les dates et nombres de la user area utilisent la locale implicite du navigateur ou du serveur au milieu d'une interface anglaise | ✅ | ✅ | ✅ | `44e89509` introduit les formateurs déterministes `en-GB`/UTC et `795c00b1` migre les surfaces hors IDE avec un garde-fou contre les `toLocale*` implicites. Vérifié avec un navigateur forcé en `fr-FR` : Playwright vert sur 1440/1024/768/390, clair/sombre, captures `user-area-locale-*`; 518 fichiers et 3 862 tests verts, typecheck/lint/build verts. |
| BUG-UX-009 | La barre animée du loader global élargit le document lorsqu'un contexte desktop ou tablette est redimensionné à 390 px | ✅ | ✅ | ✅ | `44e89509` confine la piste du loader global. Le test réel redimensionne les contextes desktop/tablette jusqu'à 390 px et vérifie `document.scrollWidth <= window.innerWidth`; matrice Playwright locale verte sur les trois profils. |
| BUG-UX-010 | Le panneau du tiroir mobile reste translaté à `-280px` lorsqu'il est fermé et étend la zone de scroll du document racine | ✅ | ✅ | ✅ | `44e89509` retire la translation persistante du panneau fermé et limite l'animation à l'ouverture, avec respect de `prefers-reduced-motion`. Vérifié sans débordement racine à 390 px, navigation et clés API utilisables, capture sombre `user-area-locale-dark-390.jpg`. |
| BUG-UX-011 | Une panne du loader principal de plusieurs surfaces hors IDE remonte jusqu'à la boundary racine, tandis qu'une navigation entre panneaux ne présente aucun skeleton local | ✅ | ✅ | ✅ | `4652a247` poussé sur `main`. Boundary assainie et récupérable sur 23 routes AppShell/ProjectShell hors IDE, retry réel et skeleton local pendant les transitions SPA. Playwright : réponse API réelle retardée puis libérée sur desktop/tablette/mobile (`12 passed`, `3 skipped`) ; panne API injectée sur Dashboard, API keys, Organization members et Data & privacy (`3/3`), clair 1440 et sombre 390, sans overflow et avec retry ≥44 px. Captures `user-area-navigation-loading-*`, `dashboard-error-*`, `api-keys-error-*`. Gates : typecheck, lint 0 erreur, build production, 521 fichiers et 3 876 tests verts. |
| BUG-UX-012 | Les cartes projet étirent leur contenu, hiérarchisent mal l'aperçu et ne rendent pas le statut, l'activité et l'action principale assez scannables selon la largeur | ✅ | ✅ | ✅ | Grille `auto-fit` plafonnée à 26 rem, aperçu réel/fallback honnête, statut superposé, activité, déploiements et CTA Open IDE ≥44 px. Playwright réel avec deux projets : 1 colonne à 390, 2 colonnes à 768/1024/1440, largeur plafonnée et zéro débordement. Captures clair/sombre et tablette dans `docs/ui-ux-evidence/2026-07-15/`. |
| BUG-UX-013 | Un échec d'enregistrement des préférences ou de lecture des notifications peut laisser l'utilisateur sans restauration fiable ni action Réessayer locale | ✅ | ✅ | ✅ | Requêtes annulables, rollback vers le dernier état confirmé, panneau conservé et retry de la requête réelle pour préférences, mark-all et mark-read. 3 tests de récupération verts ; Playwright réel sur 390/768/1024/1440 avec PATCH interrompu, restauration, retry ≥44 px, persistance après reload et zéro débordement. Captures erreur/récupération clair 1440 et sombre 390 dans `docs/ui-ux-evidence/2026-07-15/`. |
| BUG-IDE-001 | **Packages — l'install vise le mauvais workspace → 502, l'UI annonce un succès.** `POST /projects/:projectId/packages/install` appelait `authorizeRuntimeWorkspace(request, projectId, …)` ; passé un projectId ce résolveur retombe sur l'id déterministe `ws-<sha256(projectId:userId)[:16]>`, un pod inexistant dès que le workspace actif du projet est un autre enregistrement (workspace créé via l'API publique `POST /projects/:id/workspaces`, ou choisi dans le sélecteur du panneau). Dans le même bloc d'action, *audit*/*outdated* utilisaient bien le workspace résolu — seul *install* l'omettait. | ✅ | ✅ branche `fix/cluster-b-ide-panels` (PR, **non mergée**) | ✅ **06/08** reproduit live | Compte JETABLE sur prod, projet `cmshm2ued…`, workspace `cmshm3a5d…` RUNNING (`primaryWorkspaceId` = `activeWorkspaceId` = ce même id, 1 seul workspace en base) ; install ciblait `ws-dcc7ee0014fa4679`. Depuis l'**UI réelle** : POST panneau → **200 `{ok:true}`** mais `grep -c lodash package.json` → **0** et `ls node_modules/lodash` → **No such file or directory** ; run enregistré `exitCode 1 / failed / "Runtime request failed"` ; appel direct API → **502 `WORKSPACE_AGENT_REQUEST_FAILED`**. Correctif : `workspaceId` optionnel dans `packagesInstallSchema`, résolution `body.workspaceId ?? projectId` + rejet **403 `WORKSPACE_PROJECT_MISMATCH`** cross-projet, BFF transmet le workspace résolu. Tests : install ciblé + rejet cross-projet ; `api.spec.ts` **123/123**. `LAUNCH_READINESS.md` |
| BUG-IDE-003 | **Ports — le panneau ne liste jamais aucun port.** `GET /api/runtime/workspaces/:id/ports` répond un **tableau nu** ; le loader faisait `{...(runtimePorts as any)}` → `[{port:5173}]` devenait `{0:{port:5173}}`. Côté UI `runtimePortsFromPayload` n'accepte qu'un tableau ou `.ports` → toujours `[]`. Le fallback d'erreur du loader (`{ports: []}`) montrait la forme attendue. | ✅ | ✅ branche `fix/cluster-b-ide-panels` (PR, **non mergée**) | ✅ **06/08** reproduit live | Port réellement ouvert au moment de la capture : runtime → `[{"port":5173,"ready":true,"url":"https://…-5173.preview.e-code.ai/"}]`, et l'URL **sert la vraie app** (HTTP 200, `<title>qa-clusterb</title>`, `id="root"`, `main.tsx`) en public ET via proxy authentifié ; payload panneau `keys = ['0','portsState','workspaces','selectedWorkspaceId','workspaceId']` ; UI → « **No ports detected yet** » sur **web 1280, tablette 768 ET mobile 375**, alors que la barre de statut affiche bien `5173`. Le panneau **Terminal** reçoit `runtimePorts` correctement → défaut isolé au loader Ports. Correctif : helper exporté `normalizeRuntimePorts()` ; 4 tests dont un verrouillant la non-régression de `{0: …}`. `LAUNCH_READINESS.md` |
| BUG-IDE-002 | **Agent — `EEXIST` remonte en 500/502 opaque au lieu de 409.** `/files/create` écrit avec `flag:'wx'` ; `rethrowFsError` mappe `ENOENT`/`EISDIR`/`ENOTDIR`/`ENOSPC`/`EDQUOT` mais **pas `EEXIST`** → 500 non codé, réétiqueté par l'API en 502 `WORKSPACE_AGENT_REQUEST_FAILED`, c.-à-d. **le signal « pod mort »** : « New file » sur un nom existant affiche « Internal server error », et le 502 déclenche à tort le fallback local-runtime en dev (piège documenté juste au-dessus du mapping `ENOSPC`). | ✅ | ✅ branche `fix/cluster-b-ide-panels` (PR, **non mergée**) | ✅ **06/08** reproduit live | 1er create `audit-new.txt` → **204** ; 2e create → **502 `WORKSPACE_AGENT_REQUEST_FAILED`** ; logs api prod : `"Workspace agent request failed: 500"` ; agent joint **en direct dans le pod** avec un token manager valide → `/files/write` **200** (agent sain, donc pas un pod mort) ; disque OK (`1%`, 9,6 G libres). Correctif : `EEXIST` → **409 `File already exists`** ; test 2e create → 409 + le contenu d'origine survit ; `workspace-agent` **47/47**. `LAUNCH_READINESS.md` |
| BUG-IDE-004 | **Éditeur — conflit de sauvegarde avalé silencieusement (perte d'édition).** Quand le fichier a changé côté serveur depuis son ouverture, la sauvegarde est refusée (protection légitime) mais **l'échec ne sortait que dans la console** : l'onglet restait `●` sans message, sans bandeau, sans Recharger/Écraser/Diff, et l'édition n'était persistée **nulle part**. Déclencheur réaliste : le reseed/reconcile qui réaligne le runtime sur le project storage réécrit des fichiers ouverts. | ✅ | ✅ branche `fix/ide-save-conflict-and-packages-status` (PR, **non mergée**) | ✅ **06/08** défaut reproduit + correctif **prouvé live sur pod prod** | **Défaut** : console `Autosave failed … Remote file changed since it was loaded` ; onglet « README.md● » toujours sale après **bouton Save, Ctrl+S et Cmd+S** ; édition absente des **3** magasins (export project storage, `/ide-state`, runtime). **Correctif 3 couches** : (1) `RemoteFileConflictError` typée portant remote/local/baseline, levée **avant** `writeFile` donc le tampon n'est jamais touché (message byte-identique, des appelants le matchent) ; (2) store de conflit + `saveFileWithConflictPrompt()` → `'saved'|'conflict'` + résolutions écraser/adopter, avec court-circuit silencieux si les deux versions sont byte-identiques ; (3) dialogue non fermable au backdrop **View diff · Reload from disk · Keep my version · Keep editing**. Câblé sur **tous** les points de sauvegarde (éditeur projet, onglet sale, **autosave**, Workbench, revert DiffView). **Preuve LIVE contre un vrai pod prod** (`ws-2e1436ecf1bd10b8`, aucun mock sous le store) : 3/3 verts — conflit typé portant les vraies versions du pod, rien écrasé au moment du conflit, « Keep my version » retrouve l'édition et l'écrit dans le pod, « Reload » adopte le disque sans réécrire. + **7 tests sur le vrai composant** (rendu + clics) : 3 issues présentes, diff réel affiché, « Keep editing » ne déclenche aucune résolution. ⚠️ preuve navigateur bout-en-bout impossible : machine au-dessus de `kern.maxfiles` (38 221/30 720 fd) à cause d'un dev server d'une autre session → `ENFILE`. `LAUNCH_READINESS.md` |
| BUG-IDE-005 | **Packages — l'action renvoie `{ok:true}` même quand le run échoue.** Le bloc `packages` retombait sur le `return json({ ok: true })` commun quel que soit `run.exitCode` ; l'échec n'était visible que dans « Install & runtime checks » de la sidebar, **sous la ligne de flottaison**. | ✅ | ✅ branche `fix/ide-save-conflict-and-packages-status` (PR, **non mergée**) | ✅ **06/08** défaut reproduit live prod | **Défaut (prod, code actuel)** : install de `@vibecore/definitely-not-a-real-package-9f3a` → **HTTP 200 `{"ok":true}`** alors que le run enregistré est `exitCode 1 / status failed` avec `npm error 404 … tarball, folder, http url, or git url.`. **Correctif** : helper exporté `describePackagesRunOutcome()` — succès → 200, échec → **422** (requête valide, commande exécutée, commande échouée) avec un `error` portant script + code de sortie + fin de la sortie npm, que le submit générique du panneau affiche déjà **inline**. 5 tests dont un **ancré sur la sortie npm réellement capturée en prod**. `LAUNCH_READINESS.md` |
- **2026-07-15 — GC workspaces + idle-sweep server-deploys ne moissonnent pas** : cause racine trouvée et corrigée le 16/07 → voir **BUG-CRON-001** ci-dessous (résolu, testé live).

| ID | Bug | 📤 Dispatché | 💻 Codé | ✅ Testé live | Preuve |
|---|---|:---:|:---:|:---:|---|
| BUG-CRON-001 | **TOUS les CronJobs plateforme morts depuis ~9/07** (workspace-gc, deploy-reap, siem-deliver, connector-reconnect, metering…) : le bump BullMQ ^5.76 (`bc7a393b`, durable deploy queue) rejette les jobId contenant `:` — `enqueue-cli.ts` composait `${job}:${ENQUEUE_DEDUP_KEY}` → chaque pod d'enqueue sortait `{"event":"enqueue.failed","error":"Custom Id cannot contain :"}` et le Job K8s passait Failed (BackoffLimitExceeded). Conséquences : aucun idle-stop workspace, aucun sleep server-deploy (les apps Phase B sont restées 1/1 12 h), reaper deploy mort, SIEM/metering morts. C'est la cause racine de l'observation « GC ne moissonne pas » du 15/07. | ✅ 16/07 | ✅ `9b3315b1` | ✅ **16/07** | Diagnostic live : pod nu (sans ENQUEUE_DEDUP_KEY) → `enqueued`, job CronJob → `enqueue.failed Custom Id cannot contain :`. Après CD : jobs `-29736310` (deploy-reap, siem) **Complete 1/1** avec worker `9b3315b127`, puis tick 05:15 `cron-workspace-gc-29736315` **Complete 1/1** et le sweep idle a endormi TOUTES les apps idle (11 Deployments `app-*` → 0/0, dont `app-cmrmb34mz`/`app-cmrmc2v0u` bloquées 1/1 depuis 12 h). +5 specs enqueue-cli (forme du jobId verrouillée). |
| BUG-AGENT-EDIT-TRUNCATION | **P0 perte de données** — une édition in-place de l'agent tronque le fichier : le `});` de fin disparaît, un fragment `</bo` reste, le JS devient invalide (constaté 2/2, déterministe). Cause racine MESURÉE : `StreamingMessageParser` (`app/lib/runtime/message-parser.ts`), branche de streaming `else` (balise fermante pas encore complète) émettait `input.slice(i)` **verbatim**, y compris une `</boltAction>` **partielle coupée entre deux chunks** (`…});\n</bo`). Cette valeur alimente l'aperçu éditeur ET l'autosave. Quand la sortie du modèle est **tronquée en pleine balise** (arrêt sur `</bo`), `onActionClose` — seul à retirer la vraie balise — ne se déclenche jamais → le `</bo` est ce qui est persisté. Défaut classique du délimiteur coupé entre chunks. | ✅ 17/07 | ✅ `0e0017ae` | ☐ | Fix `withoutTrailingCloseTagPrefix()` : retient le plus long suffixe qui est un préfixe propre de `</boltAction>`, sur les branches `file` ET `diff`. Preuve sur le parser RÉEL, cas exact 2/2 : **AVANT** `saved="…setup(server);\n</bo"` sha `26bb7dc9…` **ne parse pas** (`Unexpected token '<'`) → **APRÈS** `saved="…setup(server);\n"` sha `ca6fb623…` **parse OK**, `});`+tail préservés. Cas dur (fichier long + accents/CJK/emoji, balise splittée en 2 chunks) : `onActionClose` byte-exact. Tests de non-régression qui **ÉCHOUENT sans le fix** (2/2 sur l'assertion `not.toContain('</bo')`) ; **63/63** specs message-parser + 3 enhanced verts. Artefacts : `docs/deploy-evidence/2026-07-17-agent-truncation/`. **✅ Testé live 24/07** : le parser déployé (web prod `6d57a401`) est **byte-identique** à HEAD (`git show 6d57a401:…message-parser.ts \| shasum` = `36b3ec18…` = HEAD). Repro déterministe `message-parser.truncation-live.spec.ts` sur ce parser (2/2) : sortie modèle tronquée sur `});\n</bo` → contenu sauvegardé = **JS valide** (`new Function` ne jette pas), `});`+`setup(server)` préservés, **aucun `</bo`** ; corps avec accents/CJK/emoji (pas de coupe UTF-8) ; contrôle pré-fix jette `Unexpected token '<'`. `docs/deploy-evidence/2026-07-24-bug-closeout/BUG-AGENT-EDIT-TRUNCATION-live.md` |

| BUG-AGENT-001 | **P0 — le parcours cœur « prompt → app → aperçu » ne produit pas d'application qui tourne, et le statut ment.** L'agent annonce « Terminé 100 % », coche « Terminé » sur les 20 fichiers et écrit « Le serveur de développement est lancé et l'aperçu affiche immédiatement des données d'exemple réalistes » — alors qu'en réel le runtime s'arrête définitivement à **19 fichiers sur 22** : **3 fichiers ne sont jamais écrits** — `src/App.tsx`, `src/hooks/useTasks.ts`, `src/components/AddTaskForm.tsx` — et **aucun serveur Vite ne tourne** (port 5173 : `Connection refused`) même **20 min après** l'annonce de fin. `src/App.tsx` étant importé par `main.tsx`, l'app **ne peut pas builder** : perte de fichiers silencieuse, pas un simple retard. Cause racine mesurée : amplification d'écritures ×37 (**750 `PUT /files/write` pour 20 fichiers**). Le garde `if (!doc)` de `workbench.ts:2880-2885` ne se referme jamais pendant le streaming, car `EditorStore.updateFile` (`app/lib/stores/editor.ts:89-95`) sort en no-op quand le document n'existe pas et **ne le crée pas** → un PUT du fichier entier part à chaque tick de 100 ms (`ACTION_STREAM_SAMPLE_INTERVAL_MS`, `workbench.ts:151`+`3444`). Les écritures streaming **contournent** la file d'exécution (`workbench.ts:2746-2752`) que l'écriture finale emprunte : un *trailing* partiel peut écraser la version complète, et `abortStreamingFileActions` (`Chat.client.tsx:745` → `action-runner.ts:791-795`) annule les écritures autoritatives encore en file → fichiers jamais écrits. | ☐ | ☐ | ☐ **Constaté live 15/08** | Env de test `vibecore-audit-test-20260807`, images `api:1c68880b39` / `web:df1287d856`, projet `cmsusbw8q00040nbf7dddmsq1` / ws `ws-8837656e73850f1c`, compte `qa-loop-1786822972@local.test`. Convergence mesurée dans le pod par échantillonnage `find /workspace` (relevés 19:54:19 → 20:08:44 UTC) : 10 → 13 → 17 → 19 fichiers, puis **définitivement bloquée à 19/22**. Manquants confirmés à T+20 min : `src/App.tsx`, `src/hooks/useTasks.ts`, `src/components/AddTaskForm.tsx`. `wget http://127.0.0.1:5173/` → `Connection refused` à T+20 min, `ps aux` : aucun process Vite, seulement le workspace-agent. Logs API : **750** `PUT …/files/write` (tous 204) pour 20 fichiers. **Le code fautif est identique sur `origin/main`** (`ACTION_STREAM_SAMPLE_INTERVAL_MS` l.157, sampler l.3490-3492, `runAction(data, true)` sous `if (!doc)` l.2915) — défaut non introduit par la branche d'audit. |
| BUG-AGENT-002 | **P1 — les 429 ne sont pas gérés sur le chemin d'écriture de l'agent, et sont rejoués.** `app/lib/runtime/retry.ts` sait pourtant classer les 429 (`TRANSIENT_STATUSES`, `isHardQuota429`) et son commentaire documente déjà l'incident (« observed in prod as 100+ file-write retries »), mais `withRuntimeRetry` **n'est branché nulle part sur ce chemin** (seulement `ProjectWorkspaceProvider.tsx:174/187`, `workbench.ts:942`, `workbench.ts:3259`). Résultat : `RemoteRuntimeAdapter.#requestOnce` (`packages/runtime-remote/src/index.ts:954-978`) ne retient que `502/503/504` comme transitoires et remonte le 429 sec ; `ActionRunner.#runActionWithRetry` (`action-runner.ts:530-570`) le rejoue **3×** sans respecter `Retry-After` → amplification supplémentaire. Un seul utilisateur consomme ~37 % de son budget minute (`API_RATE_LIMIT_MAX ?? 2000`, `services/api/src/app.ts:8023`) sur une seule génération. | ☐ | ☐ | ☐ **Constaté live 15/08** | Logs API sur la génération ci-dessus : **32× `429` sur `POST /projects/<id>/snapshots`** et **10× `429` sur `POST /api/runtime/workspaces`**, pour un seul utilisateur en usage nominal. Code identique sur `origin/main`. |
| BUG-AGENT-003 | **P1 — les 5 sous-agents parallèles échouent tous, le consensus est rejeté à 0 %, et le run est quand même affiché « Terminé 100 % ».** Le panneau Agent affiche « Plan 0/5 terminées », les cinq voies (Architecte, Frontend, Backend, DevOps, QA) portant chacune « Cet agent spécialisé n'a pas pu terminer sa tâche », et « Consensus · quorum · **Rejeté** · 0 % d'accord » — pendant que l'en-tête du run affiche « Agent · **Terminé** · **100 %** ». Statut malhonnête : un échec total de la voie multi-agents est présenté comme un succès complet. La dernière action fichier (`src/lib/taskLogic.test.ts`) est en outre marquée « **Arrêté** » (32,3 s) sans que cela dégrade le statut global. | ☐ | ☐ | ☐ **Constaté live 15/08** | Même run que BUG-AGENT-001 (projet `cmsusbw8q00040nbf7dddmsq1`). Relevé textuel du panneau Agent, format desktop 1440. |
| BUG-IDE-006 | **P1 — l'arbre de fichiers reste désynchronisé et « Actualiser les fichiers » ne le répare pas.** Après la génération, la Bibliothèque annonce « **9 fichiers** » puis « 10 » après clic sur *Actualiser les fichiers*, pendant que le badge Git du même écran annonce « **20 fichiers modifiés** » et que le store projet en contient bien 20 (`GET /api/projects/<id>/files` → 20 entrées). L'utilisateur ne voit donc pas les fichiers que l'agent vient de créer, et l'action de rafraîchissement explicite ne corrige pas l'écart. | ☐ | ☐ | ☐ **Constaté live 15/08** | Badges relevés dans le DOM : `Bibliothèque, 9 fichiers` → clic `Actualiser les fichiers` → `Bibliothèque, 10 fichiers`, avec `Git, 20 fichiers modifiés` au même instant. `GET /api/projects/cmsusbw8q00040nbf7dddmsq1/files` = 20 chemins (dont `src/App.tsx`, `src/components/*`, `src/index.css`). Desktop 1440. |
| BUG-UX-014 | **P2 — l'écran de démarrage de la Webview affiche « Prêt » et coche ses 4 étapes alors qu'il indique juste en dessous « Le serveur d'aperçu démarre encore ; nouvelle tentative… ».** Les deux messages se contredisent dans le même panneau, et l'état est resté figé ainsi (aucun serveur ne tournait réellement — cf. BUG-AGENT-001). Le journal affiché ajoute « Le point de contrôle de l'IA a été ignoré après l'acceptation du patch » ×2, qui se lit comme une erreur pour un utilisateur. | ☐ | ☐ | ☐ **Constaté live 15/08** | Capture du panneau Webview, desktop 1440 : titre « DÉMARRAGE DE LA WEBVIEW », sous-titre « **Prêt** », corps « Le serveur d'aperçu démarre encore ; nouvelle tentative… », 4 étapes (Compilation de…, Compilation en…, Démarrage du…, Prêt) toutes cochées. |
| BUG-API-004 | **P2 — course au provisioning : l'API tape le workspace agent avant que le DNS du Service soit résolvable et rend un `502` sec au lieu d'un état « en cours de provisionnement ».** `GET /api/runtime/workspaces/:id/files?path=.` → **42×** `502 WORKSPACE_AGENT_REQUEST_FAILED` / `getaddrinfo ENOTFOUND workspace-ws-<id>.workspaces.svc.cluster.local`. Le Service et ses endpoints existent bien (créés à la même seconde que le pod) : l'erreur est une fenêtre de propagation DNS, mais elle est rendue à l'utilisateur comme une panne serveur pendant le chargement de l'IDE. | ☐ | ☐ | ☐ **Constaté live 15/08** | Logs API (3 réplicas) sur 15 min : 42 occurrences, horodatages 19:45:16→19:45:20 UTC pour `ws-8837656e73850f1c` (pod+svc créés 19:45:18Z) et 19:47:25 pour `ws-417afdf7cc9ffb2d` (créé 19:47:31Z). `kubectl -n workspaces get endpoints` : endpoints présents et sains après la fenêtre. |
| BUG-IDE-007 | **P3 — `lost+found` du PVC ext4 est exposé dans l'arbre de fichiers du projet, et sa lecture rend `400`.** Le répertoire système du volume remonte dans la liste des fichiers utilisateur ; le lecteur le refuse ensuite. | ☐ | ☐ | ☐ **Constaté live 15/08** | Logs API : `GET /api/runtime/workspaces/<id>/files/read?path=lost%2Bfound` → **400** (×4, sur deux workspaces distincts). |

| BUG-I18N-001 | **P1 — sur `https://e-code.ai/solutions` en locale FR, 8 des 9 cartes affichent leur titre et leur description en ANGLAIS**, au milieu d'une page par ailleurs entièrement traduite (titre « Solutions E-Code », chapô, « Voir la page », CTA « Commencez à créer avec E-Code »). Seule la carte **Enterprise** est en français. Page publique, premier écran d'un prospect ou d'un investisseur qui clique « Solutions ». Cause racine : l'index rend `localizeMarketingPage(page, language)` (`app/components/marketing/EcodeMarketingPages.tsx:512`), qui retombe sur la définition anglaise dès que `getMarketingPageCopy(slug, 'fr')` ne trouve rien — or **`marketingPageCopyFr` (`app/lib/i18n/catalogs/marketing.ts`) ne contient d'entrée que pour `enterprise`** ; `app-builder`, `website-builder`, `game-builder`, `dashboard-builder`, `chatbot-ai-agent-builder`, `internal-ai-builder`, `startups` et `freelancers` en sont absents (vérifié sur `origin/main`). Incohérence aggravante : les **pages de détail** de ces mêmes solutions (`/solutions/app-builder`, servies par des routes statiques dédiées) sont, elles, **intégralement en français** — la traduction existe donc déjà, elle n'est simplement pas reliée à l'index. | ☐ | ☐ | ☐ **Constaté live 15/08** | `https://e-code.ai/solutions`, locale FR, desktop 1440 clair. Relevé textuel : « App Builder — *Describe a business workflow and turn it into responsive screens…* », « Website Builder — *Create polished marketing sites…* », « Game Builder — *Design and launch interactive browser experiences…* », « Dashboard Builder — *Build data-rich dashboards…* », « Chatbot / AI Agent Builder — *Deploy conversational assistants…* », « Internal AI Builder — *Bring private AI tools…* », « Startups — *Ship products quickly…* », « Freelancers — *Deliver client projects faster…* » ; **Enterprise** en FR. Contrôle : `/solutions/app-builder` rend un titre `<title>` et un corps 100 % FR. Grep de couverture sur `origin/main` : 8 slugs sur 9 absents de `marketingPageCopyFr`. ⚠️ **Correctif = lot de copie marketing** — recoupe les fichiers travaillés par la session « Démo investisseurs » (`EcodeProductMarketingPages.tsx`, `Pricing.tsx`) : à traiter en commit atomique dédié pour éviter la collision. |

## Balayage QA continu — env de test dédié (2026-08-12)

Environnement : `vibecore-audit-test-20260807` (`docs/audit/TEST_ENV_RUNBOOK.md`), tenant de test
`qa-sweep-0812@ecode-audit.test` / org `org-q3t9fprz`. **Aucun compte ni cluster de prod touché.**

⚠️ **Réserve d'environnement (2026-08-12 ~10:00)** — une autre session redéployait la plateforme
pendant la passe : release Helm `vibecore` du cluster d'audit en **rev 5 `failed`** puis
**rev 6 rollback `failed`** (conflits de field-manager : `allow-dns-clusterip` vs
`kubectl-client-side-apply`, image `web` vs `kubectl-set`), tier `web` épinglé à la main sur
`web:seedfix001` alors que les 6 autres tiers sont sur `api:3a537e9a7e`. Les constats **runtime**
relevés pendant cette fenêtre sont donc non concluants et sont marqués `À RE-TESTER` ; seuls les
défauts **déterministes (code + rendu)** sont consignés comme confirmés ci-dessous.

| ID | Bug | 📤 Dispatché | 💻 Codé | ✅ Testé live | Preuve |
|---|---|:---:|:---:|:---:|---|
| BUG-QA-PROMPT-LEAK-001 | **Le README livré à l'utilisateur contient le prompt système interne.** Tout projet créé depuis `/projects/new` (chemin `sourceType='ai'`) reçoit un `README.md` où le champ « Prompt » n'est pas la demande de l'utilisateur mais le prompt **augmenté** : ~25 lignes d'instructions internes (`Production quality bar:`, `ZERO placeholder code: no TODO-only paths, dead buttons…`, `Target Fortune 500 / enterprise polish…`, `Finish with a start action so the live preview can attach automatically.`) + `Requested model: claude-sonnet-4-5-20250929`. Le fichier est visible dans l'arborescence, exporté au ZIP et commité au dépôt Git du projet — l'ingénierie de prompt E-Code part donc avec chaque projet client. | ☐ | ☐ | — (détecté, non corrigé) | **Code** : `app/routes/projects.new.tsx:590` compose `generationPrompt = languagePrefix + projectPromptForArtifact(prompt, …)` (les 25 lignes sont à `projects.new.tsx:345-373`, dont `ECODE_PROJECT_REQUIREMENT_LINES` de `app/lib/common/prompts/ecode-requirements.ts`) puis l'envoie tel quel en `prompt:` à `/orgs/:id/projects/from-ai` (`:616`) ; `services/api/src/app.ts:5786` écrit `…Prompt:\n\n${input.prompt}\n` dans `README.md`. **Live** : projet `cmsppjgo9001n0nf9oom73sxh` (« Crée une page d'accueil »), README rendu dans l'éditeur = `Contexte de génération / Type d'artefact : web / Framework privilégié : React + Vite + TypeScript / Modèle demandé : claude-sonnet-4-5-20250929`, puis `Production quality bar: - ZERO placeholder code…`. Le prompt réel de l'utilisateur n'apparaît qu'après ~25 lignes d'instructions internes. **Correctif attendu** : persister le prompt **utilisateur** dans le README et ne transmettre le prompt augmenté qu'à l'agent. |
| BUG-QA-AGENT-PROGRESS-001 | **La barre de progression de l'agent reste figée après une erreur terminale.** L'en-tête du panneau agent affiche indéfiniment « Agent · Génération de la réponse — 67 % » alors que la requête s'est terminée par un échec **définitif** (`AUTH_FAILED`, `isRetryable:false`) et que la carte d'erreur « Erreur d'authentification » est déjà rendue juste en dessous, avec ses boutons Réessayer / Ouvrir les paramètres / Fermer. Les 5 sous-agents parallèles affichent bien, eux, leur état terminal (« consensus : Rejeté · 0 % d'accord »). L'utilisateur voit donc simultanément « en cours à 67 % » et « échec ». | ☐ | ☐ | ✅ **12/08** reproduit live | Projet `cmsppjgo9001n0nf9oom73sxh`. Console : `[chat-onError] /api/chat request failed … {"error":true,"message":"Clé API absente ou invalide","statusCode":401,"code":"AUTH_FAILED","isRetryable":false}`. DOM relu ~8 min après l'échec : `{hasGenerating:true, percents:["67 %","0 %"], hasAuthError:true}` — le pourcentage n'est jamais remis à zéro ni masqué sur erreur terminale. Reproduit sur 1/1 tentative. |
| BUG-QA-I18N-COUNT-001 | **Compteur de fichiers non traduisible : espace perdu et pluriel absent.** Le compteur du panneau Bibliothèque rend « 7fichiers » (sans espace) et, à un seul fichier, « 1fichiers » (sans espace **et** au pluriel). Cause : `app/components/chat/BaseChat.tsx:10862` écrit `{fileCount} files`, ce qui produit **deux nœuds texte** JSX (`7` et ` files`) ; la couche i18n traduit le nœud ` files` en le normalisant → l'espace de tête disparaît et aucune règle de pluriel n'est appliquée. L'`aria-label` du bouton parent est correct (« Bibliothèque, 7 fichiers »), donc seul le libellé visible est atteint. | ☐ | ☐ | ✅ **12/08** reproduit live | Projet template `cmspq6pid001z0nf966f7sn68` (7 fichiers) : `document.querySelector('.bolt-project-files-count').textContent` → `"7fichiers"`. Projet `cmsppjgo9001n0nf9oom73sxh` (1 fichier) → `"1fichiers"`. Attendu FR : « 7 fichiers » / « 1 fichier ». **Correctif attendu** : clé i18n avec interpolation du compte et règle de pluriel (`t('files.count', {count})`) au lieu d'un littéral juxtaposé. |
| ~~BUG-QA-TPL-NORUNTIME-001~~ | **RETIRE le 12/08 — FAUX POSITIF, ma methodologie etait en cause.** J'avais conclu qu'un projet cree depuis un modele n'obtenait jamais d'espace d'execution. **C'est faux.** Re-teste sur le build propre (rev 7 `deployed`, `82603d55f7`) avec **l'IDE reellement ouvert et laisse ouvert** : le workspace `ws-090fb06e5a098932` passe **PENDING a t+15 s puis RUNNING a t+30 s**, le pod est cree, les fichiers sont synchronises (`README.md`, `index.html`, `package.json`, `src/`, `vite.config.ts`, **`node_modules` installe**) et **Vite sert l'app en HTTP 200** (`curl 127.0.0.1:5173` depuis le pod → `<!doctype html>` avec le HMR React). **La boucle coeur modele fonctionne.** | — | — | ❌ **retire** | **Pourquoi je m'etais trompe** : le provisionnement du runtime est **paresseux et pilote par le client** — il exige que l'IDE soit ouvert **et le reste**. Mes 3 "preuves" ne remplissaient pas cette condition. (1) Tentative 1 : projet cree **pendant le rollout Helm** (`rev 5 failed` / `rev 6 rollback failed`), le `workspace-manager` redemarrait — le clic « Redemarrer l'espace de travail » est reste sans effet parce que son backend etait indisponible, pas parce que le parcours modele est casse. (2) Tentative 2 : j'ai quitte l'IDE tout de suite apres la creation (navigation vers `/projects` puis tests curl) — le client n'est jamais reste pour declencher l'ensure. (3) Tentative 3 : creation **en curl, sans navigateur du tout** — aucun client, donc aucun provisionnement attendu. **Lecon** : sur un provisionnement pilote par le client, l'absence de pod ne prouve rien si le client n'est pas reste ouvert ; et ne jamais conclure a un defaut produit pendant un rollout en echec. La difference `pendingPrompt` entre `projects.new.tsx:682` et `dashboard_.templates.tsx:46-58` est **reelle** mais elle ne change que le *declencheur* (auto-submit du prompt vs ouverture simple), pas la capacite a provisionner. |
| BUG-QA-TOKEN-IN-URL-001 | **Le jeton de session est écrit en clair dans les journaux d'accès de l'API.** Les endpoints de flux de l'IDE portent le jeton en **paramètre d'URL** (`GET /api/runtime/workspaces/:id/files/watch?token=session_…`, idem `…/ports/watch`) ; le logger pino de l'API journalise `req.url` intégralement, donc chaque ligne `incoming request` contient un jeton de session **utilisable tel quel**. Tout puits de logs (Cloud Logging, SIEM, proxy intermédiaire) en reçoit une copie ; l'URL fuit aussi par `Referer`. Le dépôt connaît déjà le motif : `services/workspace-agent/src/app.ts:1575` documente qu'« un `?token=` en HTTP simple ferait fuiter le porteur dans les journaux d'accès/proxy ». | ☐ | ☐ | ✅ **12/08** observé live | Journal du pod `vibecore-vibecore-platform-api-5d7bb58cc6-tx8rk` : `{"level":30,…,"req":{"method":"GET","url":"/api/runtime/workspaces/cmspq6pid001z0nf966f7sn68/files/watch?token=session_ppEFxRALCrvCNU5bhSEAgRkAZijlxkHfuty5FjhGtkM",…},"msg":"incoming request"}`. Jeton **rejoué avec succès** depuis l'extérieur (`curl -H "Authorization: Bearer …"` → 404 métier `ORG_NOT_FOUND`, pas 401 : le jeton est bien accepté). **Contrainte réelle** : `EventSource` ne permet pas d'en-tête, d'où le paramètre — le correctif porte donc sur la **rédaction du champ `url`** dans le sérialiseur pino (et/ou un jeton de flux éphémère à usage unique), pas sur la suppression du paramètre. |
| OBS-QA-IDENTITY-FLIP-001 | **Bascule d'identité observée 2 fois dans le navigateur — conclusion : très probablement un ARTEFACT de sessions QA concurrentes, PAS un défaut plateforme.** Symptôme : sans déconnexion de ma part, l'onglet est passé de `qa-sweep-0812@ecode-audit.test` à `qa-audit-61926004@ecode-audit.test` (2 fois : `/account-settings.data` + `/projects.data` renvoyant les projets de l'autre tenant et masquant les miens ; la 2e fois capture d'écran de `/account-settings` affichant « QA Audit » / `qa-audit-61926004@…`). Ma ligne `Session` n'était jamais révoquée et une reconnexion explicite restaurait l'identité correcte. | ☐ | ☐ | ⚠️ **non retenu comme bug — cause externe probable** | **Le serveur a été disculpé par 4 tests ciblés.** (1) **Identité stable 6/6** : `curl` avec MON cookie `vc_session` → `qa-sweep-0812@ecode-audit.test` sur 6 requêtes consécutives. (2) **Aucune fuite d'identité par état de pod** : requête avec le **seul** cookie d'affinité `vc_upstream` (sans `vc_session`) → HTTP 202, **aucun e-mail**, idem sans aucun cookie — le tier web ne sert donc jamais une identité issue d'un cache par pod. (3) **Portée du cookie sûre** : `Set-Cookie: vc_session=…; Path=/; HttpOnly; SameSite=Lax; Secure` — **pas d'attribut `Domain`**, donc cookie *host-only* pour `app.…`, non partagé avec les hôtes `*.preview.…` (pas de fixation inter-sous-domaine). (4) **Pas de cache HTTP partagé** : `Vary: Cookie` présent, aucun `Cache-Control`/`Age`. `app/lib/.server/require-session.ts` relu : sans état, cookie revalidé par requête via `GET /auth/me`. **Cause retenue** : plusieurs sessions QA tournaient en parallèle sur cette même machine et sur ce même env (utilisateurs `qa-audit-61926004`, `gobolt-qa`, `gobolt-qa2`, `tpl026-*` ; session `qa-audit` créée à 06:55:15, ma bascule constatée ~06:59) — un profil/jar de cookies de navigateur partagé entre sessions explique exactement le symptôme. **À refaire proprement** si le doute persiste : 2 tenants dans 2 profils navigateur **isolés**, en boucle sur `/account-settings.data`, sans autre session QA active. |

### Vérifié et NON retenu (contrôles négatifs de la passe du 12/08)

Consigné pour éviter de re-chasser ces pistes : chacune a été testée en réel et s'est révélée saine.

| Piste | Verdict | Preuve |
|---|---|---|
| `preview.proxy.unreachable` port 5173 dans les logs de l'agent | **Sain** — transitoire pendant le boot de Vite | 9 occurrences pendant le démarrage, puis **0 sur les 3 dernières minutes** ; `curl` depuis le pod → `127.0.0.1:5173` et `10.20.2.10:5173` répondent, `node fetch` sur `localhost` → OK. (`::1` est bien injoignable sous gVisor, mais Node résout `localhost` en IPv4 ici.) |
| Isolation des hôtes d'aperçu | **Sain — fail-closed** | `GET https://ws-b20fe6a27570b5f3-5173.preview.…/` sans session → **403 `PREVIEW_TENANT_FORBIDDEN`**. (Réserve mineure : la réponse met **15,5 s** à sortir — un refus devrait être immédiat.) |
| Portée du cookie de session | **Sain** | `vc_session` sans attribut `Domain` → *host-only*, non partagé avec `*.preview.…` ; `Secure`, `HttpOnly`, `SameSite=Lax`. |
| Redirection d'authentification | **Sain** | Anonyme sur `/@org/<projet>`, `/dashboard`, `/@org/<projet-inexistant>` → **302 vers `/login?returnTo=…`** (jamais de 404 qui révélerait l'existence d'un projet). |
| Lenteur d'ouverture de l'IDE (37 s au 1er appel) | **Sain — cache froid** | Mesures successives sur la même route : 8,0 s → 1,8 s → 1,0 s ; contrôle `/dashboard` : 1,7 s → 0,7 s. |
| Débordement horizontal responsive (mobile 375) | **Sain** | `/` et `/pricing` à 375×812 : `documentElement.scrollWidth == innerWidth == 375`, **0 élément** dépassant la fenêtre. Accueil rendu (10 427 px de haut, 2 290 caractères). |
| Compteur de fichiers « ?fichiers » / éditeur Monaco vide au rechargement | **Sain — artefacts de rendu** | `?` est l'état de chargement ; Monaco contient bien le texte (`view-line` = `# React SaaS`, boîte 613×532). Les captures « vides » étaient des trames non composées du panneau navigateur. |
| Sidebar tronquée sur `/dashboard/templates` | **Sain** | Mesuré : largeur 240 px, **0 libellé** avec `scrollWidth > clientWidth` — troncature vue en capture = trame transitoire d'animation. |
| Routes marketing | **Saines** | 17 routes testées : 16× **200**, `/upgrade` **302** (attendu) ; temps 0,37 s – 2,6 s. |
| BUG-QA-DB-REFETCH-LOOP-001 | **P1 — Boucle de rechargement infinie du panneau Base de données : un seul onglet sature le tier API et le fait autoscaler jusqu'au maximum.** Dès que la charge utile du panneau devient vide/`undefined` (typiquement après un provisionnement de base **en échec**), `DatabaseWorkbench` reboucle sur `fetcher.load()` **sans borne ni backoff** : mesuré à **~110 requêtes `GET /projects/:id/databases` par tranche de 30 s** (≈ 3,7 req/s) depuis **un seul onglet navigateur**, en continu. Effet mesuré sur le cluster : CPU du tier API à **212 % de la cible de 70 %**, HPA **2 → 4 → 7 → 10 réplicas (plafond atteint)**, et `workspace-manager` monté à 3. Second défaut lié : le provisionnement échoué **n'affiche aucune erreur** — l'état vide (« Aucune base de données pour le moment » + bouton) disparaît et le panneau reste **totalement blanc**. | ☐ | ☐ | ✅ **12/08** reproduit + attribué live | **Cause racine (code)** : `app/components/database/DatabaseWorkbench.tsx:124-128` — `useEffect(() => { if (fetcher.state === 'idle' && !fetcher.data) fetcher.load(base); }, [fetcher, base])`. La dépendance `fetcher` issue de `useFetcher()` **change d'identité à chaque rendu**, et la garde `!fetcher.data` reste vraie tant que la réponse est vide → `load` → changement d'état → nouveau rendu → `load`… **boucle sans fin**. Le même `!fetcher.data` explique le panneau blanc (rien à rendre). **Attribution par expérience contrôlée (3 mesures de 30 s)** : IDE ouvert au repos → **0 req** ; panneau Base de données **ouvert sans créer** → **0 req** ; **après clic « Créer une base de données »** → **~110 req/30 s** en continu. La charge **persiste panneau refermé** (composant toujours monté) et ne retombe à **0** qu'en **quittant la route IDE** (démontage → cleanup) ; CPU retombe alors de 212 % à 6 % et le HPA revient à 2. **Déclencheur ici** : l'opérateur CNPG est absent de ce cluster de test, donc `POST /projects/:id/database/provision` ne peut pas aboutir — mais **la boucle se déclenchera en production sur n'importe quel échec de provisionnement** (quota, incident CNPG, timeout), ce qui en fait un amplificateur de panne : plus le provisionnement casse, plus les clients matraquent l'API. **Correctif attendu** : garde sur un état `requested`/`loaded` (et non `!fetcher.data`), dépendance stable (`fetcher.load` seul, ou un `useRef`), backoff + plafond de tentatives, et surface d'erreur explicite quand le provisionnement échoue. |
| BUG-QA-DB-IDE-BRICK-001 | **P0 — Un provisionnement de base échoué rend l'IDE du projet DÉFINITIVEMENT inutilisable.** Conséquence directe de `BUG-QA-DB-REFETCH-LOOP-001`, mais bien plus grave que la seule surcharge : après la tentative de création ratée, l'onglet `database` est **persisté côté serveur comme onglet ACTIF** dans `ProjectIdeState`. À **chaque** ouverture ultérieure du projet, l'IDE restaure cet onglet, `DatabaseWorkbench` se monte, la boucle de rechargement infinie démarre et **l'IDE ne finit jamais de monter** : écran « Chargement d'E-Code » indéfiniment. L'état étant **serveur**, le blocage survit au rechargement, à un onglet neuf, et changerait de machine ou de navigateur. **Aucune issue depuis l'interface** : l'IDE ne rendant jamais, l'utilisateur ne peut pas fermer l'onglet fautif — le projet est perdu pour lui. | ☐ | ☐ | ✅ **12/08** reproduit + **preuve causale par patch/dépatch** | **Isolation** : le projet `react-saas-mspwdodf` (provisionnement DB tenté et échoué) reste bloqué sur le splash dans **2 onglets neufs** et après plusieurs minutes, pendant que, **dans le même onglet**, `/dashboard` s'affiche parfaitement et l'IDE d'un **autre** projet du même tenant (`cr-e-une-page-d-accueil`, jamais soumis à un provisionnement DB) monte normalement — le navigateur et la plateforme sont donc hors de cause. Santé serveur vérifiée au même instant : pod workspace `Running` 59 min, API `/health` **200**, route IDE en **200 / 46 043 o / 4 s** en `curl`. **État persisté comparé (table `ProjectIdeState`)** — projet bloqué : `activeTabId: "tab-database-2db5ab6e-…"`, `workspaceTabs: ["editor","git","packages","database"]` ; projet sain : `activeTabId: "tab-preview-default"`, aucun onglet database. **Preuve causale** : après avoir retiré l'onglet `database` et remis `activeTabId="tab-preview-default"` directement en base, **le même projet remonte immédiatement** (« Agent prêt », « 8 fichiers », « Aperçu disponible »). **Remédiation immédiate pour un utilisateur bloqué** : purger l'onglet database de son `ProjectIdeState`. **Correctif de fond** : celui de `BUG-QA-DB-REFETCH-LOOP-001` (garde + backoff), plus un **garde-fou de montage** — un panneau qui échoue ne doit jamais empêcher le shell IDE de rendre (error boundary par onglet), et un onglet en échec ne devrait pas être restauré comme onglet actif. |
| BUG-QA-MONITORING-CRASH-001 | **P2 — Le panneau Supervision plante systématiquement (`ReferenceError: language is not defined`).** Ouvrir `?panel=monitoring` (ou « Supervision » dans la barre latérale) ne rend jamais le contenu : le panneau tombe immédiatement sur sa frontière d'erreur « Ce panneau a rencontré une erreur — L'erreur a été isolée afin que le reste de l'espace de travail puisse continuer à fonctionner » avec les actions « Recharger le panneau » / « Signaler le bug ». Reproduit **100 %** des ouvertures, sur un projet sain, workspace `RUNNING`. La supervision applicative est donc **entièrement inaccessible**. | ☐ | ☐ | ✅ **12/08** reproduit live | Console : `ReferenceError: language is not defined` puis `[panelBoundary] Supervision crashed {componentStack: at RZ (…/assets/vendor-react-CsnH0ZFe.js)}`. Variable `language` référencée sans être déclarée/importée dans l'arbre du panneau (`ProjectMonitoringPanel`, `app/components/chat/BaseChat.tsx:16351`, rendu depuis `:11572-11573`) — le nom minifié `RZ` empêche de pointer la ligne exacte sans sourcemap, mais le défaut se reproduit en une ouverture de panneau. **Point positif à conserver** : la frontière d'erreur par panneau fonctionne et protège le reste de l'IDE — c'est exactement le garde-fou qui manque à `BUG-QA-DB-IDE-BRICK-001` (une boucle de rendu n'étant pas une exception, aucune error boundary ne l'intercepte : il faut une garde de boucle, pas seulement une frontière). |
| BUG-QA-I18N-COUNT-002 | **Le défaut d'espace/pluriel des compteurs est systémique, pas isolé.** Même famille que `BUG-QA-I18N-COUNT-001`, confirmée sur plusieurs surfaces indépendantes : Bibliothèque « **8fichiers** » / « **1fichiers** » ; panneau Compétences « **Projet4** », « **Espace de travail0** », « **Communauté12** ». S'y ajoutent deux variantes : le repli `(s)` non résolu dans Paquets — « **1 fichier(s) de verrouillage détecté(s)** » — et un titre de panneau resté en anglais, « **Secrets** », alors que la navigation affiche « Variables secrètes ». | ☐ | ☐ | ✅ **12/08** reproduit live | Relevés sur `react-saas-mspwdodf` (8 fichiers) et `cr-e-une-page-d-accueil` (1 fichier), build `web:c3636dad2b`. Cause commune : compteur et nom juxtaposés en deux nœuds texte JSX (`{count} files`), la couche i18n normalisant le nœud traduit fait disparaître l'espace de tête et n'applique aucune règle de pluriel. **Correctif attendu** : clés i18n à interpolation avec `count` (`t('files.count', {count})`) sur **toutes** ces surfaces, et traduction du titre du panneau Secrets. |

### Contrôles négatifs — passe 3 (panneaux + responsive, 12/08, build propre rev 7 `82603d55f7` / `web:c3636dad2b`)

| Piste | Verdict | Preuve |
|---|---|---|
| Boucle cœur **modèle** (création → runtime → aperçu) | **Saine** | Workspace `PENDING` t+15 s → `RUNNING` t+30 s ; fichiers synchronisés + `node_modules` installé ; `curl 127.0.0.1:5173` depuis le pod → **200** avec le HTML React/HMR réel. A invalidé `BUG-QA-TPL-NORUNTIME-001`. |
| Panneaux Git / Paquets / Déploiements / Secrets / Compétences | **Sains** | Git : branche `main`, 0 modification, connecteurs proposés. Paquets : 7 dépendances, npm, runtime `remote-kubernetes` Actif. Déploiements : framework `vite` détecté, historique `fe508c7 chore: initial scaffold`. Secrets : état vide + import `.env`. Compétences : 4 projet / 0 espace / 12 communauté, catalogue traduit avec bascules. |
| Frontière d'erreur par panneau | **Saine — et utile** | Le crash Supervision est isolé : message dédié + « Recharger le panneau » / « Signaler le bug », le reste de l'IDE continue de fonctionner. |
| Contraste thème clair **et** sombre | **Sain** | Vignette projet « Aucun aperçu pour le moment » : contraste **9,61** en sombre (`#C2C8CC` sur `#1A2030`) et **9,21** en clair (`#334155` sur `#EEF2F7`) — très au-dessus du seuil WCAG AA de 4,5. Les vignettes « noires et vides » vues en capture étaient une trame non peinte, pas un défaut. |
| Responsive tablette 768 | **Sain** | `/dashboard` et route IDE : `scrollWidth == innerWidth == 768`, **aucun défilement horizontal** ; grille projets sur 2 colonnes, panneau de visite guidée lisible. Un seul élément déborde de 11 px (en-tête `sticky` à marges négatives `-mx-2 sm:-mx-6`) mais il est écrêté par un ancêtre — sans effet visible. |
| Thème piloté par cookie | **Sain (par conception)** | `prefers-color-scheme: light` n'est pas suivi : le thème vient du cookie applicatif `ecode_theme` (défaut `dark`), avec bascule utilisateur dédiée. Comportement voulu, pas un défaut. |
| Blocage IDE sur `?panel=secrets` | **Faux positif** | Attribué d'abord au paramètre d'URL ; en réalité c'était `BUG-QA-DB-IDE-BRICK-001` (le projet était déjà bloqué). `secrets` est bien un nom de panneau valide (`IDE_MANAGEMENT_PANELS`, `BaseChat.tsx:336-361`). |

> **Note de méthode.** 6 « défauts » vus en capture d'écran se sont révélés être des trames non composées du panneau navigateur (compteur « ?fichiers », éditeur Monaco vide, sidebar tronquée, section `/import` écrêtée, deux grands cercles blancs dans l'IDE, vignettes projet vides en sombre). **Systématiquement mesurer dans le DOM avant de conclure** : sur cette passe, la capture d'écran seule aurait produit 6 faux positifs sur 6.
| BUG-QA-PROVIDERS-SSR-ENV-001 | **`/api/configured-providers` déclare TOUS les fournisseurs LLM non configurés alors qu'ils le sont** — 0 / 22 « configuré », y compris Anthropic, pendant que l'agent génère réellement avec cette même clé. Le loader lit `process.env[apiTokenEnvVar]` (`app/routes/api.configured-providers.ts:82`), or dans le bundle SSR `process.env` est **shimmé à `{}`** par `vite-plugin-node-polyfills` — piège déjà documenté dans le dépôt (`app/lib/.server/require-session.ts:14-21` et `ai-usage.ts` lisent explicitement `globalThis.process.env` pour cette raison). Le repli `context.cloudflare.env` est vide hors Cloudflare, et `llmManager.env` en dérive. Conséquence produit : l'UI Connexions/Réglages annonce à l'utilisateur qu'aucun fournisseur n'est disponible, et toute logique d'auto-activation basée sur cet endpoint reste morte, même quand la plateforme a bien ses clés. | ☐ | ☐ | ✅ **12/08** prouvé par contradiction | **Preuve directe et contradictoire, même instant, même build** : (1) la clé est présente dans les pods — `web` et `api` exposent `ANTHROPIC_API_KEY` (longueur 108, préfixe `sk-ant-…`) ; (2) `POST /api/chat` répond **HTTP 200** avec une génération Anthropic réelle en streaming ; (3) `GET /api/configured-providers` répond au même moment `{"name":"Anthropic","isConfigured":false,"configMethod":"none"}` et **0 configuré sur 22**. Le même `process.env` shimmé explique les trois fournisseurs présents en secret (Anthropic, OpenAI, Google) tous rapportés absents. **Correctif attendu** : lire `globalThis.process.env` comme le font déjà `require-session.ts` et `ai-usage.ts`, avec un test de non-régression verrouillant le cas SSR. |
| BUG-QA-STREAM-CHOPPY-001 | **Streaming agent saccadé : le texte arrive par blocs de ~110 caractères toutes les ~700 ms, pas en flux continu.** Le lissage côté client ne peut rien y faire — `Chat.client.tsx:677` fixe `experimental_throttle: 40` (~25 fps) avec le commentaire « keeps streaming smooth », mais un *throttle* ne fait que **limiter** les rendus : il ne peut pas **fabriquer** d'images intermédiaires. Entre deux chunks espacés de 700 ms, l'UI n'a rien de nouveau à peindre, donc le texte apparaît par paliers au lieu de « se taper ». Le transform `smoothStream()` du SDK `ai` — précisément prévu pour découper les chunks en mots/caractères et les cadencer — **n'est utilisé nulle part** dans le dépôt, alors qu'il est disponible dans la version installée. | ☐ | ☐ | ✅ **12/08** mesuré live | **Mesure horodatée des chunks `0:` sur une réponse de ~1,5 Ko** — via l'ingress public : TTFB **1,95 s**, **14 chunks** / 1 515 caractères en **9,36 s**, écart **médian 695 ms**, p90 773 ms, max 785 ms, **0 écart > 1 s** (soit ≈1,4 mise à jour par seconde pour un client capable de 25 fps). **nginx est disculpé** : le même appel exécuté **depuis l'intérieur du pod web** (`127.0.0.1:3000`, ingress totalement contourné) donne la **même cadence** — 14 chunks, écart médian **704 ms** — donc ni la bufferisation proxy ni l'absence d'en-tête `X-Accel-Buffering` ne sont en cause ; le découpage vient du flux fournisseur/SDK en amont. **Correctif attendu** : ajouter `experimental_transform: smoothStream({ chunking: 'word' })` au `streamText` du serveur (`app/lib/.server/llm/stream-text.ts`) pour redistribuer les blocs en mots cadencés — le `throttle: 40` du client devient alors réellement utile. |
| BUG-QA-AGENT-TRUNCATION-002 | **P1 (requalifié le 12/08 — corruption TRANSITOIRE, pas permanente) — Un fichier généré par l'agent est écrit ET committé tronqué pendant plusieurs minutes : le projet est syntaxiquement invalide, l'aperçu casse, et l'agent annonce malgré tout un succès complet.** Prompt simple (« remplace src/App.tsx par un compteur de clics avec Incrémenter et Reset ») via l'UI réelle : l'agent génère, l'ActionRunner écrit, et le fichier écrit **s'arrête net sur `disab`** (au lieu de `disabled`). Résultat : JSX non fermé, accolades déséquilibrées (**7 `{` pour 6 `}`**), aucune fermeture de `export default`. Vite renvoie **HTTP 500** sur `/src/App.tsx` (`Unexpected token (36:0)`, babel-parser) et l'app ne s'affiche plus. **Le contenu tronqué n'est pas seulement dans le runtime : il est committé dans le stockage canonique du projet** — donc c'est le fichier de l'utilisateur qui est corrompu, pas un cache. Aucune erreur, aucun avertissement, aucun état « incomplet » n'est surfacé : côté UI la génération se présente comme terminée. | ☐ | ☐ | ✅ **12/08** reproduit live, clé LLM réelle | **Runtime** (`/workspace/src/App.tsx`, pod `workspace-ws-090fb06e5a098932`) : 960 o ; `curl 127.0.0.1:5173/src/App.tsx` → **500** avec `{"message":"/workspace/src/App.tsx: Unexpected token (36:0)\n  34 | onClick={reset}\n  35 | disab\n> 36 |"}`. **Stockage canonique** (export ZIP du projet, décodé) : `src/App.tsx` = **959 octets**, 220 derniers caractères se terminant littéralement par `onClick={reset}\n            disab\n` — `'disabled' in contenu` = **False**, `{`=7 vs `}`=6, ne se termine ni par `}` ni par `;`. Les deux copies sont donc identiquement tronquées. **Famille connue mais manifestation NOUVELLE** : `BUG-AGENT-EDIT-TRUNCATION` (corrigé par `0e0017ae`, `withoutTrailingCloseTagPrefix()`) ne traitait que le cas d'une **balise `</boltAction>` partielle** en fin de flux ; ici il n'y a **aucun fragment de balise** — le contenu lui-même est coupé, donc la garde existante ne s'applique pas. **Piste** : flux interrompu (plafond de tokens / arrêt du modèle) alors que le compte est en `plan:free` (`messagesRemaining:50`, `byok.allowed:false`) — mais quelle qu'en soit la cause amont, le défaut est que **du contenu incomplet est écrit et committé sans validation**. **AGGRAVANT — l'agent annonce un SUCCES COMPLET sur un projet cassé** : en-tête « **Agent · Terminé · 100 %** », les 4 actions toutes vertes (`Créer src/App.tsx — Terminé — 762 ms`, `src/App.css — 5,5 s`, `src/main.tsx — 58 ms`, `Démarrer l'application — 26 ms`), message final « **C'est prêt ✅** » — et le résumé revendique explicitement « *Bouton Reset : … est désactivé quand le compteur est déjà à 0 (**état `disabled` géré**)* », c'est-à-dire **exactement l'attribut sur lequel le fichier est coupé**. Au même moment et dans le même écran, trois signaux contredisent ce succès : le **Plan affiche `0/2 terminées`**, les **agents parallèles Frontend et QA ont tous deux échoué** (« Cet agent spécialisé n'a pas pu terminer sa tâche ») avec **`consensus : Rejeté · quorum · 0 % d'accord`**, et la barre d'état compte **`Problèmes ⊗4 ⚠4`**. Les diagnostics de la plateforme voient donc bien la casse — c'est **le statut de complétion de l'agent qui les ignore**. Le sous-agent QA, dont la tâche était littéralement « vérifier que le composant s'affiche et que les boutons marchent », a échoué sans rien escalader. **LOCALISATION DECISIVE — le serveur envoie du contenu COMPLET, c'est l'ECRITURE CLIENT qui tronque.** En rejouant le même appel `POST /api/chat` en `curl` et en reconstituant le flux, l'action `type="file" filePath="src/App.tsx"` contient **1 292 octets valides** : `'disabled' in contenu` = **True**, accolades **équilibrées 8/8**, balise **`</boltAction>` bien reçue**, fin propre sur `export default App;`. Or le fichier écrit sur disque et committé ne fait que **959 octets**, coupé à `disab`. Le modèle, le fournisseur et le transport sont donc **hors de cause** : le défaut est dans le chemin de parsing/écriture **côté navigateur** (`StreamingMessageParser` / ActionRunner). **Reproductible 2/2 — mais le système CONVERGE finalement (correction de ma première conclusion).** Relancé avec « L'aperçu est cassé : src/App.tsx ne compile pas. Diagnostique et corrige. », l'agent réécrit le fichier dans un état **encore plus court — 278 octets**, coupé en pleine chaîne (`<section className="coun`), Vite passant de `Unexpected token (36:0)` à `Unterminated string constant. (11:25)` ; cet état tronqué est resté **stable pendant les 105 s** de surveillance (7 mesures). **J'en avais conclu à tort que le self-repair aggravait définitivement.** Une vérification ultérieure montre la convergence vers un fichier **valide** : runtime **1 258 o** et stockage canonique **1 255 o**, `disabled` présent, accolades **équilibrées 10/10**, fin propre, **Vite 200**, panneau Problèmes revenu à **0 erreur / 0 avertissement**. La séquence complète est donc **1 292 attendus → 959 (tronqué, committé) → 278 (tronqué, ~105 s) → 1 255 (valide)**. **Ce qui reste solide et sérieux** : (a) du contenu tronqué est réellement **écrit ET committé dans le stockage canonique** — vérifié par export ZIP à 959 o — donc un export, un `Publier` ou un clone effectué pendant cette fenêtre embarque un fichier cassé ; (b) l'aperçu est **500 pendant plusieurs minutes** ; (c) l'agent affiche « Terminé 100 % / C'est prêt ✅ » tout du long. Ce n'est **pas** une perte de données permanente. Les longueurs successives ne correspondent à aucun plafond fixe, ce qui oriente vers une **écriture depuis un tampon partiel** dont un flush ultérieur finit par rétablir le contenu complet. **Distinct de `BUG-AGENT-EDIT-TRUNCATION`** (corrigé par `0e0017ae`) : là-bas la balise fermante arrivait **partielle** (`</bo`) et la garde `withoutTrailingCloseTagPrefix()` la neutralisait ; ici la balise fermante **arrive entière** et le contenu est quand même coupé bien avant la fin — la garde existante ne couvre pas ce cas. **Correctif attendu** : (1) n'écrire le fichier qu'à la **fermeture d'action** à partir du contenu accumulé complet, jamais depuis un tampon intermédiaire ; (2) refuser d'écrire/committer un fichier dont la syntaxe ne parse pas quand l'extension est connue, et surfacer « génération interrompue » ; (3) **interdire l'état « Terminé 100 % / C'est prêt ✅ » quand le plan est à 0/N, que le consensus est Rejeté ou que le compteur Problèmes est non nul** — le succès affiché doit être dérivé de ces signaux ; (4) ajouter un test de non-régression qui compare **octet à octet** le contenu du flux serveur et le contenu écrit. |
| BUG-QA-DEPLOY-BASEPATH-404-001 | **✅ RÉSOLU — corrigé sur `main` le 13/08 (`949f0748`), certifié en prod le 20/08 (voir « CERTIFICATION PROD » en fin de preuve).** *Énoncé d’origine :* **P1 — DÉTERMINISTE (2/2) : TOUT déploiement statique marqué `READY` sert une PAGE BLANCHE : tous ses assets renvoient 404 à cause d'un décalage de base path.** Le déploiement se crée et se termine en statut **`READY`**, l'URL publique est présentée à l'utilisateur comme en ligne et le document HTML répond bien **200**. Mais l'`index.html` généré référence ses assets sous le préfixe **`/static-deployments/<deploymentId>/assets/…`** alors que le serveur statique les expose **à la racine** (`/assets/…`). Chaque `<script>`/`<link>` part donc en **404 `STATIC_DEPLOY_FILE_NOT_FOUND`** : le bundle ne se charge jamais, `#root` reste vide, l'utilisateur voit une page blanche. Aucun signal d'erreur : ni le statut du déploiement, ni l'UI ne mentionnent quoi que ce soit. | ✅ | ✅ **13/08 `949f0748`** | ✅ **20/08 certifié en prod** | Déploiement `cmsq9g5s1005x0ndf5in0rbgw` (provider `static`, statut **READY**) sur `https://s-cmsq9g5s1005x0ndf5in0rbgw.preview.34.163.208.161.sslip.io/`. **Document** : `GET /` → **200**, 485 o, `<title>React SaaS</title>`, `<div id="root">`, `<script type="module" crossorigin src="/static-deployments/cmsq9g5s1005x0ndf5in0rbgw/assets/index-tSVnAJOB.js">`. **Assets référencés** : `…/static-deployments/<id>/assets/index-tSVnAJOB.js` → **404** `{"error":"File not found in deployment","code":"STATIC_DEPLOY_FILE_NOT_FOUND"}` ; idem pour le CSS `index-BEH7rByq.css` → **404**. **Preuve du décalage** : les mêmes fichiers répondent **200 à la racine** — `GET /assets/index-tSVnAJOB.js` → **200**, `GET /index.html` → **200** — tandis que `GET /static-deployments/<id>/index.html` → **404**. Les artefacts sont donc bien présents et servis ; **seul le préfixe injecté dans le HTML est faux**. **Piste** : `base` de build positionné à `/static-deployments/<deploymentId>/` alors que l'hôte de déploiement sert depuis `/` (ou préfixe retiré côté proxy pour le document mais pas pour les assets). **Confirmé 2/2 — le défaut est systématique, pas accidentel** : second déploiement `cmsqgpz6i00040nf4vrck0051` (statut **READY**), document `GET /` → **200 / 485 o**, et **ses deux** assets référencés en 404 — `src="/static-deployments/cmsqgpz6i00040nf4vrck0051/assets/index-tSVnAJOB.js"` → **404** et `href="…/assets/index-BEH7rByq.css"` → **404** — alors que les mêmes fichiers répondent **200 à la racine** (`/assets/index-tSVnAJOB.js`, `/assets/index-BEH7rByq.css`). Le préfixe fautif reprend à chaque fois l'id du déploiement courant, donc **aucun déploiement statique ne peut s'afficher**. **IDENTIFIÉ COMME BUG-DEPLOY-LIVE — correctif `57234a22` existant mais NON MERGÉ.** Le commit `57234a22` (« fix(deploy): ne plus prefixer le HTML quand le deploiement a une origine dediee ») décrit **exactement** ce constat, reproduit indépendamment sur le déploiement `cmspyoxst000l0na32v10bj3n` — donc **deux reproductions indépendantes**, la mienne ajoutant le caractère **déterministe (2/2)**. Il vit sur la branche `fix/deploy-static-base-path` et **n'est PAS ancêtre de `origin/main`**. Les images tournant sur l'env d'audit (`api:82603d55f7`, `web:c3636dad2b`) **ne le contiennent pas** — ce qui explique mes 404. **Cause racine (code)** : `rewriteHtmlAbsoluteUrls(original, '/static-deployments/<id>/')` était appliqué **inconditionnellement**, alors que ce préfixe n'a de sens qu'en mode *legacy* par chemin ; avec une **origine dédiée** (`PREVIEW_DOMAIN` défini) le snapshot est servi **à la racine** de `s-<id>.preview.<domaine>`, donc le chemin réécrit est cherché **dans** le snapshot et 404 — exactement ma mesure (assets **200 à la racine**, **404 sous le préfixe**). Correctif : `staticDeployDedicatedOrigin(deploymentId) ? original : rewriteHtmlAbsoluteUrls(...)`. **Effet du correctif VÉRIFIÉ en isolement (12/08)** — le commit embarque `services/api/src/static-deploy-base-path.spec.ts` (112 lignes, 3 cas). Exécuté sur un worktree détaché à `57234a22` : **3/3 verts**. **Contrôle négatif** : le même spec rejoué contre le code **actuel non corrigé** → **1 échec / 2 passés**, l'assertion en échec étant `expect(html).toContain('src="/assets/index-CZ8Vosv_.js"')` avec pour sortie réelle `src="/static-deployments/cmspyoxst000l0na32v10bj3n/assets/index-CZ8Vosv_.js"` — **le préfixe fautif exact**. Le spec échoue donc sans le correctif et passe avec : l'effet est prouvé au niveau unitaire. Le spec couvre aussi la **non-régression du mode legacy** (préfixe conservé quand aucune origine dédiée n'existe) et la copie de l'arbre d'assets. **✅ CORRECTIF VALIDÉ EN RÉEL (12/08) — le 404 disparaît.** Image `api` construite depuis un worktree détaché sur `57234a22` (Cloud Build `b656cfa2`, **SUCCESS**, 6 min 29 s, tag `fix57234a`), déployée sur l'env d'audit par `kubectl set image` (rollout OK, image confirmée sur le Deployment). Nouveau déploiement statique `cmsqoay7u00060n91guhztq2b` → **READY**. **Résultat AVANT / APRÈS sur le même env, même projet** : `src="/static-deployments/<id>/assets/index-tSVnAJOB.js"` → **404** devient `src="/assets/index-tSVnAJOB.js"` → **200** ; le CSS passe de **404** à **200** ; le nombre d'occurrences de `static-deployments` dans le HTML tombe de **2 à 0**. Les deux assets référencés se chargent donc désormais, ce qui élimine la page blanche. **Conclusion : `57234a22` corrige effectivement le défaut, prouvé au niveau unitaire ET en déploiement réel. Il ne reste qu'à le MERGER.** **⚠️ CORRECTION DU CONSTAT « NON MERGÉ » (20/08).** L’affirmation ci-dessus (« `57234a22` … n’est PAS ancêtre de `origin/main` ») était exacte **au moment de la mesure du 12/08** mais a été **périmée dès le lendemain** : le correctif a été reporté sur `main` en **`949f0748`** (date de commit **2026-08-13 13:15 +0300**), copie **rebasée** de `57234a22`. Vérification : `services/api/src/static-deploy-base-path.spec.ts` est **byte-identique** entre `57234a22` et `origin/main` ; `services/api/src/deployments.ts` ne diffère que par un changement **postérieur et sans rapport** (honnêteté de readiness du provider `static`, 17/08). La branche `fix/deploy-static-base-path` est donc **superflue** (1 commit en avance, 369 en retard) et **aucune PR n’était à ouvrir** : il n’y avait ni merge ni déploiement à faire. L’image `api` que testait l’env d’audit (`82603d55f7`) est bien **antérieure** à `949f0748` — ce qui explique les 404 mesurés le 12/08. **✅ CERTIFICATION PROD (20/08) — le défaut ne se reproduit plus.** Prod tourne `api:93ed3c708f` (19/08), qui **contient** `949f0748` (299 commits après) ; `PREVIEW_DOMAIN=preview.e-code.ai` est bien défini dans le ConfigMap prod, donc `staticDeployDedicatedOrigin()` renvoie une origine dédiée et la branche « pas de réécriture » du correctif s’applique. **Déploiement statique réel créé de bout en bout** (compte jetable `qa-basepath-1787226945@local.test`, jamais un compte prod) : projet blank → workspace `ws-66ecbbb21333f833` → écriture des 7 fichiers du starter Vite → `POST /projects/…/deployments` → build réel en pod (`vite v5.4.21`, `✓ 31 modules transformed`, `✓ built in 2.32s`) → déploiement **`cmt1gv371003x0ndiyp2vv45v`** → **`READY`** sur `https://s-cmt1gv371003x0ndiyp2vv45v.preview.e-code.ai/`. **Mesures** : `GET /` → **200**, 401 o, `text/html`. HTML servi : `<script type="module" crossorigin src="/assets/index-BTe6Mnad.js">` et `<link rel="stylesheet" crossorigin href="/assets/index-BzjJVwsc.css">` — **assets référencés À LA RACINE**, et le compte d’occurrences de `static-deployments` dans le HTML est **0** (contre 2 le 12/08). **Les deux assets répondent 200** : `/assets/index-BTe6Mnad.js` → **200**, 142 850 o, `application/javascript` ; `/assets/index-BzjJVwsc.css` → **200**, 770 o, `text/css`. **Contrôle négatif** : l’ancien chemin préfixé `/static-deployments/cmt1gv371003x0ndiyp2vv45v/assets/index-BTe6Mnad.js` → **404** — le préfixe serait donc toujours fatal, ce qui prouve que c’est bien sa **suppression** qui répare le déploiement. **Plus de page blanche (rendu réel)** : page chargée en navigateur headless → `#root` contient **1 enfant** et le texte « E-CODE PROJECT / QA BasePath Cert / Start building your app with the E-Code agent. », `document.title = "QA BasePath Cert"`, **0 erreur console**, capture d’écran conforme. **Avant / après** : `static-deployments` dans le HTML **2 → 0** ; assets référencés **404 → 200 (2/2)** ; `#root` **vide → rendu**. *Note de reproductibilité* : les snapshots des anciens déploiements prod (≤ 01/08) ont été purgés (`GET /` → 404), donc l’« avant » n’était plus mesurable en direct — il reste établi par les mesures du 12/08 (env d’audit) et par le contrôle négatif ci-dessus. La réécriture ayant lieu **au moment du snapshot**, seuls les déploiements construits après le 13/08 sont corrects — les anciens le resteraient s’ils survivaient. Compte + org + projet de QA supprimés après mesure. |

### Contrôles négatifs — passe 5 (12/08, cluster stable rev 7)

| Piste | Verdict | Preuve |
|---|---|---|
| **Historique de fichier / Chronologie** | **Sain** | Le panneau liste les fichiers avec leur **état Git réel** : `App.css — untracked` (créé par l'agent), `main.tsx — modified`, `vite.config.ts — modified` — cohérent avec l'état effectif du dépôt. |
| **Responsive desktop 1440** | **Sain** | `documentElement.scrollWidth == innerWidth == 1440`, **0 élément** dépassant la fenêtre ; disposition complète (agent + onglets + éditeur + arborescence + terminal) correctement agencée. |
| **Terminal — connexion** | **Sain** | Après « Connexion à l'espace de travail… », prompt réel du pod `/workspace $`. Le chemin d'exécution de commandes dans le pod est par ailleurs prouvé (action agent « Démarrer l'application » → Vite tourne). **Saisie interactive NON vérifiable** avec l'outillage : même avec `.xterm-helper-textarea` focalisée (`document.activeElement` confirmé), les frappes synthétiques n'atteignent pas xterm — **non retenu comme défaut**. |
| **Self-repair de l'agent** | **Sain, et diagnostic exact** | L'agent a identifié lui-même la cause : « *`src/App.tsx` était coupé à `disab`, laissant le JSX et la fonction non fermés → erreur de parsing/compilation TypeScript* », puis a réécrit le composant avec `disabled={count === 0}` et toutes les balises fermées. Convergence vérifiée : 1 255 o valides, Vite 200, Problèmes 0. |
| **Intégrité après GC de workspace** | **Saine** | Le pod workspace a été recyclé puis recréé pendant la passe ; tous les fichiers ont survécu via le PVC (`App.tsx` 1 258 o intact, arborescence complète). |
| **Modes agent Ask / Plan** | **Sains** | Plan-first : plan structuré (`planned:true`, `needsApproval:true`, 2 tâches avec `roleId`) ; approbation → exécution réelle et génération d'artefacts. `chatMode: discuss` répond en conseil sans écrire de fichier. |

### Passe 6 — confirmations et limites d'outillage (12/08)

| Piste | Verdict | Preuve |
|---|---|---|
| **Déploiement statique — base path** | **P1 CONFIRMÉ 2/2, déterministe** | 2e déploiement `cmsqgpz6i00040nf4vrck0051` **READY** : document 200, **ses 2 assets en 404** sous `/static-deployments/<id>/assets/…`, et **200 à la racine** (`/assets/index-tSVnAJOB.js`, `/assets/index-BEH7rByq.css`). Le préfixe fautif reprend l'id du déploiement courant → **aucun déploiement statique ne peut s'afficher**. |
| **Terminal — saisie interactive** | **Non vérifiable (limite d'outillage), NON retenu comme défaut** | Connexion prouvée (prompt `/workspace $` du pod). Frappes synthétiques sans effet malgré `.xterm-helper-textarea` focalisée. Tentative de contournement par le chemin serveur `POST /commands/run` : **401 `WORKSPACE_AGENT_UNAUTHORIZED`** (jeton d'agent requis, non forgé volontairement). Le chemin d'exécution de commandes reste **prouvé par le produit lui-même** : l'action agent « Démarrer l'application » a lancé `npm run dev` et Vite sert l'app. |
| **Auth du workspace-agent** | **Saine — fail-closed** | Appel `POST /commands/run` sans jeton valide, **depuis l'intérieur même du pod** : rejeté en **401**. Aucun accès implicite par localhost. |

> **Bilan de couverture.** Tous les panneaux du périmètre ont été passés : éditeur, arborescence, aperçu, terminal (connexion), console/journaux, déploiements, base de données, chat agent (streaming, Ask/Plan, self-repair), problèmes, historique de fichier, compétences, Git, paquets, secrets, supervision. Responsive validé en 375 / 768 / 1440, thèmes clair et sombre. Restent **BLOCKED-clé** : facturation (Stripe), comptes connectés (OAuth), invitations (Resend).

### Passe 7 — parcours user-area exercés fonctionnellement (12/08)

Jusqu'ici ces routes n'avaient été vérifiées qu'en code HTTP ; cette passe exécute de **vraies mutations**.

| Parcours | Verdict | Preuve |
|---|---|---|
| **Clés API — cycle complet** | **Sain de bout en bout** | Création via l'action réelle (`intent=create`, `scope.read=on`, `expiresInDays=30`) → **200** ; persistée et relue : `id=cmsqivgab00050nf4yh1uasq0`, `name=qa-sweep-key`, `keyPrefix=vck_********` (valeur masquée — clé d'un env de test détruit), `scopes=["read"]`, `expiresAt=2026-09-11`. Le jeton émis **authentifie réellement** sur l'API : `GET /api/keys` avec `Authorization: Bearer vck_…` → **200**, et `lastUsedAt` passe à `2026-08-12T20:09:27Z` — l'usage est donc tracé. |
| **Clés API — hygiène du secret** | **Saine** | Le jeton complet n'est renvoyé **qu'une fois**, à la création ; les relectures ultérieures n'exposent que `keyPrefix`. |
| **Clés API — application des scopes** | **Saine, fail-closed** | Avec une clé **read-only** : `POST /api/keys` (tentative d'émettre une clé `admin` → escalade de privilèges) → **403 `API_KEY_SCOPE_INSUFFICIENT`** « API key is missing the 'write' scope » ; `DELETE /api/keys/<id>` → **403** identique. Aucune escalade possible depuis une clé de lecture. |
| **Validation du formulaire de clé** | **Saine** | Création sans scope → **400** « Select at least one scope. » ; sans nom → **400** « Give the key a name. » — rejets côté serveur, pas seulement côté UI. |
| BUG-QA-PANEL-429-MASKED-001 | **Une limitation de débit (429) sur une écriture est présentée comme un échec de CHARGEMENT.** Créer un instantané manuel après une salve d'auto-checkpoints renvoie **HTTP 429**, mais l'utilisateur lit « **The panel data could not be loaded. Please try again.** » avec le code `PANEL_REQUEST_FAILED` — alors qu'il s'agit d'une **écriture refusée pour quota**, pas d'un chargement raté. Rien n'indique qu'une limite a été atteinte, ni laquelle, ni quand réessayer : « Please try again » invite précisément à refaire l'appel qui vient d'être limité. | ☐ | ☐ | ✅ **12/08** reproduit live | Projet `cmspwdoh0003j0nfz0v4yomp8`. `POST /api/projects/:id/ide-panel/snapshots {"label":"QA sweep checkpoint"}` → **429** + `{"code":"PANEL_REQUEST_FAILED","error":"The panel data could not be loaded. Please try again."}` ; rejoué → **429** identique. **Le défaut est borné, pas systémique** — la même BFF sait être précise ailleurs : `{"intent":"restore","snapshotId":""}` → **400 `SNAPSHOT_REQUIRED`** « Select a snapshot to restore. » (message juste), et un id inconnu → **404** « The requested panel data was not found. » (acceptable). Seul le **429 tombe dans le seau générique de chargement**. **Contexte** : la limite est atteinte légitimement car l'agent avait déjà créé **5 instantanés automatiques** (« Patch de l'IA accepté pour src/App.tsx / src/App.css », `kind=manual`) — l'auto-checkpoint sur édition d'agent fonctionne bien, c'est seulement sa restitution d'erreur qui trompe. **Correctif attendu** : mapper 429 sur un message dédié (limite atteinte + délai de réessai, en s'appuyant sur `Retry-After`), au lieu du repli « could not be loaded ». |
| BUG-QA-GIT-ROUTE-SLOW-001 | **`/git` reste visuellement vide 7 à 18 s, puis affiche le chat générique au lieu d'une UI d'import Git.** La route `app/routes/git.tsx` est un `ClientOnly` : le SSR ne renvoie que la coquille « Chargement d'E-Code » (14 caractères de texte visible). Le contenu n'apparaît qu'après hydratation complète — mesuré **8,3 s / 18,0 s / 7,3 s** sur trois chargements anonymes en prod, contre **3,3–5,1 s** pour `/` et **4,0 s** pour `/login` : `/git` est 2 à 4× plus lent que les autres routes de la même app. Pendant tout ce temps l'écran est blanc, sans squelette ni indicateur de progression. Ce qui finit par s'afficher est le **fallback `BaseChat`** (accueil « Transformez vos idées en logiciels fonctionnels »), **sans aucun champ d'URL de dépôt** : `GitUrlImport` ne monte jamais son formulaire, car sans `?url=` son `useEffect` sort immédiatement (et, si `historyReady && gitReady`, fait un `window.location.href = '/'` — redirection **client** au lieu d'une redirection serveur, donc un second chargement complet de l'app). La frame principale navigue **3 fois** sur `/git` avant de se stabiliser. ⚠️ Ne pas confondre avec une page morte : une mesure prise à 2,5 s conclut à tort « page vide ». | ☐ | ☐ | ✅ **26/08** reproduit live prod | **Repro** (anonyme, prod) : `node /tmp/qa-sweep/git-timing.mjs` — Playwright ouvre `https://app.e-code.ai/git` en `waitUntil:'commit'` puis sonde `document.body.innerText.length` toutes les 250 ms. Résultats : `/git` firstContent **8303 ms** puis **17971 ms** ; `/` **3309/5082 ms** ; `/login` **4006/4110 ms**. Contrôle SSR : `curl -s https://app.e-code.ai/git` → 200, 26 516 octets, mais **14 caractères** de texte visible (« Loading E-Code »). Contrôle UI : après rendu, `input[placeholder*=repo|git|url]` → **absent** ; seuls une `textarea` « How can E-Code help you today? » et 2 `input[type=file]` existent. Navigations frame principale : `["/git","/git","/git"]`. Fichiers suspects : `app/routes/git.tsx:25` (`ClientOnly fallback={<BaseChat />}`), `app/components/git/GitUrlImport.client.tsx:119-137` (`useEffect` → `window.location.href = '/'`). Capture : `/tmp/qa-sweep/git-final.png`. 🚫 **CHIFFRES DE TEMPS RETIRÉS (27/08).** Toutes les mesures en millisecondes de cette entrée sont **invalidées** et ne doivent être ni citées ni utilisées comme budget de performance. Motif : le poste de mesure était sous charge non maîtrisée — `load average` relevé à **26,26**, dominé par des processus macOS sans rapport (`MediaAnalysis` 79 % CPU, `SkyLight` 44 %), plus un upload Cloud Build de 342 Mo pendant la première série. Contre-preuve directe : un contrôle serveur `curl` sur la prod rend `www` et `app` en **0,4 à 1,8 s**, alors que le même chargement en Chrome headless local variait de **12 à 38 s** et échouait parfois au-delà de 110 s — l'écart est donc **côté client de mesure**, pas côté produit. Une reprise contrôlée (3 runs × 2 viewports) a donné `/` en échec 6 fois sur 6 puis `/pricing` entre 14,8 s et 38,1 s : **non reproductible, donc inexploitable**. **Ce qui reste établi et indépendant de la charge** : (a) le SSR ne rend que 14 caractères de texte visible (`curl` seul, aucun navigateur) ; (b) après rendu, **aucun champ d'URL de dépôt** n'existe (`input[placeholder*=repo|git|url]` absent) — seuls une `textarea` de chat et 2 `input[type=file]` ; (c) la frame principale enregistre **3 navigations** sur `/git` ; (d) le `window.location.href = '/'` de `GitUrlImport.client.tsx` est une redirection **client** là où une redirection serveur suffirait. **À faire** : rechronométrer sur un poste au repos, ou mieux sur l'environnement de test, avant de qualifier la lenteur. La sévérité de cette entrée est donc **à confirmer** — le défaut structurel est réel, son impact temporel n'est pas mesuré. **Portée du défaut — bornée, pas systémique** : `git grep "window.location.href = '/"` sur `origin/main` ne rend que **4 occurrences**. Trois sont dans `GitUrlImport.client.tsx` (le défaut décrit ici) ; la quatrième, `app/routes/projects.$projectId.ide.tsx:916`, est une navigation **consécutive à une action** (suppression / duplication / fork / renommage) où le rechargement complet est un choix défendable pour repartir d'un état propre. Aucune autre route ne redirige côté client au premier rendu. Le correctif se limite donc à `GitUrlImport`. |
| BUG-QA-PANEL-AGENT-FALLBACK-001 | **Un `?panel=` non reconnu est avalé en silence : sur mobile il rend le panneau Déploiements, sur desktop il laisse l'onglet précédent — dans les deux cas l'URL ment sur ce qui est affiché.** Deux défauts se combinent. **(1) Allow-list trop étroite** : `readPanelSearchParam(searchParams, IDE_URL_PANELS)` rejette toute clé absente de `IDE_URL_PANELS` et renvoie `undefined` — sans avertir, sans corriger l'URL. Or **`agent` n'est PAS dans `IDE_URL_PANELS`** alors qu'il est l'un des **3 onglets mobiles par défaut** (`ECODE_MOBILE_DEFAULT_TABS = ['preview','agent','deployments']`). **(2) Repli muet sur `deployments`** : `activeMobileServicePanel` fait `isIdeManagementPanel(activeProjectPanel) ? activeProjectPanel : 'deployments'`. Résultat : **25 des 50 clés** de `ECODE_MOBILE_TAB_META` ne sont pas des management panels et retombent donc toutes sur Déploiements — `preview, agent, deploy, files, editor, search, locks, actions, assistant, publishing, app-storage, auth, console, debug, developer, history, multiplayer, collaboration, collaborate, checkpoints, shell, kv-store, storage, web, tools`. **(3) Titre et contenu décorrélés** : le panneau reçoit `panel={activeMobileServicePanel}` (issu de l'URL) mais `displayTitle={mobileServiceHeaderTab?.name}` (issu de `activeMobileOpenTabId`) — l'en-tête peut donc afficher « Agent » au-dessus du contenu Déploiements, ce qui est exactement le symptôme observé. ⚠️ **Le tap sur l'onglet n'est PAS affecté** : `activateMobileTool('agent')` fait `setProjectPanelSearchParam()` (efface le param) et bascule sur `chat`. Le défaut ne se déclenche que par **URL directe** (deep-link, lien partagé, rechargement, restauration d'onglet). | ☐ | ☐ | ✅ **26/08** desktop reproduit live prod ; mobile 390 px **confirmé par Avi**, non rejoué par moi (voir réserve) | **Repro desktop (prod, projet `@org-dzj1djmz/create-a-polished-portfolio-with-5`)** : ouvrir `…?panel=agent` → onglet affiché = **« Base de données »** (dernier onglet restauré), l'URL indique toujours `panel=agent`. Idem `?panel=zzz-nope` → **« README.md »**. Aucun message, aucune correction d'URL. **Contrôle de non-régression** : les **29 clés valides ont toutes été rejouées en navigation réelle et mappent correctement** — `overview→Vue d'ensemble, studio→Studio de l'agent, problems→Problèmes, database→Base de données, object-storage→Stockage d'objets, packages→Paquets, skills→Compétences, monitoring→Supervision, ports→Ports, extensions→Extensions, integrations→Intégrations, workflows→Flux de travail, debugger→Débogueur, deployments→Déploiements, security→Sécurité, env→Variables d'environnement, secrets→Secrets, git→Git, activity→Activité, terminal→Shell, logs→Journaux, collaborators→Collaborateurs, domains→Domaines, snapshots→Instantanés, settings→Paramètres, editor→README.md, preview→Webview, files→arbre de fichiers (panneau latéral, comportement normal)`. **Le défaut est donc borné aux clés hors allow-list**, pas au mapping lui-même. ✅ **RÉSERVE LEVÉE — preuve mobile 390 px obtenue le 27/08** sur l'environnement de test d'audit déployé au SHA `040dd2976d` de `main` (release `vibecore` rév. 21). Chrome réel piloté par Playwright, viewport **390×844**, UA iPhone, `deviceScaleFactor` 3, session authentifiée, projet `cmtbdqetq00040nh6bw7zbkcg`. Lecture de `[data-testid="ide-service-panel"]@data-panel` (le composant réellement monté) et de `[data-testid="mobile-ide-header"]` (le titre affiché) : `?panel=deployments` → `data-panel=deployments`, en-tête « Déploiements » ✅ ; **`?panel=agent` → `data-panel=deployments`, en-tête « Agent »** ❌ ; **`?panel=web` → `data-panel=deployments`, en-tête « Agent »** ❌ ; **`?panel=zzz-inconnu` → `data-panel=deployments`, en-tête « Agent »** ❌ (l'en-tête reste même figé sur la valeur précédente : les **deux** sont faux). Contrôle négatif : `?panel=editor` → `data-mobile-panel=editor`, en-tête « Éditeur » ; `?panel=preview` → `data-mobile-panel=preview`, en-tête « Webview » — les panneaux d'espace de travail, eux, routent correctement. **Capture** : `docs/audit/evidence-2026-08-27/390-panel-agent-rend-deploiements.png` — en-tête « **Agent · 1 message** » avec l'icône Agent et l'onglet Agent actif dans la barre inférieure, au-dessus d'un corps entièrement **Déploiements** (« Dernier statut / Pas de déploiement », onglets « Vue d'ensemble / Journaux / Domaines / Gérer », « Aucun déploiement pour l'instant »). ⚠️ **Portée à ne pas mal lire** : une passe de 112 mesures sur les **25 clés valides** (390 et 768 px, clair et sombre) ne montre **aucun** écart `data-panel` ≠ clé demandée. La navigation normale est donc saine ; **le défaut ne se déclenche qu'en accès direct par URL avec une clé hors liste** (lien partagé, favori, rechargement, restauration d'onglet). Ne pas en conclure que le problème n'existe pas. ℹ️ Correctif en cours sur la **PR #236** (branche `fix/ide-panel-resolution`), **pas encore sur `main`** — traité par une autre session, non dupliqué ici. Fichiers : `app/components/chat/BaseChat.tsx:437` (`IDE_URL_PANELS`), `:447` (`ECODE_MOBILE_DEFAULT_TABS`), `:3868-3870` (repli `deployments`), `:9313-9319` (`displayTitle` vs `panel`), `app/utils/project-ide-panel-url.ts:5-12` (`readPanelSearchParam`). **Correctif attendu** : (a) ajouter `agent` (et alias) à l'allow-list ou les router vers le chat ; (b) remplacer le repli muet `: 'deployments'` par un état « panneau inconnu » + nettoyage de l'URL ; (c) dériver l'en-tête et le corps de la MÊME source. |
| BUG-QA-PANEL-META-ORPHANS-001 | **Deux incohérences mineures dans les registres de panneaux.** (a) `IDE_MANAGEMENT_PANELS` déclare **`'problems'` deux fois** (doublon dans le tableau `as const`) — sans effet à l'exécution (`includes()`), mais le registre et son type dérivé sont faux. (b) La clé **`web`** existe dans `ECODE_MOBILE_TAB_META` (donc un onglet peut porter ce libellé) mais n'est dispatchée **nulle part** : ni dans la chaîne `if` de `activateMobileTool`, ni dans `MOBILE_TOOL_TO_MANAGEMENT_PANEL`. Un onglet `web` restauré est donc affichable mais **inouvrable**, et via `?panel=web` il tombe dans le repli `deployments` de `BUG-QA-PANEL-AGENT-FALLBACK-001`. | ☐ | ☐ | ✅ **26/08** vérifié par lecture de `origin/main` (statique, non rejoué à l'écran) | Extraction automatisée sur `git show origin/main:app/components/chat/BaseChat.tsx` et `app/lib/mobile-ide-tabs.ts` : doublon `problems` dans `IDE_MANAGEMENT_PANELS` (26 entrées, 25 distinctes) ; `ECODE_MOBILE_TAB_META` \ (chaîne `if` ∪ `MOBILE_TOOL_TO_MANAGEMENT_PANEL`) = **`{web}`**. Contrôle : `commands` et `share`, d'abord suspectés, sont bien gérés par des branches dédiées (`activateMobileTool`, palette de commandes / panneau Collaborateurs) — **faux positifs écartés**. |
| BUG-QA-IDE-BREAKPOINT-1200-001 ✅ CLOS (voulu) | **La coque compacte de l'IDE (sans rail de gauche) ne démarre pas à 1080 px mais à 1199 px inclus — et jusqu'à 1366 px sur écran tactile.** `useMobileIde = layout.isMobile || layout.isTablet` : dès que le breakpoint n'est pas `desktop`, l'IDE rend la coque mobile/tablette et le rail de gauche disparaît. `breakpointFromViewport` classe `desktop` **uniquement si `width > TABLET_MAX_WIDTH` (1199)**. Le basculement est donc à **1200 px**, pas 1080 : 1080 px tombe déjà dans `tablet-landscape`. **C'est cohérent avec le code, donc voulu au sens strict** — mais le seuil a trois conséquences discutables. **(a) Fenêtre desktop 1080–1199 px** (navigateur non maximisé, écran 1080p avec chrome) → perte du rail alors que l'utilisateur est sur un vrai desktop avec souris. **(b) `TOUCH_TABLET_MAX_WIDTH = 1366` avec pointeur grossier** : un **portable tactile 1366×768** (configuration Windows très répandue) est classé `tablet-landscape` et perd le rail **à une largeur desktop**. **(c) `detectMobileViewport` renvoie `mobile` dès que `height < 500`**, quelle que soit la largeur : une fenêtre large mais basse (ex. 1400×450) bascule en coque **mobile**. → **Décision produit attendue d'Avi** : garder 1200 px, ou abaisser le seuil desktop (ex. 1024) et ne réserver la coque compacte qu'au vrai tactile. | ☐ | ☐ | ✅ **27/08** seuils vérifiés sur `origin/main` **ET falaise prouvée en réel, capture à l'appui** | `packages/editor/src/index.ts:27-31` (`MOBILE_BREAKPOINT=768`, `MOBILE_LANDSCAPE_MAX_HEIGHT=500`, `TABLET_MIN_WIDTH=768`, `TABLET_MAX_WIDTH=1199`, `TOUCH_TABLET_MAX_WIDTH=1366`), `:122-141` (`breakpointFromViewport`), `app/components/chat/BaseChat.tsx:2490` (`useMobileIde`). Table rejouée (`node /tmp/qa-sweep/bp.mjs`) : `390×844 tactile → mobile` ; `768×1024 tactile → tablet-portrait` ; `1024×768 → tablet-landscape` ; **`1080×900 → tablet-landscape`** ; **`1199×900 → tablet-landscape`** ; **`1200×900 → desktop`** ; `1280×800 → desktop` ; **`1366×768 tactile → tablet-landscape`** ; `1440×900 → desktop`. Dans tous les cas `tablet-*` et `mobile`, `useMobileIde` vaut `true` → coque compacte, rail absent. **PREUVE LIVE 27/08** (env de test au SHA `040dd2976d`, projet réel, panneau `deployments`, mesure du DOM rendu + capture) — la bascule se joue **à un pixel près** : **1199 px** → `.bolt-responsive-ide` porte `bolt-responsive-ide-mobile` **et** `bolt-responsive-ide-tablet-landscape`, sélecteur de rail **absent** → capture `docs/audit/evidence-2026-08-27/ide-1199px-sans-rail.png` ; **1200 px** → `bolt-responsive-ide-desktop`, rail **présent** → capture `docs/audit/evidence-2026-08-27/ide-1200px-avec-rail.png`. Résultat **identique en clair et en sombre** (12 mesures : 3 panneaux × 2 largeurs × 2 thèmes). Aucun débordement horizontal ni à 1199 ni à 1200. → **La question posée est donc tranchée sur le fond** : ce n'est pas un bug de rendu, c'est le seuil `TABLET_MAX_WIDTH=1199` qui s'applique exactement comme codé. **DÉCISION D'AVI, 27/08 — QUESTION CLOSE** : « pour tablet ce doit être comme mobile ». Le comportement actuel est **voulu et conservé** ; `TABLET_MAX_WIDTH = 1199` **n'est pas à modifier**, et les trois conséquences listées (fenêtre 1080–1199 px, portable tactile 1366 px, hauteur < 500 px) sont **acceptées**. Cette entrée n'est donc **pas un défaut** : elle devient la **référence de comportement attendu**. **Conséquence pour la QA** : la tablette 768 doit être vérifiée avec les **mêmes exigences que le mobile 390** — cibles ≥ 44 px, aucune action accessible seulement au survol, barre inférieure cohérente, mêmes états et libellés. Voir `BUG-QA-TABLET-MOBILE-PARITY-001`. |
| BUG-QA-BUILD-PAYLOAD-264MB-001 | **Chaque build Cloud Build téléverse ~264 Mo d'artefacts de preuve qui ne servent à aucun Dockerfile.** Sur un export propre de `origin/main` (`git archive`), le contexte de build pèse **358 Mo**, dont **`docs/` = 264 Mo** : `docs/parity` **163 Mo**, `docs/deploy-evidence` **94 Mo**, `docs/ui-ux-evidence` 4,9 Mo, `docs/images` 892 Ko — des captures, HAR et attestations. Le `.gcloudignore` ignore `*.md` puis **ré-inclut** `!docs/*.md` et `!docs/**/*.md` ; les fichiers **binaires** de `docs/` ne sont couverts par aucune règle et partent donc dans le tarball. **Aucun des 4 Dockerfiles** (`Dockerfile`, `infra/docker/deps.Dockerfile`, `node-service.Dockerfile`, `screenshotter.Dockerfile`) ne référence `docs/`. Effet mesuré : l'upload du contexte a dépassé **38 minutes** sur une liaison domestique avant d'être interrompu ; une soumission antérieure du même contexte s'est soldée par un **`INTERNAL_ERROR`** côté Cloud Build après une phase d'upload comparable. Exclure `docs/parity`, `docs/deploy-evidence`, `docs/ui-ux-evidence` et `docs/images` ramène le contexte à **~94 Mo (−74 %)**. Impact : allonge tout `gcloud builds submit` (CD de `main` compris) et fragilise la soumission, sans rien apporter à l'image. | ☐ | ☐ | ✅ **27/08** mesuré (tailles + absence de référence dans les Dockerfiles) ; **gain de temps non encore chronométré de bout en bout** | `du -sh` sur `/tmp/qa-sweep/src-040dd2976d` (export `git archive origin/main`, SHA `040dd2976d`) : total **358M**, `docs` **264M**, `docs/parity` **163M**, `docs/deploy-evidence` **94M**, `public` 38M, `app` 23M, `packages` 22M. `grep -rn 'docs' Dockerfile infra/docker/*.Dockerfile` → **aucune correspondance**. Règles en cause : `.gcloudignore` lignes `*.md` / `!docs/*.md` / `!docs/**/*.md`. **Correctif proposé** : ajouter `docs/parity/**`, `docs/deploy-evidence/**`, `docs/ui-ux-evidence/**`, `docs/images/**` au `.gcloudignore` du repo. |
| BUG-QA-CI-NO-MOBILE-COVERAGE-001 | **P1 — Aucun signal automatisé à la largeur iPhone : le seul job CI qui teste en 390 px n'aboutit jamais, et l'étape E2E « mobile » n'exécute que 2 tests sur 21, aucun à une largeur de téléphone.** **(1) `i18n-live-audit.yml`** est le seul workflow avec une matrice de viewports (`desktop-1440`, `desktop-1024`, `tablet-768`, `mobile-390`). Sur les runs des 24 et 25/08, **`Playwright mobile-390` est `cancelled` à chaque fois** pendant que les 3 autres viewports passent en `success`. Cause : `timeout-minutes: 90` atteint — run `32873413018` démarré à 16:41:00, terminé à 18:12:06 = **91 min** ; l'étape « Run exhaustive EN/FR live audit » est annulée, puis « Verify complete proof set » échoue. Le shard 390 ne produit donc **aucune preuve**, alors que `tablet-768` en produit. **(2) `e2e.yml:141`**, étape nommée « Playwright mobile viewport tests », lance `tests/e2e/responsive-ide.spec.ts` avec **`--project=chromium`** (Desktop Chrome, 1280×720, `isMobile:false`). Or les tests sont gardés par `test.skip(!isMobile, …)` et `test.skip(!isCompactIdeProject(testInfo), …)` avec `isCompactIdeProject = project.name === 'mobile' || 'tablet'`. Décompte sur `origin/main` : **21 tests** → 3 exclus par `--grep-invert @runtime`, **16 ignorés** par ces gardes (7× `!isMobile`, 9× `!isCompactIdeProject`), **2 réellement exécutés** — `tablet exposes icon-only tab navigation…` et `tablet landscape uses the compact mobile IDE shell`, **tous deux en 1024×768 uniquement**. **Aucun `--project=mobile` ni `--project=tablet` n'existe dans aucun workflow** (`git grep -- '--project=' .github/workflows/` → seulement `chromium`, plus la matrice i18n). **(3)** Le projet Playwright `mobile` est **Pixel 7 (412×915, UA Android)** : même s'il était lancé, il ne couvrirait ni la largeur **390** ni **WebKit/iOS**. Conséquence : les régressions responsive à largeur iPhone — débordement horizontal, cibles tactiles < 44 px, panneaux tronqués — **ne peuvent pas être détectées par la CI**. C'est précisément le format de test principal d'Avi. | ☐ | ☐ | ✅ **27/08** vérifié sur `origin/main` + historique GitHub Actions réel | **Preuves.** `gh run view 32873413018` → `success Playwright desktop-1024` / `success desktop-1440` / `success tablet-768` / **`cancelled Playwright mobile-390`** ; idem runs `32861803440`, `32824390377`, `32786472284`, `32758054785`. Durée du job annulé : 91 min pour `timeout-minutes: 90` (`i18n-live-audit.yml:18`). Décompte des tests : script d'analyse sur `git show origin/main:tests/e2e/responsive-ide.spec.ts` (21 `test(`, gardes `test.skip` recensées par test). `tests/e2e/responsive-ide.spec.ts:4-6` (`isCompactIdeProject`), `playwright.config.ts` (projets `chromium` / `tablet` 1194×834 / `mobile` = Pixel 7), `.github/workflows/e2e.yml:141`. ⚠️ **Piège de lecture** : les 5 tests nommés « mobile … » ne tournent PAS en 1280×720 — ils sont **ignorés** par `test.skip(!isMobile)`. L'étape CI est donc quasi vide, pas faussement verte sur un mauvais viewport. **Correctif attendu** : (a) lancer `--project=mobile` et `--project=tablet` dans `e2e.yml` ; (b) ajouter un projet Playwright iPhone 390 (WebKit de préférence) ; (c) découper ou accélérer l'audit i18n 390 pour qu'il rentre dans son budget de 90 min. |
| BUG-QA-TAP-TARGETS-MARKETING-001 | **Cibles tactiles sous le minimum de 44 px sur 4 pages marketing, identiques en clair et en sombre, en 390 px comme en 768 px.** Deux familles, de gravité différente — la distinction compte pour le correctif : **(a) De vrais contrôles interactifs trop bas.** `/features` : les **6 filtres** « Tout / Développement / Collaboration / Infrastructure / Sécurité / Analyses » font **36 px de haut** (58×36, 132×36, 116×36, 120×36, 83×36, 88×36). `/docs` : **7 à 8 éléments à 34 px de haut** (« @ mentions de fichiers » 172×34, « / commandes slash » 150×34, « Checklist avant exécution » 190×34, « Branches de conversation » 191×34, « Partage de conversations » 293×34, « Présence en direct » 145×34, et en 768 px « Application automatique… » 358×34). `/contact` : deux contrôles non étiquetés à **40 px** (308×40 en 390, 299×40 en 768) — 4 px sous la cible. **(b) Liens `mailto:` en ligne, à 18 px de haut** : `sales@`, `support@`, `press@`, `security@` sur `/contact` (106×18 à 124×18) et `privacy@` sur `/terms` (116×18). Ce sont des ancres **en flux de texte**, dont la hauteur suit l'interligne — comportement web standard, **moins grave** que (a), mais sur une page Contact ce sont les actions principales et elles restent difficiles à viser au doigt. Le guide Apple (HIG) recommande 44×44 pt ; Material recommande 48 dp. | ☐ | ☐ | ✅ **27/08** mesuré live prod, 390 **et** 768, clair **et** sombre — 4 combinaisons concordantes par page | **Repro** : `node /tmp/qa-sweep/mob/public.mjs` — Chrome réel (`channel:'chrome'`), UA iPhone, `deviceScaleFactor` 3 (390) / 2 (768), `colorScheme` forcé, attente d'hydratation puis mesure de `getBoundingClientRect()` sur `button, a[href], [role=button], input, select` visibles. **72 mesures** (18 routes × 2 viewports × 2 thèmes), artefacts et captures dans `/tmp/qa-sweep/mob/public-art/`. Résultats **strictement identiques** entre clair et sombre sur les 4 pages → défaut de mise en page, pas de thème. **Contrôle de validité de la sonde** : 14 des 18 pages rendent **0** contrôle sous 44 px — la sonde n'est donc pas muette, le 0 est un vrai 0. **Résultat positif à conserver** : **aucun débordement horizontal** (`scrollWidth − clientWidth ≤ 2 px`) sur **les 72 mesures**, 390 px inclus. ⚠️ 5 mesures étaient d'abord revenues vides (`len=19`, page non hydratée) ; **rejouées une à une, les 5 rendent normalement avec `ovf=0`** — c'étaient des ratés de mesure, pas des pages cassées. Elles ne sont pas comptées comme défauts. |
| BUG-QA-TAP-TARGETS-IDE-MOBILE-001 | **Le chrome permanent de l'IDE mobile est sous le minimum tactile de 44 px — sur les 25 panneaux, dans les deux thèmes, en 390 comme en 768 px.** La mesure sépare deux familles, et la première est la seule vraiment gênante : **(a) SOCLE COMMUN — 8 contrôles présents sur 50/50 des mesures 390 px** (donc sur *tous* les panneaux, quel que soit leur contenu) : « Retour au tableau de bord » **36×36**, « Activité » **36×36**, « Ouvrir les outils » **36×36**, « Plus d'options » **36×36** — soit **8 px sous la cible** ; le champ « Rechercher des outils et des fichiers » **202×20** — **24 px sous la cible**, le pire du lot ; et les 3 actions rapides « Exécuter l'aperçu » / « Améliorer project/README.md » / « Ajouter une fonctionnalité » à **181×38**. Ces contrôles sont la navigation permanente : un doigt les rate sur toute la surface de l'IDE mobile. **(b) CONTENU PROPRE AUX PANNEAUX — uniformément à 42 px de haut**, soit **2 px sous la cible** : `collaborators` ajoute **12** contrôles 42 px (champs d'invitation, sélecteurs de rôle, « Inviter au projet », « Ajouter un commentaire », « Créer un lien expirant », « Activer l'IA partagée »…), `activity` en ajoute **8** (« Actualiser maintenant » 324×42, filtres 358×42, puces de comptage). L'écart étant de 2 px et homogène, (b) relève d'un ajustement de jeton de hauteur, pas d'une refonte. | ☐ | ☐ | ✅ **27/08** mesuré live, 112 mesures + énumération exhaustive, captures à l'appui | **Environnement** : env de test d'audit au SHA **`040dd2976d`** de `main` (release `vibecore` rév. 21, `web` et `api` à ce SHA). Chrome réel via Playwright, UA iPhone, `deviceScaleFactor` 3 (390) / 2 (768), thème forcé, projet `cmtbdqetq00040nh6bw7zbkcg`. **Méthode** : `getBoundingClientRect()` sur `button, a[href], [role=button], [role=tab], input, select` visibles ; seuil 44 px (Apple HIG). **Passe** : 25 panneaux × {390, 768} × {clair, sombre} = **100 mesures**, plus 12 à 1199/1200 px → `docs/audit/evidence-2026-08-27/passe-112-mesures.json`, captures dans `/tmp/qa-sweep/mob/ide-art/`. **Énumération exhaustive** (échantillon non tronqué) sur `integrations`, `git`, `collaborators`, `activity` → `/tmp/qa-sweep/mob/focus.log` section C. ⚠️ **Piège méthodologique écarté** : les écarts clair/sombre apparents (`packages` 8 vs 19, `skills` 8 vs 21, `integrations` 8 vs 60, `git` 8 vs 29…) **ne sont PAS des défauts de thème** — ils suivent la longueur du contenu chargé (`len` 336 vs 1787, 344 vs 1646, 346 vs 4078, 247 vs 1588). Un panneau mesuré avant l'arrivée de ses données n'expose que le socle. La mesure retenue par panneau est donc **celle où le contenu a le plus chargé**, et les panneaux restés vides même au meilleur essai (`overview`, `object-storage`, `debugger`, `security`, `secrets`, `logs` en 390) sont déclarés **non probants**, pas conformes. ⚠️ **Réserve** : `integrations` a compté **60** cibles dans la passe où il était chargé, mais l'énumération exhaustive l'a rattrapé vide (`len=392`) — la **famille** est caractérisée (contrôles de formulaire à 42 px), les 60 items ne sont **pas** énumérés un à un. ⚠️ **Réserve d'environnement** : seuls `web` et `api` sont au SHA de `main` ; les 5 autres services restent au `1c68880b39` (12/08). Sans effet attendu sur la géométrie, mais l'environnement n'est pas homogène. **Résultat positif à conserver** : **zéro débordement horizontal sur les 112 mesures** (390, 768, 1199, 1200 px, clair et sombre) — l'IDE tient la largeur iPhone. |
| BUG-QA-PRISMA-CODE-LEAK-001 | **Le gestionnaire d'erreurs global de l'API renvoie au client le code d'erreur Prisma brut.** `services/api/src/app.ts:8779` (sur `origin/main`) : `const code = (error as Error & { code?: string }).code ?? 'API_ERROR';` puis `reply.code(statusCode).send({ error: …, code })`. Le **message** est correctement assaini (« Internal server error » générique pour les 5xx, via `appPublicEnglish('INTERNAL_SERVER_ERROR')`), **mais le champ `code` ne l'est pas** : pour une `PrismaClientKnownRequestError`, `error.code` est le code Prisma. Toute erreur de persistance non interceptée expose donc à l'appelant la classe exacte de la panne — `P2002` (contrainte d'unicité), `P2003` (clé étrangère), `P2025` (enregistrement absent), `P2037` (connexions épuisées)… Conséquences : (a) divulgation de la pile de persistance (Prisma/PostgreSQL) et du type de contrainte violée, utile à un attaquant pour cartographier le schéma ; (b) des clients pourraient se mettre à brancher sur des codes internes Prisma, qui ne sont pas un contrat d'API. ⚠️ **À ne PAS confondre avec un défaut de facturation.** Le 500 qui a révélé ce comportement (`GET /billing/subscription` et `/billing/usage` → `{"error":"Internal server error","code":"P2003"}`) est un **artefact de capacité de l'environnement de test**, pas un bug produit : les logs API montrent « *Too many database connections opened: remaining connection slots are reserved for roles with privileges of the pg_use_reserved_connections role* » sur `prisma.session.findUnique()`. L'instance Cloud SQL d'audit est un **`db-g1-small`** avec **5 réplicas API** et un balayage navigateur concurrent — le runbook §2 précise d'ailleurs que le « comportement sous charge réelle » n'est plus prouvé sur cet environnement. **Le défaut retenu ici est uniquement la fuite du code**, pas le 500. | ☐ | ☐ | ✅ **27/08** code confirmé sur `origin/main` + réponse client capturée en réel | **Preuve client** (env de test, SHA `040dd2976d`) : `curl -H "authorization: Bearer <token>" https://api.<lb>.sslip.io/billing/subscription` → **HTTP 500** `{"error":"Internal server error","code":"P2003"}` ; rejoué → identique. **Preuve serveur** : logs du pod API, `"type":"PrismaClientKnownRequestError"`, message « Too many database connections opened… » sur `prisma.session.findUnique()`. **Preuve code** : `git show origin/main:services/api/src/app.ts` → `setErrorHandler` ligne 8735, construction du `code` ligne 8779, envoi ligne 8786. ⚠️ **Non rejoué sur la production** : déclencher une erreur Prisma en prod exigerait de provoquer volontairement une panne de persistance — écarté. Le constat repose sur le **code de `main`**, qui est le même binaire, et sur la reproduction en environnement de test. **Correctif proposé** : n'exposer que des codes applicatifs connus (liste blanche : `VALIDATION_ERROR`, `AUTH_*`, `PANEL_*`…) et rabattre tout code non reconnu — a fortiori tout code commençant par `P` suivi de chiffres — sur `API_ERROR`, en conservant le code réel uniquement dans le log et Sentry (ce que le handler fait déjà ligne 8759). |
| BUG-QA-TABLET-MOBILE-PARITY-001 | **Contrôle de parité tablette 768 ↔ mobile 390 sur les 25 panneaux de l'IDE — exigence posée par Avi le 27/08 : « pour tablet ce doit être comme mobile ».** **Résultat principal : 20 panneaux sur 25 en parité stricte**, et surtout **zéro action accessible uniquement au survol sur les 50 mesures** (25 panneaux × 2 largeurs) — le critère tactile est respecté aux deux formats. Barre de navigation inférieure **identique** entre 390 et 768 sur tous les panneaux (8 entrées ; 7 pour `deployments`, mais **7 des deux côtés**, donc pas un écart de parité). En-têtes et onglets identiques. Nombre de cibles < 44 px identique entre les deux largeurs sur ces 20 panneaux. **5 panneaux ont montré un écart au premier passage** — `overview`, `database`, `workflows`, `domains`, `activity` — mais l'examen des libellés montre des **états transitoires de backend**, pas un traitement différent selon la largeur : boutons « **Réessayer** » présents d'un seul côté (`database` en 390, `domains` en 768, **côtés inversés** selon le panneau), et compteur qui s'incrémente entre deux chargements (`activity` : « export zip: 59 » vs « export zip: 60 »). **Cause identifiée et attribuée à la mesure elle-même** : l'environnement de test était saturé — **22 erreurs « Too many database connections opened » en 3 minutes** dans les logs API, avec un Cloud SQL `db-g1-small` et **5 réplicas** d'API. Ramené à 2 réplicas, puis campagne de confirmation lancée (3 essais par panneau, **ordre des viewports alterné** pour annuler tout biais d'ordre). **VERDICT FINAL 27/08 — aucun écart de traitement 390↔768 n'est retenu sur les 25 panneaux.** Les 5 candidats ont été rejoués avec un critère de mesure corrigé (voir ci-dessous) : `database` **282 = 282**, cibles 9 = 9 → **parité** ; `domains` **990 = 990**, cibles 10 = 10 → **parité** ; `overview` **1278 = 1278** sur l'essai où les deux côtés ont effectivement chargé (un essai 768 n'a pas chargé du tout, `len=26`, drapeau « chargé » à faux → **écarté comme raté de mesure, pas comme écart**) ; `workflows` **564 = 564** et cibles **13 = 13** sur le second essai (le premier donnait 558 côté 768, soit 6 caractères d'écart, non reproduit) ; `activity` **non comparable par égalité stricte** — son contenu embarque des compteurs vivants qui s'incrémentent entre deux chargements (« export zip: 59 » → « 60 »), et les deux côtés fluctuent (8032/8747 en 390, 8497/8368 en 768) ; la **structure**, elle, concorde (16/16 puis 16/17 cibles). **Bilan : 22 panneaux en égalité stricte, 2 en égalité dès que les deux côtés chargent, 1 non comparable par nature. Zéro divergence attribuable à la largeur.** | ☐ | ☐ | ✅ **27/08 — 25/25 sans écart de traitement 390↔768** | **Méthode** : harnais `/tmp/qa-sweep/mob/parite.mjs` — le **même panneau est chargé aux deux largeurs dans la même passe**, avec attente de **stabilisation du contenu** (2 relevés consécutifs identiques) avant comparaison, ce qui neutralise les artefacts d'état de chargement qui avaient faussé une première comparaison. Signature comparée par panneau : ensemble trié des libellés de contrôles visibles, entrées de la barre inférieure, onglets, en-tête, cibles < 44 px, et **éléments dont l'action n'est révélée qu'au survol** (`opacity:0` ou `visibility:hidden` au repos sur un élément interactif). UA iPhone en 390 (`deviceScaleFactor` 3), UA iPad en 768 (`deviceScaleFactor` 2), `hasTouch` dans les deux cas. Env de test au SHA `040dd2976d` de `main`. **Captures appariées** pour les 25 panneaux : `/tmp/qa-sweep/mob/parite-art/<panneau>-390.png` et `-768.png` (local). Conservées dans le dépôt pour les 3 panneaux contestés : `docs/audit/evidence-2026-08-27/parite-{overview,workflows,activity}-{390,768}.png`, plus le jeu de données complet `parite-390-768.json`. ⚠️ Ajouter `docs/audit/evidence-*/**` au `.gcloudignore` avec les autres dossiers de preuves (voir `BUG-QA-BUILD-PAYLOAD-264MB-001`) pour ne pas les réintroduire dans le contexte de build. **Parité stricte confirmée sur 22 panneaux** : `packages`, `skills`, `monitoring`, `ports`, `extensions`, `integrations`, `deployments`, `security`, `env`, `secrets`, `git`, `terminal`, `logs`, `collaborators`, `snapshots`, `settings`, `studio`, `problems`, `object-storage`, `debugger`, `database`, `domains` — longueurs, libellés, navigation et comptes de cibles **strictement égaux** entre 390 et 768. ⚠️ **DÉFAUT DE MA PROPRE MÉTHODE, corrigé — à connaître pour toute campagne future.** Le premier critère de stabilisation acceptait une mesure dès que la longueur du contenu était identique trois fois de suite. Or **un écran « Chargement de <panneau>… » est parfaitement stable** : il satisfaisait le critère, et trois campagnes entières ont donc mesuré l'écran d'attente en croyant mesurer le panneau — d'où des longueurs aberrantes de 349 à 466 caractères sur des panneaux qui en font 8000 à 9800 une fois chargés, et des verdicts « PARITÉ NON » entièrement faux. **Preuve du diagnostic** : sur `activity`, le BFF répondait déjà `HTTP 200` avec **30 591 octets** pendant que la page affichait encore « Chargement de activité… » ; le contenu n'apparaissait qu'à **132 891 ms** (session réutilisée) et **119 201 ms** (connexion fraîche) — le panneau n'était pas bloqué, seulement très lent **sur cet environnement**. **Critère corrigé** : exiger l'**absence de tout indicateur de chargement** en plus de la stabilité, avec 240 s de budget par panneau. ⚠️ **Ces ~2 minutes ne sont PAS retenues comme un défaut de performance produit** : l'environnement de test a un Cloud SQL `db-g1-small` et 3 réplicas d'API, et le runbook §2 précise que le « comportement sous charge réelle » n'y est pas prouvable. À mesurer sur un environnement dimensionné avant toute conclusion. |
| BUG-QA-THUMBNAIL-404-CAPTURE-001 | **P1 — La vignette stockée d'un projet est la capture d'écran d'une page d'erreur JSON 404 : c'est le « rectangle vide » vu sur les cartes du tableau de bord.** Les 3 vignettes qui chargent sur le tableau de bord de production font 1280×800 et sont **visuellement blanches**, avec une seule ligne de texte en haut à gauche. Au zoom, c'est la capture d'une réponse JSON rendue par le visualiseur de Chrome : `{"message":"Route GET:/ not found","error":"Not found","statusCode":404}` — **texte identique sur les 3**, y compris pour un projet dont la carte porte le badge « **Deployed** ». C'est la forme d'un 404 **Fastify** (`Route <MÉTHODE>:<chemin> not found`), et non celle de l'API plateforme (qui ajoute un champ `code`). **Chaîne de déclenchement** : `app/components/workbench/Preview.tsx:1261-1272` — quand `activePreview.ready === true`, le client poste `activePreview.baseUrl` sur `/api/projects/:id/thumbnail/refresh` ; l'API (`services/api/src/app.ts`, route `/projects/:projectId/thumbnail/refresh`) valide l'URL puis fait `thumbnailCapturer.schedule(project.id, body.url)`. Le screenshotter photographie donc **l'URL fournie par le client, telle quelle**. **Deux causes possibles, non départagées à ce stade** : (a) `ready` passe à `true` alors que `GET /` n'est pas encore servi par l'application de l'utilisateur ; (b) `baseUrl` pointe vers un service **Fastify** (agent d'espace de travail) plutôt que vers le serveur de développement du projet. La forme Fastify du 404 rend (b) plus probable, mais je ne l'affirme pas sans l'avoir prouvé. **Aucune garde côté capture** : rien ne vérifie que la page photographiée est bien l'application (statut HTTP de la cible, `content-type` HTML, page non vide) avant de stocker le résultat comme vignette du projet. | ☐ | ☐ | ✅ **27/08** reproduit live prod, contenu de la vignette lu au zoom | Relevé complet, tableau par projet et commandes de repro : `docs/audit/evidence-2026-08-27/vignettes-tableau-de-bord.md`. ⚠️ **Le constat initial « rectangle vide à la place de la vignette » est à reformuler** : le tableau de bord a bien un état de repli propre (« No preview yet » + « A captured app preview will appear here »), qui fonctionne. Le rectangle blanc n'est **pas** un repli manquant — c'est une vignette **réellement chargée dont le contenu est une page d'erreur**. Le correctif n'est donc pas côté affichage mais côté capture. |
| BUG-QA-AUDIT-LOGS-NO-PAGINATION-001 | **P1 — `/audit-logs` rend jusqu'à 2000 entrées d'un seul bloc, sans pagination ni virtualisation : sur la production, l'onglet du navigateur se fige.** **Sur la production**, après chargement de `https://app.e-code.ai/audit-logs`, toute évaluation JavaScript dans l'onglet échoue : `CDP sendCommand "Runtime.evaluate" timed out after 45000ms on tab — The renderer may be frozen or unresponsive`, **deux fois de suite**, et la capture d'écran devient impossible. Le rendu du processus est saturé. **Sur l'environnement de test**, où le volume est moindre, la page reste utilisable et permet de **quantifier** : **44 945 nœuds DOM**, **1818 lignes**, **249 379 caractères** de texte, document de **461 480 px** de haut — soit **547 écrans d'iPhone** à faire défiler. `domContentLoaded` à 5030 ms, contenu à 10 161 ms. **Chaîne complète, lue sur `origin/main`** : le loader `app/routes/audit-logs.tsx:175` appelle `apiRequest(request, \`/orgs/${organization.id}/audit-logs\`)` **sans aucun paramètre de limite, d'offset ou de page** ; l'API `services/api/src/app.ts` (`GET /orgs/:orgId/audit-logs`) retourne `{ auditLogs: await store.listAuditLogs(orgId) }` sans filtrage ; le store `services/api/src/prisma-store.ts:5637` plafonne à **`take: 2000`**. Le plafond existe donc **au niveau des données**, mais **pas au niveau du rendu** : la page affiche l'intégralité de ce qu'elle reçoit. À ~25 nœuds par ligne, 2000 lignes approchent les **50 000 nœuds DOM** — ce qui correspond aux 44 945 mesurés pour 1818 lignes. Le commentaire du loader assume le choix (« keeps the loader a single API round-trip »), mais rien ne borne l'affichage. ⚠️ Les boutons « Suivant » / « Plus tard » présents sur la page sont des **raccourcis de plage de dates**, pas une pagination de la liste rendue. | ☐ | ☐ | ✅ **27/08** gel reproduit sur la prod ; volumétrie mesurée sur l'env de test | **Repro production** : ouvrir `https://app.e-code.ai/audit-logs` sur un compte à l'historique fourni, puis tenter n'importe quelle évaluation JS → délai dépassé, renderer figé. **Repro quantifiée (env de test, 390 px)** : `node /tmp/qa-sweep/mob/audit.mjs` → `{noeuds:44945, lignes:1818, longueurTexte:249379, hauteurDoc:461480, ecrans:547, domContentLoaded:5030}`. Capture : `/tmp/qa-sweep/mob/audit-logs-390.png`. ⚠️ **Réserve d'attribution** : une partie des 1818 entrées de l'environnement de test provient de **mes propres balayages** du jour. Cela ne change pas le défaut — l'absence de bornage au rendu — mais le volume de test n'est pas représentatif d'un usage normal. Le gel constaté en **production** l'est, lui. **IMPACT UTILISATEUR, en une phrase** : sur un compte à l'historique fourni, ouvrir « Journaux d'audit » **fige l'onglet du navigateur** — l'utilisateur doit forcer la fermeture de l'onglet, et la page est donc **inutilisable en production**. **CORRECTION RECOMMANDÉE — le plafond côté données ne suffit pas, il faut borner le RENDU.** Trois options, par ordre de simplicité : **(1) Pagination au loader** *(la plus simple, recommandée)* — passer `limit` (25 à 50) et un curseur à `/orgs/:orgId/audit-logs` depuis `app/routes/audit-logs.tsx:175`, et ajouter les contrôles « page précédente / suivante ». Nécessite d'accepter `limit`/`cursor` côté API (`GET /orgs/:orgId/audit-logs`) et dans `listAuditLogs`, qui prend aujourd'hui un `take: 2000` figé. **(2) Virtualisation de la liste** — ne monter que les lignes visibles (fenêtre glissante). Conserve le « single API round-trip » assumé par le commentaire du loader, mais ramène le DOM de ~45 000 nœuds à quelques centaines. Plus de travail côté composant. **(3) Palliatif immédiat** — abaisser le `take: 2000` de `prisma-store.ts:5637` et afficher « affichage des N dernières entrées », en attendant (1) ou (2). Réduit le gel sans le supprimer sur les gros comptes, et **dégrade la fonction** (on ne peut plus consulter l'historique complet) : à ne retenir que comme mesure d'urgence. ⚠️ Ne **pas** se contenter d'abaisser le plafond du store en croyant le problème réglé : c'est le rendu de toutes les lignes reçues qui sature, quel que soit le plafond. |
| BUG-QA-TAP-TARGETS-APP-001 | **Liste exacte des commandes sous 44 px dans l'espace utilisateur — mesurées en 390 ET en 768 px, dimensions réelles du rendu.** Presque toutes les violations sont des **boutons d'action à 36 px de haut** (8 px sous la cible) et des **liens à 39 px** (5 px sous). Les comptes sont **identiques entre 390 et 768** sur 4 des 5 pages : ces défauts ne dépendent donc pas de la largeur, ils sont dans les composants eux-mêmes. **`/templates` — 7 cibles, en 390 comme en 768** : `Aller au contenu` **135×39** (`<a>`) ; **6× `Utiliser le modèle` 143×36** (`<button>`) — un par carte de modèle, c'est l'action principale de la page. **`/billing` — 6 cibles, en 390 comme en 768** : `Aller au contenu` 135×39 ; `Enregistrer la limite` **156×36** ; `Bloquer les IA externes` **180×36** ; `Passer à la formule Pro` **178×36** ; `Passer à la formule Équipe` **200×36** ; `Ouvrir le portail client` **169×36**. **`/upgrade` — 6 cibles** : `Aller au contenu` 135×39 ; `Contacter l'équipe commerciale` 295×36 (390) / **277×36** (768) ; `Formule actuelle` 295×36 / 277×36 ; `Choisir la formule Pro` **295×34** / 277×34 ; `Choisir la formule Team` **295×34** / 277×34 ; et un second `Contacter l'équipe commerciale` **313×39** en 390 mais **199×18** en 768 — **la plus petite cible relevée de tout le balayage**. **`/explore` — 5 cibles, en 390 comme en 768** : les filtres de catégorie, tous à **36 px de haut** — `Tous` **64×36**, `API et services applicatifs 1` 216×36, `IA et machine learning 1` 194×36, `Mobile 2` **91×36**, `Applications web 16` 167×36. **`/plan-comparison` — 5 cibles en 390, 4 en 768** : `Aller au contenu` 135×39 ; `Choisir Starter` 295×39 (390) / **123×39** (768) ; `Choisir Core` 295×39 / **106×39** ; `Choisir Pro` 295×39 / **98×39** ; `Contacter l'équipe commerciale` 295×39, **présent en 390 seulement**. ⚠️ `Aller au contenu` (135×39) est le lien d'évitement d'accessibilité, visible seulement au focus clavier — **moins prioritaire** que les autres, mais listé pour être exhaustif. **Le motif dominant est une hauteur de 36 px sur les boutons d'action** : une correction du jeton de hauteur des boutons réglerait la majorité des cas d'un coup. | ☐ | ☐ | ✅ **28/08** mesuré live sur env de test au SHA `040dd2976d`, 390 et 768 px | Relevé brut complet : `/tmp/qa-sweep/mob/tap44.log`. Méthode : `getBoundingClientRect()` sur `button, a[href], [role=button], [role=tab], input, select, textarea` visibles, seuil 44 px (Apple HIG), Chrome réel, UA iPhone en 390 / UA iPad en 768, `hasTouch` dans les deux cas, session authentifiée. |
| BUG-QA-SLUG-ACCENTS-001 | **Le nom d'un projet accentué produit une URL mutilée et définitive : « Crée une page web simple » devient `/@org/cr-e-une-page-web-simple`.** Le correctif **existe déjà** dans `services/api/src/slugify.ts` : `baseSlug()` applique `normalize('NFD')` puis retire les marques combinantes **avant** le filtre `[^a-z0-9]`, et l'en-tête du fichier documente précisément ce symptôme (« *Without that step every accented letter collapsed to a dash… "Créez une page de tarification" → cr-ez-une-page-de-tarification* ») en avertissant qu'il ne faut pas dupliquer l'implémentation « *in prisma-store.ts and app.ts* ». **C'est exactement ce qui s'est produit** : `services/api/src/app.ts` **n'importe jamais `slugify`** — uniquement `slugifyRouteSegment` (ligne 173, pour la résolution d'URL). Les chemins de **création** réimplémentent le slug en ligne, sans retirer les diacritiques : `slug: body.slug ?? name.toLowerCase().replace(/[^a-z0-9]+/g, '-')` — **5 occurrences** : **18893** (`POST /orgs`), **20026** (`POST /orgs/:orgId/projects`), **20135** (`POST /orgs/:orgId/projects/from-template`), **20205** (`POST /orgs/:orgId/projects/from-ai`), **20747** (`POST /orgs/:orgId/projects/import/github`). Le côté **lecture** normalise correctement (`slugifyRouteSegment`), le côté **écriture** non : c'est la dérive exacte que le fichier partagé cherchait à empêcher. **Portée** : tout nom de projet **ou d'organisation** contenant un accent — donc l'essentiel des noms en français, et le premier mot des prompts générés par l'IA. L'URL est figée à la création et accompagne le projet pour toute sa vie. **Impact** : cosmétique mais permanent et visible — l'adresse partagée d'un projet porte `cr-e-` au lieu de `cree-`. Aucune erreur de résolution constatée (le slug stocké est celui utilisé dans l'URL, et sa normalisation est idempotente). | ☐ | ☐ | ✅ **28/08** reproduit live de bout en bout sur env de test au SHA `040dd2976d` | **Repro** : créer un projet depuis `/projects/new` avec le prompt « Crée une page web simple avec un titre et un bouton ». Résultat observé : redirection vers **`/@qa-sweep-org/cr-e-une-page-web-simple`**, et `GET /orgs/:orgId/projects` confirme `slug = "cr-e-une-page-web-simple"` pour `name = "Crée une page web simple"` (`sourceType: ai`). **Correctif** : importer `slugify` depuis `./slugify.js` et remplacer les **5** expressions en ligne par `slugify(name)` — le module partagé existe déjà et est testé. |
| QA-VERIF-AUTH-CREATION-001 ✅ CONFORME | **[Vérification, pas un défaut] Authentification et parcours de création de projet en 390 px — conformes.** **Authentification.** Mot de passe invalide → reste sur `/login`, message affiché : « **L'adresse e-mail ou le mot de passe est incorrect.** ». Formulation **non énumérante** (identique pour un e-mail inexistant, donc elle ne révèle pas si le compte existe) — bonne pratique respectée. `aria-invalid="true"` posé sur 2 champs, saisie de l'e-mail conservée, aucune erreur JavaScript. Connexion valide → `/dashboard` en **4228 ms**. **Création de projet.** `/projects/new` rend sa zone de saisie ; après saisie d'un prompt, le bouton **`Créer` devient actif et mesure 106×44 px — conforme au minimum tactile**. Le clic crée réellement le projet et redirige vers `/@qa-sweep-org/…`, **0 erreur JavaScript**. Le projet apparaît ensuite dans `GET /orgs/:orgId/projects` avec `sourceType: ai`. ⚠️ **Une seule anomalie sur ce parcours**, consignée à part : le slug généré est mutilé sur les accents → `BUG-QA-SLUG-ACCENTS-001`. | — | — | ✅ **28/08** rejoué live, captures | Captures : `docs/audit/evidence-2026-08-27/auth-390-message-erreur.png` (message d'erreur de connexion, pleine page) et `creation-projet-resultat-390.png` (état après création). Journal : `/tmp/qa-sweep/mob/parcours.log`, données `/tmp/qa-sweep/mob/parcours-art/parcours.json`. ⚠️ **Correction d'une de mes propres alertes** : ma première sonde avait conclu « aucun message d'erreur affiché » — elle ne lisait que les **300 premiers caractères** de la page, alors que le message est plus bas. Vérification faite sur la page complète, le message est bien présent, **à trois endroits** (un bandeau `vc-auth-alert-error` plus deux messages sous les champs). Redondance discutable, mais **pas un défaut**. ⚠️ **Non couvert sur ce parcours** : inscription d'un nouveau compte, réinitialisation de mot de passe, OAuth Google/GitHub (applications de test absentes sur cet environnement, voir runbook §5), et vérification d'e-mail. |
| BUG-QA-TABLET-MOBILE-DIVERGENCE-APP-001 | **Écarts de traitement 390 ↔ 768 dans l'espace utilisateur — la tablette montre des commandes que le mobile n'a pas, sur TOUTES les routes.** Rappel de la règle posée par Avi le 27/08 : « pour tablet ce doit être comme mobile ». Ces écarts y contreviennent. **(a) Systématique, sur les 17 routes à coque applicative, en clair ET en sombre — 3 commandes présentes en 768, absentes en 390** : **`Ouvrir la palette de commandes`** (bouton « ⌘ Rechercher » de l'en-tête), **`Anglais`** et **`Langue actuelle : Français`** (le sélecteur de langue EN/FR). Conséquence concrète : **sur iPhone, l'utilisateur ne peut ni changer de langue ni ouvrir la palette de commandes** depuis l'en-tête ; sur iPad, il le peut. **(b) `/projects/new` — 16 commandes supplémentaires en 768** : le bloc **« TYPE DE CRÉATION »** avec ses 10 puces (`Web`, `Mobile`, `Présentation`, `Animation`, `Design`, `Visualisation de données`, `Automatisation`, `Jeu 3D`, `Document`, `Tableur`), la section **« ESSAYER UN EXEMPLE »** avec ses 3 exemples de prompts et son bouton `Actualiser les exemples de prompts`, et la section **« MODÈLES — Partir du catalogue existant »**. En 390 px, tout cela est remplacé par un unique accordéon replié **`Options avancées — Web`** (seul libellé présent en 390 et absent en 768). Longueur de contenu : **1392 caractères en 390 contre 1793 en 768**. C'est la **surface de création de projet**, donc le parcours d'entrée du produit : un utilisateur iPhone ne voit ni les types de création, ni les exemples, ni les modèles. **(c) `/deployments`, `/search`, `/explore`, `/gallery`** — bandeau d'annonce marketing présent en 768 seulement (`Commencer`, `Fermer l'annonce`, `Parler à un expert d'E-Code…`). Écart réel, mais **d'impact faible** et plutôt favorable au mobile ; listé pour l'exhaustivité. **Seule `/settings` ne présente aucun écart.** | ☐ | ☐ | ✅ **28/08** mesuré live, 84 mesures, captures appariées | **Captures** : `docs/audit/evidence-2026-08-27/creation-projet-390.png` et `creation-projet-768.png` — la comparaison est directement lisible (en 768 : en-tête avec « ⌘ Rechercher » et EN/FR, 10 puces de type, 3 exemples, section Modèles ; en 390 : aucun des trois, accordéon « Options avancées » à la place). **Méthode** : 21 routes × {390, 768} × {clair, sombre} = **84 mesures**, comparaison des ensembles triés de libellés de contrôles visibles à conditions égales. Données : `/tmp/qa-sweep/mob/surfaces-art/surfaces.json`. **Résultats positifs du même balayage** : **zéro débordement horizontal** et **zéro image cassée** sur les 84 mesures. ⚠️ **`/admin` non couvert** : la route redirige vers `/dashboard` pour mon compte QA, qui n'a pas le rôle plateforme. Non testé — donc **ni conforme ni non conforme**. |
| BUG-QA-THUMBNAIL-500-001 | **3 vignettes de projet sur 6 renvoient HTTP 500 avec un corps vide en production.** `GET /api/projects/:id/thumbnail` → **500**, `content-length` absent, 0 octet, en **458 à 869 ms** (projets `…qjtuhjeb`, `…bummu3vk`, `…hrf98xad`). Les 3 autres répondent normalement (302 vers une URL signée, image 1280×800 servie en 450–1531 ms). **Le 500 vient du backend, pas du BFF** : `app/routes/api.projects.$projectId.thumbnail.ts` relaie le statut amont (`error.status === 404 ? 204 : new Response(null, {status: error.status})`) et ne fabrique lui-même qu'un 502. Un 500 ne peut donc provenir que de l'API. Côté API (`services/api/src/app.ts`, `GET /projects/:projectId/thumbnail`), tous les cas prévus rendent **404** — stockage désactivé, objet absent, dépassement de délai (`StorageDeadlineError`) ; le repli final est `sendObjectStorageError(reply, error)`. Un 500 correspond donc à une **erreur de stockage d'objets non anticipée**, distincte du cas « pas encore de vignette ». **Effet utilisateur limité mais réel** : la carte bascule sur « No preview yet », état correct, donc le défaut est silencieux — mais il masque une panne de stockage derrière un message rassurant, et ces 3 projets ne pourront jamais afficher d'aperçu tant que la cause n'est pas traitée. | ☐ | ☐ | ✅ **27/08** reproduit live prod, 6/6 projets testés | Repro et tableau des statuts : `docs/audit/evidence-2026-08-27/vignettes-tableau-de-bord.md`. ⚠️ **Cause exacte non identifiée** : je n'ai pas accès aux logs de l'API de production depuis cette session pour lire l'erreur derrière `sendObjectStorageError`. À compléter côté exploitation — c'est la première chose à regarder. |
| BUG-QA-THUMBNAIL-SLOW-PAGE-001 | **Sur le tableau de bord, une vignette pourtant valide met plus de 20 secondes à apparaître ; pendant tout ce temps la carte affiche « No preview yet ».** Mesuré en production : à **20 s** après chargement de la page, les 6 balises `<img>` de vignette sont encore `complete:false, naturalWidth:0` — donc **les 6 cartes** affichent l'état « aucun aperçu », y compris les 3 dont la vignette est parfaitement servie. Relevé plus tard, la même balise passe à `complete:true, naturalWidth:1280, visibility:visible`. En parallèle, la **même URL** chargée isolément via `new Image()` répond en **450 à 1531 ms**. L'écart n'est donc pas dû au backend. Les balises portent `loading="lazy"` alors qu'elles sont **dans le viewport** (`visible:true`) — piste à vérifier, de même qu'une éventuelle annulation/reprise des requêtes lors des rendus successifs de la page (la console émet par ailleurs des `AbortError: signal is aborted without reason` sur la restauration d'état de l'IDE). **Effet** : un utilisateur qui revient sur son tableau de bord voit d'abord « No preview yet » partout, ce qui est **faux** pour les projets qui ont un aperçu. C'est la première impression au retour sur le produit. | ☐ | ☐ | ✅ **27/08** mesuré live prod | Mesures et commandes : `docs/audit/evidence-2026-08-27/vignettes-tableau-de-bord.md`. ⚠️ **Piste non confirmée** : je n'ai pas isolé la cause exacte du retard (lazy-loading, concurrence des 6 requêtes, annulation au re-rendu). Le fait mesuré est l'écart entre **> 20 s sur la page** et **< 1,6 s en isolé**, sur la même URL et la même session. |
| BUG-QA-AUDIT-RELEASE-NAME-COLLISION-001 | **P1 sécurité d'exploitation — la release Helm de l'environnement d'audit porte EXACTEMENT le même nom et le même namespace que la production, et le runbook affirme le contraire.** Constaté : sur le cluster d'audit, `helm list -n vibecore` rend **`vibecore` / ns `vibecore` / révision 20** ; sur le cluster de prod, **`vibecore` / ns `vibecore` / révision 1085**. **Nom identique, namespace identique.** Le **seul** discriminant est donc le contexte `kubectl`/`--kube-context`. Or le contexte ambiant de ce poste était **`connectgateway_vibecore-495216_europe-west9_vibecore-prod-app`**, c'est-à-dire **la production** : un `helm upgrade vibecore -n vibecore …` tapé sans `--kube-context` explicite part **sur la prod**, sans aucun garde-fou. **Le runbook aggrave le risque au lieu de le réduire** : `docs/audit/TEST_ENV_RUNBOOK.md` §4 prescrit `helm upgrade --install vibecore-audit …` et annote « release `vibecore-audit` (⚠️ la prod s'appelle `vibecore` — ne pas confondre) ». Cette phrase laisse croire que **le nom** protège. Il ne protège pas : les deux s'appellent `vibecore`. Suivre le runbook à la lettre créerait en plus une **seconde** release parallèle sur le cluster d'audit, divergente de celle réellement déployée. Risque concret : un déploiement d'audit qui atterrit en production, ou un `helm rollback`/`helm uninstall` visant l'audit et frappant la prod. | ☐ | ☐ | ✅ **27/08** constaté en réel sur les deux clusters | `helm --kube-context gke_vibecore-audit-test-20260807_europe-west9-a_vibecore-audit-cluster list -n vibecore` → `vibecore  vibecore  20  vibecore-platform-0.1.0`. `helm --kube-context connectgateway_vibecore-495216_europe-west9_vibecore-prod-app list -n vibecore` → `vibecore  vibecore  1085  vibecore-platform-0.1.0`. `kubectl config current-context` → **contexte de PRODUCTION**. `env | grep HELM_` → aucune variable (donc aucun épinglage protecteur non plus). **Correctifs attendus** : (a) corriger le runbook — dire que la release d'audit s'appelle bien `vibecore` et que **le nom ne discrimine pas** ; (b) rendre `--kube-context` **obligatoire** dans toutes les commandes du runbook ; (c) ajouter aux scripts `scripts/audit-env/*.sh` une garde qui **refuse** de s'exécuter si le contexte résolu ne contient pas `vibecore-audit` (aujourd'hui ils refusent seulement si le contexte contient `vibecore-prod` — une garde par liste noire, qui laisse passer un contexte inattendu). |

### Passe 9 — mutations sur les panneaux (12/08) — partiellement concluante

| Piste | Verdict | Détail |
|---|---|---|
| **Instantanés — auto-checkpoint** | **Sain** | 5 instantanés créés **automatiquement** par l'agent lors de ses éditions (« Patch de l'IA accepté pour src/App.tsx » / « …src/App.css »), horodatés et labellisés. |
| **Instantanés — création manuelle** | **Anomalie** → `BUG-QA-PANEL-429-MASKED-001` | 429 (quota atteint) restitué en « The panel data could not be loaded ». |
| **Paquets — install** | **NON CONCLUANT — ne pas considérer comme un bug** | `POST …/ide-panel/packages {"intent":"install","packages":["is-odd"]}` → **500 `PANEL_REQUEST_FAILED`** (0,6 s, workspace `RUNNING`), et `is-odd` absent de `package.json` comme de `node_modules`. **Mais le contrat de l'action POST n'a PAS été établi** : je n'ai lu que le *loader* GET du panneau `packages`, et mon corps de requête est une supposition. Un 500 sur un payload non validé ne prouve rien. **À refaire** en lisant le vrai handler d'action (intents/champs attendus) ou, mieux, **depuis l'UI réelle** — d'autant que l'écriture de paquets peut, comme l'ActionRunner, dépendre du client. À rapprocher de `BUG-IDE-001` / `BUG-IDE-005` (correctifs sur branche **non mergée**) avant toute conclusion. |
| **Piège évité** | — | Premier essai d'install : **500 après 30 s** — cause réelle = **workspace GC'd entre-temps** (pod absent), pas un défaut d'install. Re-provisionné avant de retester. Rappel permanent : sur ce runtime paresseux, **toujours revérifier l'existence du pod avant d'interpréter une erreur**. |

### Passe 10 — mutations réelles sur les panneaux (12/08) — aucune anomalie

| Mutation | Verdict | Preuve |
|---|---|---|
| **Secrets — création** | **Saine** | `POST …/ide-panel/secrets {"intent":"create","key":"QA_SWEEP_SECRET",…}` → **200 `{ok:true}`**, clé présente à la relecture. |
| **Secrets — hygiène de la valeur** | **Saine** | La relecture du panneau **ne renvoie jamais la valeur en clair** (`'valeur-de-test-123' in réponse` = **False**), seule la clé est exposée. |
| **Secrets — suppression** | **Saine** | `{"intent":"delete"}` → **200**, puis clé **absente** de la relecture (`QA_SWEEP_SECRET in réponse` = False). Cycle create → read → delete complet. |
| **Git — commit** | **Sain** | `{"intent":"commit","message":"QA sweep: commit de test"}` → **200**, et le commit est **réel** : historique passé à 2 entrées, `180c6a0c QA sweep: commit de test` au-dessus de `fe508c72 chore: initial scaffold`, auteur `You`, 0 fichier modifié restant. |
| **Skills — bascule** | **Saine** | `{"intent":"toggle","skillId":"documentation","enabled":true}` → **200** avec l'objet compétence renvoyé, et la relecture confirme `documentation → enabled=True` (elle était désactivée par défaut). Bascule **persistée par projet**. |

| **Invitation d'organisation — création** | **Saine** | `POST /organization-members {intent=invite, orgId, email, roleKey=member}` → **200**, et l'invitation est **réellement persistée** : `qa-invite-0812@ecode-audit.test` apparaît à la relecture de `/organization-members.data`. Le premier essai en 400 venait de **ma** requête (`orgId` omis, champ `role` au lieu de `roleKey`) — validation serveur correcte, **pas un défaut**. |
| **Suppression de compte — garde de confirmation** | **Saine (non exécutée)** | Sur `/account-settings/data`, `intent=request` déclenche une **demande de suppression de compte** (et non un export, contrairement à ce que je supposais). **Non exécuté volontairement** — action destructrice sur le compte de test en pleine campagne. **Bon contrôle relevé à la lecture du code** : la confirmation est revalidée **côté serveur contre l'e-mail de la session courante** (`/auth/me`), jamais contre un champ de formulaire caché — commentaire explicite « never a hidden form field, which the client could tamper with ». |
| **Membres d'org / Sécurité — lectures** | **Saines** | `/organization-members.data` et `/security-settings.data` → **200** ; surfaces chargées sans erreur. Mutations restantes non exercées : changement de rôle, retrait de membre, transfert de propriété, MFA, révocation de session. |

> Ces cinq mutations sont propres — consignées comme contrôles négatifs. Avec le cycle complet des clés API (passe 7), ce sont les parcours les plus sains de la campagne.

### Passe 11 — dernières mutations (12/08)

| Mutation | Verdict | Preuve |
|---|---|---|
| **Invitation — révocation** | **Saine (faux positif évité)** | `intent=invite-revoke` → **200**, mais la ligne `OrganizationInvite` **persiste** et l'invitation reste listée : j'ai d'abord suspecté une révocation sans effet. **Vérification** : le BFF route correctement vers `POST /orgs/:orgId/invitations/:inviteId/expire` (`organization-members.tsx:160`) — **révoquer = expirer** par conception (« the invitation link stops working »), la ligne est **volontairement conservée**. L'`expiresAt` que je voyais « avancer » (00:07:56 → 00:08:33) était simplement mis à `now()` à chacun de mes deux appels, espacés de 37 s. **Comportement correct.** |
| **Changement de rôle sur soi-même** | **Garde active — 409** | `intent=update` sur mon propre `userId` → **409 Conflict** : impossible de modifier son propre rôle (protection contre l'auto-escalade / l'auto-démotion du dernier propriétaire). Réponse rendue en HTML par Remix, donc message non extractible en `curl` — l'UI l'affiche en bandeau. **Non retenu comme défaut.** |
| **Transfert de propriété** | **Contrat lu, NON exécuté** | `intent=transfer` → `POST /orgs/:orgId/memberships/:userId/transfer-ownership`, décrit comme « atomic server-side hand-off: promotes the target, demotes the caller ». **Volontairement non exécuté** : action destructrice irréversible sur le seul compte propriétaire de l'org de test. |
| **MFA / révocation de session** | **NON exercées** | Surfaces `security-settings` et `mfa-setup` présentes et chargées (**200**), mais les mutations n'ont pas été déclenchées — activer un MFA TOTP demande un secret partagé et un code valide, non automatisable ici sans authenticator. **Trou de couverture assumé.** |

## Balayage QA continu — env de test dédié (2026-08-15)

Environnement : `vibecore-audit-test-20260807` (`docs/audit/TEST_ENV_RUNBOOK.md`), compte de test
`qa-loop-1786822972@local.test` / org `org-2h3n3zrk`, projet `cmsusbw8q00040nbf7dddmsq1`,
workspace `ws-8837656e73850f1c`. **Aucun compte ni cluster de prod touché** — contexte kubectl
épinglé sur `gke_vibecore-audit-test-20260807_europe-west9-a_vibecore-audit-cluster`.

⚠️ **Réserve de version** — le cluster d'audit tourne un build **antérieur à `main`** et
**hétérogène** : 6 tiers sur `1c68880b39` (api, ai-gateway, admin, preview-proxy, screenshotter,
worker) mais **`web` sur `df1287d856`**, alors que `origin/main` est à `9fc9f812`. Les constats
purement **runtime** sont donc datés de ce build ; en revanche le défaut central
(**BUG-AGENT-001/002**) a été **re-vérifié dans le code de `origin/main`** et s'y trouve à
l'identique — il n'est pas un artefact de l'environnement de test.

### Passe 1 — parcours cœur « prompt → app → aperçu » (15/08)

| Étape | Verdict | Preuve |
|---|---|---|
| **Inscription** (`POST /auth/register`) | **Saine** | **201**, session + organisation créées dans la foulée. |
| **Connexion** (UI, desktop 1440) | **Saine** | Formulaire FR complet, redirection tableau de bord, **zéro erreur console**. |
| **Tableau de bord** | **Sain** | Onboarding 0/3, palette de commandes, capacité d'espace de travail rendus. |
| **Création de projet par prompt** | **Dégradée** | Le projet se crée et l'IDE s'ouvre (~35 s), mais voir BUG-AGENT-001. |
| **Génération agent** | **Dégradée — P0** | 20 fichiers annoncés « Terminé », **3 jamais écrits** dans le runtime (`src/App.tsx`, `src/hooks/useTasks.ts`, `src/components/AddTaskForm.tsx`), **750** `PUT /files/write`. → **BUG-AGENT-001**, **BUG-AGENT-002**, **BUG-AGENT-003**. |
| **Arbre de fichiers** | **Dégradée** | 9→10 fichiers affichés contre 20 réels. → **BUG-IDE-006**. |
| **Aperçu / Webview** | **Cassée** | Aucun serveur Vite (5173 refusé), `src/App.tsx` absent, overlay « Prêt » contradictoire. → **BUG-AGENT-001**, **BUG-UX-014**. |
| **Déploiements** | **Non exercé** | 0 déploiement — le projet n'était pas buildable. Lot sensible : consigné, laissé à la session dédiée. |

> **Écart de documentation** — le runbook (`docs/audit/TEST_ENV_RUNBOOK.md` §5) et la fiche
> d'accès listent encore les clés LLM comme **BLOCKED**. C'est **périmé** : le secret
> `vibecore-platform-secrets` du cluster d'audit porte bien `ANTHROPIC_API_KEY`, et l'agent a
> réellement tourné pendant cette passe. Restent effectivement absents : Stripe, OAuth
> Google/GitHub, Resend, et `OPENAI_API_KEY` (d'où `GET /agent-memory/preferences` → **503**
> `AGENT_MEMORY_UNCONFIGURED`, ×6 — dégradation à corriger côté UI, l'IDE affichant
> « Impossible de charger la mémoire de l'IDE du projet »).
